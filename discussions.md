# Discussions — unfinalized design review, not yet in the official knowledge-graph

**Status: scratchpad.** Nothing in this file has been promoted to `PROJECT.md` or any repo's `TASKS.md` yet. Once an item below is actually decided, move it into `PROJECT.md`'s "Threat analysis" section (and the relevant repo's `TASKS.md`) and delete or mark it resolved here — don't let this file and the official docs drift into disagreeing with each other. Until then, nobody should build against anything in this file as if it were settled.

Started 2026-09-13, reviewing `noxos-app` session 15's network pKVM cheap-filter + on-device classifier work (see `PROJECT.md`'s "Threat analysis" → "Network-side pKVM-gated cheap filter" and `noxos-app/TASKS.md` session 15).

---

## 1. Direction mismatch: the cheap filter may be inspecting the wrong side of the connection — OPEN, circling back

The stated reason for adding pKVM isolation to network traffic was closing the asymmetry with files: `PacketUtils`/`TcpRelayManager` parse network-controlled bytes unisolated in the host process today, the same risk class as the EXIF parser's real fuzzing-found heap overflow.

But the packet sampled for the VM check is captured "at the exact moment the destination is first flagged" — and a destination is flagged the moment `NetMonitorService`'s tun-read loop first sees traffic *to* it, i.e. the outbound packet an app on this device is sending out, generated by this device's own OS network stack. That's not attacker-controlled data. The genuinely hostile bytes — a malicious server's *response* — are parsed by `TcpRelayManager`'s reply-pump later, after the connection is live, and never reach this mechanism as currently specified, because they don't exist yet at flag-time.

**Not resolved.** If the goal is real, the sample likely needs to be inbound response data, captured after the connection is established, not the initiating outbound packet. Revisit before `noxos-payload` builds a guest-side check against data that was never the actual threat.

**Built, session 17 (`noxos-app` commit `bcfda8e`) — promoted to `PROJECT.md`'s addendum and `noxos-app/TASKS.md` session 17.** The sample now includes the first inbound reply alongside the outbound packet (see item 3's resolution below for the mechanism) — the guest-side check, once built, can and should look at the destination-controlled bytes now, not just the outbound one. Doesn't fully close the "is the check itself real" concern in item 4 below (that's still `noxos-payload`'s open design call), but the data it would need to be real is now actually captured.

## 2. Session-scoped forgetting — RESOLVED, by design, not a bug

Clarified by the user: session-scoping is the intended *default* tier, not a gap. The ACL itself is the permanence layer — it's local, persistent storage, and a user who wants a verdict to stick adds/promotes it there directly. "Whatever a session decides stays ephemeral until you make it permanent" is the actual two-tier model, working as intended.

**Residual note worth keeping in mind, not a bug**: a destination nobody ever manually curates into the ACL gets zero cross-session benefit from being resolved — it's re-flagged, re-VM-checked, and (if it escalates) re-asked of the AI/classifier every single session, indefinitely. Purely passive users get no accumulating protection; only actively-curated entries do. Fine as a design choice, just worth being deliberate that this is the real, permanent shape of it rather than something to "eventually improve."

## 3. In-memory packet buffer has no failure/recovery path — RESOLVED (direction agreed 2026-09-13, not built yet)

Concrete scenario: `NetworkSampleVmDispatcher` needs `NetMonitorService.pendingPacketSamples` (in-process RAM only) to actually run its check. If the VPN service process dies or restarts between "destination flagged" and "dispatcher picks it up" (battery-saver killing a background service, the user toggling monitoring off/on, a crash), the buffered packet is gone. The ACL row survives (it's a real DB row: `FLAGGED`, `cheapFilterChecked = false`) but nothing re-triggers capturing a fresh sample, nothing notices the row is stranded, and nothing surfaces this state anywhere (not the audit trail, not the ACL screen). That destination silently never resolves — not allowed, not blocked, not escalated — forever, with zero visibility into why.

**Needs a decision**, not just documentation: options include (a) persist the sample somewhere durable (a small table, not just RAM) until dispatched, (b) a staleness sweep that re-flags/re-samples a `FLAGGED` row that's sat unchecked past some window, or (c) explicitly accept the loss and make it visible (e.g. a distinct ACL state like "sample lost, needs a fresh connection to retry") rather than an indistinguishable-from-normal `FLAGGED`.

**Proposed resolution (2026-09-13), agreed direction — real code change, not built yet:**
- Rely on natural client retry behavior instead of building explicit staleness-recovery machinery. Concretely: `pendingPacketSamples` stops being "one packet, captured once, at flag time" and becomes **a per-destination buffer that keeps appending every packet seen for that destination for as long as it's unresolved** (`FLAGGED`, `cheapFilterChecked = false`). No explicit "retry detection" needed — a retried connection just generates more packets, which land in the same still-open buffer; whenever the dispatcher's next poll runs, it grabs whatever's accumulated (one packet or several) and dispatches that as the sample.
- **If never retried**: the row just stays `FLAGGED` with an empty/never-flushed buffer, indefinitely. Accepted as fine — the connection was already relayed live at first contact (existing cost-gating behavior), so a permanently-unresolved row isn't blocking anything, just permanently uncategorized. Same shape of accepted tradeoff as item 2's "passive users get no cross-session carryover."
- **Concurrency — decided 2026-09-13: mutex lock**, not a lock-free/accepted-race approach. A plain `Mutex`-guarded buffer (or `synchronized`) between the capture-loop coroutine (appending) and the dispatcher (reading + clearing) — traffic volume here is far too low for lock contention to ever be a real cost, so this removes the race for free rather than budgeting for a rare lost packet.
- **Reuses item 7's structure**: this accumulating per-destination buffer is the same data structure item 7's batching proposal needs (batch N destinations' samples into one VM call) — build it once, it serves both.

**Built, session 17 (`noxos-app` commit `bcfda8e`) — two honest deviations from the proposal above, flagged not hidden:**
- **Concurrency: `CopyOnWriteArrayList` per destination, not an explicit `Mutex`.** Functionally equivalent for this use (its own `add()` is internally synchronized, safe for the capture-loop coroutine and both relay paths to append from concurrently, safe for the dispatcher to iterate/remove) — just a different, arguably simpler mechanism than the literal "mutex-guarded buffer" this item proposed. No lock object, no `synchronized` block, same "no lost writes" guarantee.
- **Capped at 2 samples per destination, not unbounded until dispatched.** The proposal above argued a cap wasn't needed ("traffic volume here is far too low for lock contention to ever be a real cost") — true for contention, but a chatty still-unresolved flow could otherwise buffer many packets in the 30s window before the dispatcher's next poll, which is an unnecessary memory/privacy cost for a mechanism that only needs "the outbound trigger plus one inbound sample" to do its job. `appendPendingSample()` silently stops appending past 2 entries per destination rather than growing further — the retry-driven recovery behavior (a retried connection just adds to whatever's already buffered) still works exactly as designed, it just doesn't accumulate every single packet of a long-lived, still-uncategorized flow.
- Batching multiple *destinations* into one VM call (item 7) is unaffected either way — still not built, still a separate, larger design question.

**Correction, session 19, from a peer review — the "Built, session 17" claim above was wrong on the retry premise itself.** The accumulating buffer (`CopyOnWriteArrayList`) really was built and works — but the *capture gate* that would need to fire again for a retry to land anything in it never got touched: `NetMonitorService`'s capture loop only appends to `pendingPacketSamples` inside `if (verdict == null && ...)`, and once a destination is `FLAGGED`, `aclCache[dstIpStr]` is never `null` again. A retried connection to an already-flagged, still-unresolved destination generates real packets, but none of them re-enter that block — the buffer never actually receives a second sample from a retry, contrary to what "the retry-driven recovery behavior... still works exactly as designed" claimed. In practice this made the design fall back to the *original* accepted-loss shape (a stranded entry just sits forever) — except worse, because `nextCheapFilterBatch()`'s oldest-first-no-cutoff query meant a stranded entry also permanently occupies a batch slot, silently starving every destination flagged after it once enough accumulate. **Real fix, session 19 (`noxos-app` commit `00da1ce`)**: a 5-minute staleness cutoff on `nextCheapFilterBatch()` — option (c) from the original 2026-09-13 discussion ("explicitly accept the loss... rather than an indistinguishable-from-normal FLAGGED", partially — it's not yet *visible* anywhere, just no longer *starving*), simpler than fixing the capture gate and doesn't depend on retry timing at all. Full detail in `noxos-app/TASKS.md` session 19. Lesson for future "Built" notes in this file: verify the actual gate/call site the design depends on, not just that the data structure it was supposed to feed exists and behaves correctly in isolation.

## 4. Wire contract locked ahead of the actual security semantics — acknowledged deliberate, still needs a real spec

User's framing: the contract was locked *specifically* so `noxos-payload` (a different agent) isn't blocked waiting on this repo — reasonable, and consistent with why this session exists (laying ground framework/API contracts across agents). Not disputing that sequencing.

Still true and still needs resolving before `noxos-payload` starts implementing: `noxos-payload/TASKS.md` currently says the actual check "is this repo's design call... the exact heuristics aren't specified anywhere." Without a concrete spec, there's real risk of a check that's technically real but provides no security value (e.g. "IP version is 4 and checksum is valid" — true of literally every packet that reaches the tun, since the kernel already guarantees that) while still paying a VM-boot cost per destination.

**Candidate starting spec, for review before handing to `noxos-payload` (not decided, just a starting draft):**
- Declared IP total-length field vs. actual received byte count (mismatch = flag).
- For TCP: SYN sample specifically — window size / flags sanity (e.g. reserved bits set, contradictory flag combos like SYN+FIN together).
- For UDP: declared UDP length field vs. actual datagram size.
- Whatever check is chosen should be validated against point 1 above first — inspecting the outbound SYN for internal consistency has limited value if the real threat is inbound response data.

## 5. On-device classifier: real signal today is close to a free existing function — OPEN

`noxos-app` can currently supply exactly two features per destination: `dst_port` (usable) and `protocol` (not yet numeric-encoded, so effectively unusable by the interpreter as shipped). Every other feature the model expects (the other ~37 UNSW-NB15 columns) falls back to `feature_defaults`, which were deliberately computed as "normal traffic" medians/modes. So the model's actual, real-world decision today tracks `dst_port` almost exclusively.

The codebase already has `NetworkPriority.classify(port)` — a free, already-shipped, zero-dependency function doing exactly that (standard vs. non-standard port → LOW/HIGH priority).

**Open question, not resolved**: is it worth wiring the on-device classifier into the live pipeline before real per-flow feature capture (volume/frequency counters, deliberately not built due to the per-packet-DB-write cost concern already flagged in `noxos-inference/TASKS.md`) exists? As specified today, wiring it in would likely add JSON-asset size, tree-walk cost, and export-pipeline maintenance for a decision that's currently indistinguishable from the one-liner already in production.

**Sequencing agreed 2026-09-13**: model choice (item 8) has to be finalized *before* the real feature list can be decided — what packet-derived data actually needs capturing/persisting follows from what the chosen model needs, not the other way around. No feature-capture work should start until item 8 is resolved.

## 6. XGBoost `dump_model` → custom JSON transform: real edge-case risk, not "mechanical" — OPEN

`noxos-inference/TASKS.md` describes the export as "a small recursive rewrite... a mechanical transform, not a retrain." One specific risk that framing undersells: XGBoost's real dump format identifies a missing-value branch by a **child node ID** (`missing: <id>`), and that ID has to be matched against the node's actual `yes`/`no` children to determine whether "missing" means "go left" or "go right" in the simplified `default_left` boolean this interpreter expects — it is not guaranteed to always be positionally `children[0]`.

Get this wrong and nothing crashes or errors — the interpreter just silently gives the wrong answer specifically on the missing-value path, which (per item 5 above) is the dominant path for nearly every real prediction right now, since almost every feature is currently "missing" and defaulted. This is exactly the failure mode you don't want in a security gate: wrong and silent, not wrong and loud.

**Needs a real test once an actual export exists**: verify the interpreter against a real `booster.dump_model()` output from the actual trained model, not just the hand-written synthetic two-tree fixture currently used in `OnDeviceNetworkClassifierTest` — that fixture proves the interpreter's own logic is self-consistent, not that it correctly reads what XGBoost actually produces.

**Recommended next step (2026-09-13): resolve item 8 first, because it changes whether this risk exists at all.**
1. If item 8 lands on a **scikit-learn-based tree ensemble** (Random Forest, or any sklearn `DecisionTreeClassifier`-based model): this entire risk category disappears. sklearn trees are plain threshold-only binary splits (`feature`/`threshold`/`children_left`/`children_right`) with no native missing-value/default-branch semantics — missing features get imputed (filled with the same `feature_defaults` infrastructure already built) *before* the tree ever sees them, not handled inside a `yes`/`no`/`missing` node the way XGBoost's dump format does. This is a concrete, separate reason to prefer Random Forest here, beyond accuracy or phone-weight concerns (see item 8) — it removes a whole class of silent-wrong-answer bug, not just a training-quality one.
2. If XGBoost is kept anyway: don't implement the transform as a positional/mechanical rewrite. Match `yes`/`no` child IDs explicitly to determine the missing-value branch, and write a real parity test — run the actual trained Python model and the Kotlin interpreter against identical feature vectors, including deliberately-missing ones, and assert they agree — before shipping.
3. Either way: hold off building the actual export script (`training/export_ondevice_model.py`) until item 8 is decided. Building and testing an XGBoost-specific transform is wasted work if the model changes.

## 7. VM-boot cost per newly-flagged destination vs. Pillar 1 (Lightweight) — proposal: batching

**Problem restated**: normal browsing can hit many genuinely-new destinations quickly (CDNs, ad/tracking domains, font hosts, API endpoints — often a dozen-plus per page load), and combined with item 2's per-session reset, this could mean a real burst of one-shot VM boots at the start of every session, in tension with the "keep the common case cheap" framing and the project's own Lightweight pillar.

**User's proposed fix: batch processing.** Instead of one VM per newly-flagged destination, batch N unique destinations' packet samples (e.g. up to some count, or up to some short time window) into a single VM invocation. Tradeoff, explicitly accepted: the *first* batch of a browsing burst pays a real, noticeable lag waiting for the batch to fill/flush and the VM to process it; every destination in that batch, once classified, resolves and its subsequent traffic flows at full speed with no further delay. This mirrors the existing "first contact always relays live regardless of ACL state" behavior — nothing about live relay changes, only how quickly a batch of pending verdicts gets resolved.

**Not yet designed, real open questions**:
- Batch trigger: fixed count, fixed time window, or whichever fires first?
- What does the payload's response shape look like for a batch (one verdict per sample, keyed how)?
- Does a batch mix samples from unrelated destinations in one VM invocation change anything about the "audit trail per destination" story, or is it still one ACL write per destination out of one VM call?

## 8. On-device model choice: XGBoost vs. Random Forest — OPEN, needs a decision with `noxos-inference`

**Input from the user's friend**: XGBoost run with too few boosting iterations (1-2) gives poor results; needs roughly 16-32+ to be any good; suggested considering Random Forest instead, on the concern that XGBoost might be "too heavy for a phone."

**My technical read on this, for the record — flagging a nuance, not overriding the suggestion:**
- The part that's correct: a gradient-boosted model genuinely does underfit badly with too few boosting rounds — that's real and worth checking. **Fact-check needed, not yet done**: nobody has recorded what `training/train_network_model.py`'s actual `n_estimators` is. The already-reported 95.75% accuracy / 0.969 F1 suggests it's probably not egregiously underfit, but this should be confirmed directly rather than assumed either way, same spirit as the earlier `AclEntity` fact-check.
- The part I'd push back on gently: "XGBoost is heavy for a phone" isn't quite the right framing for *this* architecture specifically, because the phone never runs XGBoost at all — it runs a plain hand-rolled tree-walk interpreter over an exported JSON (that's the whole point of the on-device design, avoiding a real ML runtime). Walking a tree is walking a tree regardless of whether XGBoost or Random Forest produced it; the phone-side cost is a function of total tree count × depth, not which training algorithm was used. So the real phone-cost question is "how many trees, how deep" — not "which library."
- If a move to Random Forest happens anyway (for accuracy/robustness reasons, which is a legitimate reason on its own): it's not a drop-in swap on the export side. Random Forest is a bagging ensemble (majority vote / averaged probability across independent trees), not a boosted one (summed margin → sigmoid) — the interpreter's actual math and the export JSON's top-level fields (`base_score`, margin-summing) documented in `noxos-inference/TASKS.md` would need real changes, not just a retrain with the same shape.
- Also worth weighing before deciding: Random Forests often need *more* trees than a well-tuned boosted model to reach comparable accuracy (commonly 100+ for stability) — which could mean a *larger* on-device asset and *more* tree-walks per prediction, not fewer. Not disqualifying, just a real tradeoff against the "lighter for phone" motivation, worth checking empirically rather than assuming either direction.

**Not decided.** Needs a joint call with whoever's driving `noxos-inference`'s training work — this changes what gets exported, not just how `noxos-app` reads it.

## 10. On-device vs. server model scope, decided 2026-09-14

**Two branches exist in `noxos-inference`** (`feature/on-device-xgboost-classifier`, `feature/ml-server-architecture`, both currently at the same commit as `main`, no divergent work yet) — **decided these are genuinely two different models, not one artifact served two ways**:
- **On-device**: the lean model, scoped to exactly what `noxos-app` can supply without new capture work (see below). Ships as the exported tree JSON, evaluated by `OnDeviceNetworkClassifier`.
- **Server (`feature/ml-server-architecture`)**: deliberately **richer**, not just a hosted copy of the on-device model. Scope not fully nailed down yet beyond "genuinely more than what the phone sends" — candidates are an external threat-intel/reputation lookup, cross-device aggregated signal, or compute-heavy work that only makes sense off-phone (e.g. real SHAP-based explainability generation, which is legitimately too heavy to run per-request on a phone). Not designed in detail yet — flagged here so nobody builds it as a same-model reskin by default.

**On-device feature set, resolved empirically, not guessed** — real diagnostic run against exactly the fields `AclEntity` (schema v8) stores today, no more, no less:

```
Exactly what AclEntity has TODAY: proto,sbytes,spkts,dbytes,dpkts,dur,tcprtt    7   0.9440   0.9597
Same, minus tcprtt (UDP never populates handshakeLatencyMillis)                6   0.9436   0.9593
Same 7, plus service+state (would need new capture code, not stored today)     9   0.9453   0.9605
Full 42-feature baseline (reference ceiling)                                   42  0.9575   0.9690
```

**Answers the standing "do we need a new dataset" question from the "What to do next" section above: no.** A model trained on exactly what Warden can supply today — `proto`, `srcByteCount`→`sbytes`, `srcPacketCount`→`spkts`, `dstByteCount`→`dbytes`, `dstPacketCount`→`dpkts`, `durationMillis`→`dur`, `handshakeLatencyMillis`→`tcprtt` — gets 94.40%, only 1.35 points off the architecturally-unreachable 42-feature ceiling. UNSW-NB15 is sufficient for the on-device model; no need to go dataset-hunting for this piece. `service`/`state` would need new capture code (port-lookup table, TCP-state tracking) for a marginal +0.13 points — not worth building for that alone right now. `destPort` (also stored on `AclEntity`) has no matching column in this dataset at all and isn't fed to the model — it stays useful only for the existing non-ML `NetworkPriority.classify(port)` heuristic.

**Real unit-reconciliation task, not yet done**: `durationMillis`/`handshakeLatencyMillis` are milliseconds; UNSW-NB15's `dur`/`tcprtt` columns are seconds. The export/training pipeline needs to convert consistently (train on the same units the app will send, or convert on one side) — not just plug milliseconds into a model trained on seconds.

**Decided next step**: retrain `service/model_network.joblib` (or a new on-device-specific artifact) on exactly this 6-7 column set, matching units to what `noxos-app` actually sends, on the `feature/on-device-xgboost-classifier` branch.

### Dataset switch, decided 2026-09-14 — not started, download begins next session

Investigated whether the `service` column's weak signal (item above) was fixable with real port numbers instead. Checked the same HuggingFace repo (`Mouwiya/UNSW-NB15`) for a fuller version — it has one: a `data/*.parquet` pair (2 files, ~230MB total) containing the **real, unreduced UNSW-NB15 dataset, 2,280,083 rows** (vs. the 175,341-row `UNSW_NB15_training-set.csv` used so far), with real `sport`/`dsport` columns present (a small, known quirk: 282 rows have hex-formatted or `-` port values, e.g. `0xc0a8` — trivially cleaned/dropped, negligible against 2.28M rows).

**Real diagnostic run against this fuller dataset, F1 used as the fair comparison (accuracy alone is inflated — this dataset's real class balance is ~11% attack, unlike the reduced set):**
```
Warden's current 7 fields (proto,sbytes,Spkts,dbytes,Dpkts,dur,tcprtt)   F1 = 0.9679
Same 7 + real dsport                                                     F1 = 0.9715
dsport + proto alone (no volume/timing features)                         F1 = 0.3125
```

**Decided: switch the whole training pipeline to this fuller dataset, for two independent reasons, not just the port question:**
1. **Real `dsport` gives a genuine ~11% relative error-rate reduction** (F1 0.9679→0.9715, i.e. error rate 3.21%→2.85%) over Warden's current 7 fields — free to add, since `destPort` is already captured and stored on `AclEntity` today; this just means actually training on it and sending it.
2. **The 175K-row reduced set is very likely artificially class-balanced** by its original curators (a common ML-benchmarking convenience) — training thresholds on it risks miscalibration against Warden's real deployment, where attacks are genuinely rare, not lab-balanced. The fuller dataset's ~11% attack rate is a much closer match to reality.

**Not started yet** — this session only confirmed the data exists, is clean enough to use, and is worth switching to. `training/train_network_model.py` hasn't been touched. Download begins next session. Real column-naming note for whoever builds this: the fuller dataset uses different casing/names for some columns than the reduced set (`Spkts`/`Dpkts` not `spkts`/`dpkts`, `smeansz`/`dmeansz` not `smean`/`dmean`) — don't assume the two schemas line up 1:1 by name.

## 9. The real feature contract, checked directly against code and the trained model (2026-09-13) — the dataset/deployment mismatch is worse than assumed

Verified directly (real CSV header, real `train_network_model.py`, real `service/app.py`, the real trained artifact's `feature_importances_`) rather than reasoned about secondhand. Findings, most important first:

**(a) Today, nothing `noxos-app` sends reaches the model at all — a real bug, not just feature starvation.** `service/app.py`'s `encode_request()` looks up each of the model's 42 real column names by exact string match in the JSON payload (`payload.get(col, default)`). `noxos-app` sends `ip`/`priority`/`reason`/`first_flagged_epoch_millis`/`dst_port`/`protocol`. None of those match a real column name — there is **no port field anywhere in the training data at all** (checked the raw CSV header directly), and `protocol` isn't `proto`. Even fixing the name wouldn't be enough: `proto`'s learned vocabulary is lowercase (`'tcp'`, `'udp'`, ... 133 values total, checked against the real model artifact), and `noxos-app` sends `"TCP"`/`"UDP"`/`"OTHER"` — a case/vocabulary mismatch that would still resolve to "unknown category" every time. **Net effect: every real request today gets the exact same normal-defaulted prediction, regardless of destination.** This is a real, fixable contract bug between `noxos-app` and `noxos-inference`, separate from the deeper feature-availability question below.

**(b) The real training feature set: 42 columns (39 numeric + 3 categorical), not 39 as `training/FEATURES.md` currently states** (off by the 3 categorical columns — worth a doc fix whenever this area gets touched again). Categorical vocabularies, from the real trained artifact:
- `proto`: 133 values (general network-protocol names — `tcp`, `udp`, `icmp`, `ospf`, `arp`, `sctp`, ...)
- `service`: 13 values (`http`, `dns`, `ftp`, `ssh`, `ssl`, `smtp`, ... plus `-` for unknown)
- `state`: 7 values (`CON`, `FIN`, `INT`, `REQ`, `RST`, `ECO`, `PAR`)

**(c) `service` is unusable as a real signal, and this isn't a Warden-side gap — it's baked into the dataset itself.** Checked the real distribution: `service = '-'` (unknown) in **53.7% of the entire training set**. More than half the original data never had a real service label. This is very likely why `service` ranked low in feature importance (0.0068, rank 14/42) even for the model that has full access to every column — it's genuinely low-information, not just hard to compute on-device.

**(d) The single dominant feature (`sttl`, 74.7% of the model's total `feature_importances_`) may not transfer to Warden's deployment at all.** `sttl` is the *connection-initiator's* IP TTL. In Warden's setup the initiator is always the phone — every outbound packet carries whatever fixed default Android's network stack sets, which won't meaningfully vary by destination. The reason `sttl` is so predictive in the original UNSW-NB15 dataset is very plausibly a testbed artifact: that dataset was captured between distinct attacker/victim lab machines that likely ran different OS images with different default TTLs — a documented failure mode in older NIDS benchmark datasets, where a model learns "which lab machine generated this" rather than "is this malicious." **Not proven — I can't test this without real phone traffic — but a serious, specific reason to distrust the 95.75% headline accuracy for this deployment specifically**, not a vague "small feature set = worse" concern.
- `dttl` (the *destination's* TTL — the one that could plausibly vary by which real server you're talking to) scored **0.0000** importance in the actual trained model. Whether that's a real null result or another testbed artifact of the same kind isn't known from here.

**(e) Full real feature-importance ranking (top 16 of 42), for reference:**
```
1.  sttl              0.7470
2.  ct_srv_dst        0.0383
3.  swin              0.0215
4.  ct_dst_sport_ltm  0.0210
5.  sbytes            0.0202
6.  smean             0.0154
7.  trans_depth       0.0105
8.  dpkts             0.0103
9.  dmean             0.0102
10. proto             0.0095
11. sloss             0.0092
12. ct_state_ttl      0.0084
13. ct_dst_src_ltm    0.0082
14. service           0.0068
15. dbytes            0.0066
16. state             0.0064
...
39. dttl              0.0000
40. dwin              0.0000
```

**Status: this reframes item 5 and item 1 into one bigger open question — see "What to do next" below.**

## What to do next (decided 2026-09-13) — sequencing, not a final architecture call

Four options were weighed, ranked cheapest-to-most-thorough:
1. **Cheap diagnostic**: retrain on the existing UNSW-NB15 data but restricted to only the columns Warden can realistically supply (`proto` properly encoded, `sttl`, `swin`/`dwin`, buffer-accumulated packet-count/duration proxies) and check the real accuracy/F1 on the same held-out split. Data's already downloaded, the training script already exists — near-zero cost, turns "feels like it won't work" into a real number.
2. **Reconsider the signal, not just the feature count**: parse the TLS ClientHello's SNI extension (the destination hostname, sent in cleartext even under TLS 1.3, readable from the very first outbound packet of a TLS connection) as the primary on-device signal instead of flow statistics — domain reputation / DGA-heuristic style classification, structurally a better fit for what a phone-side, payload-blind, mostly-TLS vantage point can actually see than porting a flow-stats model down ever will be. `PROJECT.md` already mentions SNI in passing as a future threat-intel seed source; this would elevate it to a primary signal, not just a seed list input.
3. **NetFlow-reformulated versions of the same benchmark datasets** (e.g. `NF-UNSW-NB15-v2`, `NF-CICIDS2018-v2`) — built around router/tap-observable fields only (5-tuple, byte/packet counts, duration, TCP flags), a closer conceptual match to Warden's vantage point than UNSW-NB15's original 42-column schema. Not verified in detail yet — a lead, not a confirmed fit.
4. **Self-collected data from Warden's own real capture/feature-extraction code**, labeled against known-benign traffic and known-malicious samples (e.g. public malware-traffic-analysis.net PCAPs). Fully closes the vantage-point gap, but real ongoing effort for a solo project — a later option, not a blocker now.

**Decided sequencing**:
- **Do #1 now** — next concrete action, not yet run.
- **#2 (SNI/domain-based) is parked, explicitly to circle back to** — not abandoned, just not the immediate next step.
- **User's own action item, in parallel**: check whether a dataset already exists that's a genuinely better fit for Warden's real vantage point (this covers evaluating #3-style leads and anything else found) before committing further engineering effort to UNSW-NB15. **If nothing better turns up, fall back to working with UNSW-NB15** via #1's restricted-feature retrain — not abandoning it outright, just not defaulting to it without a real look around first.

### #1 run, 2026-09-13 — real numbers, and a significant correction to item 9(d)

Ran the restricted-feature retrain (same data, same script, real held-out split) across several realistic feature-tier variants:

```
Variant                                                                #feat    Acc      F1
Tier A only (proto, sttl, swin, dwin)                                     4   0.9297   0.9509
Tier A + service/state (approximated)                                     6   0.9358   0.9550
Tier A + B (adds spkts/dpkts/dur proxies)                                 7   0.9374   0.9559
Everything gettable without ongoing state tracking                       10   0.9438   0.9595
Full 42-feature baseline (today's actual server model)                   42   0.9575   0.9690
```

A small feature set holds up much better than expected — only ~2.8 points behind the full model. **But this number is misleading on its own**, and digging into *why* it holds up produced the important correction:

```
SOURCE-side only (sttl, swin) - reflects the PHONE, not the destination    2   0.9232   0.9465
DESTINATION-side only (dttl, dwin) - reflects the actual destination       2   0.9074   0.9363
proto/service/state only, no numeric stats                                 3   0.8164   0.8596
Honest "destination-observable" set (14 cols: proto/service/state +
all d*/connection-level columns)                                          14   0.9393   0.9567
```

**Correction to item 9(d)**: `dttl`/`dwin` scoring `0.0000` importance in the *full* 42-feature model does **not** mean they're uninformative — it means they're *redundant* once `sttl`/`swin` are already available (trees credit whichever correlated feature gets split on first). Tested alone, `dttl`/`dwin` reach 90.7% — barely behind `sttl`/`swin`'s 92.3% — and unlike `sttl`/`swin`, they genuinely reflect the *destination's* behavior, not an artifact of which phone sent the request.

**This changes the priority of item 1, not just item 9.** Item 1 (the pKVM sample being the outbound initiating packet, not the destination's inbound response) was flagged purely as a security-isolation concern. It's now also **the specific fix that unlocks a legitimately decent, causally-sound on-device model**: the honest 14-feature destination-observable set (which requires inbound capture to populate `dttl`/`dwin` and friends) reaches **93.9% / 0.9567** — only 1.8 points behind the full server model, using only features that could actually mean something for Warden's purpose. **Item 1 should be treated as higher priority now, not purely theoretical** — resolving it isn't just closing a security gap, it's the path to the best on-device model result found so far, better than anything achievable from source-side (outbound-only) data.

**Not yet answered**: this still doesn't resolve *how* to get inbound response bytes into the pVM sample given the async/one-shot dispatch design (see item 1 and item 3) — that's real design work, not done by this diagnostic. This diagnostic only establishes that it's worth doing.

## 10. Live on-device testing (session 18, 2026-09-13) — four real bugs found, force-allow confirmed genuinely broken until now, and a hard confirmation of the real-VM timeline

Not a design question — a report of what actually happened testing `noxos-app` for real on a fresh Cuttlefish build, included here (not just `noxos-app/TASKS.md`) because it bears directly on items above and on planning conversations that reference "does force-allow actually work" or "when does the real VM path come online."

**Hard confirmation for item 1/4/7's planning**: `android.permission.MANAGE_VIRTUAL_MACHINE` is signature-level. A regular `adb install`-ed APK gets `SecurityException: Virtmgr didn't send any data through pipe` on every VM attempt (file-scan and network-sample both), even though the manifest already declares the permission, even on a build with the real `android.software.virtualization_framework` feature and `com.android.microdroid.empty_payload` present. **Nothing about the wire contract, the cheap-filter design, or the on-device classifier plan can be exercised for real until Warden is a priv-app/platform-signed install** (tracked in `noxos-os/TASKS.md` Phase 1c) — this isn't a `noxos-payload`/`noxos-inference` blocker, it's a `noxos-os` integration blocker that sits in front of all of them. Worth being explicit about this in any planning conversation about timelines.

**The original "user can force and allow the file" requirement (the very first design discussion of this whole feature, months ago) was confirmed broken until this session, then fixed and verified.** Force-allow physically restored the file, but `FileArrivalWatcher`'s own `ContentObserver` immediately re-detected the restored file as a new arrival and re-quarantined it within seconds — a real loop, confirmed live (not hypothesized), fixed with a small one-shot exemption set. Worth knowing if this ever gets discussed as "have we actually verified force-allow works" — the honest answer before today was no, and it was silently broken since quarantine was first built (session 14), never caught because nobody had exercised the full quarantine→restore round-trip live until now.

**Real user policy decision made mid-session, changes the honest current behavior described in `PROJECT.md`/`noxos-app/TASKS.md`'s earlier session write-ups**: quarantine now triggers on *any* non-`Success` scan outcome (a parser-confirmed `Failure`, or a scan that couldn't even run, `Error`) — not just a parser-confirmed `Failure` as originally built in session 14. Given the current state (no real VM anywhere on any device), this means **every single auto-scanned file gets quarantined right now**, always, regardless of content — an intentional, explicit fail-closed choice by the user given the real permission gap above, not an oversight. Worth surfacing in any discussion of "why does everything get quarantined" — that's expected until the priv-app path lands, not a bug.

Three other real, smaller bugs (network-analysis request sending a field the model can't use, a UI state that didn't reflect a really-running service, auto-scanned files displaying a MediaStore row number instead of a filename) — full detail in `noxos-app/TASKS.md` session 18, not repeated here since they're closed and don't need further discussion, just a record that they existed.

**v2.0.0 released** with all of the above plus everything from sessions 14-17.

## 11. On-device feature set, LOCKED 2026-09-14 — 10 features, F1 0.9724, build against this

Two more real diagnostics run before locking: `smean`/`dmean` as *derived* ratios (`sbytes/spkts`, `dbytes/dpkts` — no new capture, just a division on counters `noxos-app` already collects) gave a real, if small, gain (`0.9715 → 0.9724`). `is_sm_ips_ports` gave nothing (`0.9715 → 0.9715`) — dropped.

**Final list (10 fields):** `proto`, `dsport` (from `destPort`), `srcByteCount`, `srcPacketCount`, `dstByteCount`, `dstPacketCount`, `durationMillis`, `handshakeLatencyMillis`, plus derived `smean` (`srcByteCount/srcPacketCount`) and `dmean` (`dstByteCount/dstPacketCount`).

**Real unit-reconciliation still required before training** (flagged earlier, restating so it isn't lost): `durationMillis`/`handshakeLatencyMillis` are milliseconds on the `noxos-app` side; the dataset's `dur`/`tcprtt` are seconds. Convert consistently — train on the same units the app will actually send, don't just plug ms into a seconds-trained model.

**On-device compute estimate, reasoned not measured**: `n_estimators=200`, `max_depth=6` (current model's real hyperparameters) means one prediction walks 200 trees, at most 6 threshold comparisons each — roughly 1,200 simple comparisons plus 200 additions and one sigmoid call, total. That's a trivial amount of arithmetic for any phone CPU from the last decade — the real-world cost will be dominated by interpreter/object overhead (Kotlin tree traversal, not raw FLOPs), not the math itself. Expect low tens of microseconds to a fraction of a millisecond per evaluation, and this only runs once per newly-flagged destination (Tier 1 of the cascade below), not per-packet — nowhere close to perceptible, let alone laggy. Not benchmarked on a real device yet; a quick 10,000-iteration local loop would confirm this cheaply whenever there's a real exported model to test against.

## 12. On-device vs. EC2, resolved 2026-09-14 — see `PROJECT.md`'s "Resolved 2026-09-14" block

Not a primary/fallback choice — a two-tier cascade (cheap filter → on-device classifier → EC2, escalating only the residue each stage doesn't resolve). Full diagram and reasoning now in `PROJECT.md`; task to wire it tracked in `noxos-app/TASKS.md`.

## 13. Server model (Tier 2 / `feature/ml-server-architecture`) scope, decided 2026-09-14

Following on from item 9's server/on-device split decision — this is what "genuinely richer" concretely means, decided in a dedicated brainstorm. **Honest scale note up front**: this is a solo project, not a fleet of devices — cross-device aggregated intelligence (the usual reason a cloud security model beats an on-device one) isn't realistically available here, and claiming it would be the same kind of inflated framing the CARE-X pivot already rejected ("don't fake a multi-institution setup with one dataset"). Scoped to what's actually real at this project's scale instead.

**Decided components:**

1. **Persistent incident history feeds the bigger model, as an input feature.** The on-device ACL is deliberately session-scoped (forgets AI verdicts every session, by design — item 2). The server has no reason to forget — "has this destination ever been flagged before, across every session this device has run" is a real feature only Tier 2 can hold, independent of any fleet-scale story. This alone justifies Tier 2 existing.
2. **External threat-intel source: AbuseIPDB, confirmed via its real docs (not assumed).** Two endpoints, both used, different roles:
   - `blacklist` — bulk list of reported IPs (up to 10,000), free tier: **5 pulls/day**. Pull once daily, cache locally, use as a fast first-pass check. Real caveat: only includes IPs above a 25% confidence floor — coarser than a live check, fine for a first pass only.
   - `check` — single-IP lookup with a real confidence score, free tier: **1,000/day**. Used live for actual Tier-2 escalations, which need precision the cached blacklist doesn't have.
   - **Fallback, not co-primary**: a static free blocklist (FireHOL level1/level2, or Spamhaus DROP) for whenever AbuseIPDB itself is unreachable — same graceful-degradation pattern as `service/clamav_client.py`'s "uncertain, not a crash" convention.
3. **Enrichment is a lookup/cache, not something that "learns" — clarifying a real conflation from the brainstorm.** AbuseIPDB doesn't self-learn from being queried; it's read-only reference data someone else maintains. The *model that consumes* the enrichment result is what retrains periodically — don't confuse "refresh the cache on a schedule" with "the enrichment step is learning."
4. **Checked and rejected: using the training dataset's own `srcip`/`dstip` as a real-world IP-reputation source.** UNSW-NB15's IP addresses are synthetic, lab-internal addresses generated by the IXIA PerfectStorm tool inside ACCS's cyber range — they have no relationship to real internet addresses Warden will ever see. Mechanically extractable, but would mean memorizing lab addressing, not learning anything that transfers. Real threat-intel has to come from a real-world source (AbuseIPDB) — the dataset itself can't supply this feature no matter how it's sliced.
5. **Human-feedback batch retraining, periodic, not online learning** (same conclusion as item 9's "self-learning" clarification, now with real safeguards specified). A scheduled batch job pulls human-confirmed labels (a user's manual ACL override is exactly a confirmed label) and retrains on a cadence. **Data-cleaning safeguards, specified now so they're not an afterthought later**: cap how much influence any single device/session can have on one retrain cycle; don't let a new label silently override a very high-confidence prior verdict without a second look; version and log every retrain so a bad one can be rolled back. Real design work for whenever this pipeline gets built — not needed today, just specified so it isn't skipped later.
6. **SHAP explainability: server-side (Tier 2) only, not on-device.** Real SHAP (even the efficient tree-specific variant) costs meaningfully more than a plain prediction; Tier 1 runs far more often than Tier 2 by design, so doubling its cost undercuts the reason Tier 1 exists at all. **On-device explainability uses a cheaper rule-based check instead** — "which of the 10 locked features was furthest from its typical/expected value" — a real, decided substitute, not a placeholder.
7. ~~Open, not yet resolved: "beta" reference~~ — **resolved, see item 14 below.**

## 14. Full XAI architecture, resolved 2026-09-14 — two pipelines, never touching each other at runtime

Clarified directly with the user's friend (the one who originally suggested "BETA" — confirmed via web search to be a real technique, **BETA = "Black Box Explanations through Transparent Approximations"**, Lakkaraju/Bach/Leskovec, arXiv:1707.01154 — two-level decision sets: an outer region descriptor + inner per-region decision rules). Full picture, confirmed correct by the friend after a round of clarifying back-and-forth:

**Evaluation-phase (dev-facing, dataset-only, offline) — PDP and ICE:**
- Run against the fixed public UNSW-NB15 dataset (or a sample of it), **never against real Warden traffic, never on a phone, never automatically on a schedule.** One-time (or per-retrain) sanity check: "does this trained model's behavior make causal sense, or did it learn something spurious from the data" — the same instinct that caught `sttl` being a source-side/lab artifact earlier.
- **PDP**: cheap, one averaged curve per feature — the first-pass triage tool. Already run for real against the locked 10-feature model (see item 11's numbers) — flagged `smean` (genuinely bumpy, non-smooth) and `handshake_latency_millis` (suspiciously narrow 0–1.4ms range, possibly another lab-network artifact) as worth a closer look.
- **ICE** (centered): targeted follow-up specifically on features PDP already flagged, not run indiscriminately on all ten. Reveals whether a bumpy PDP average is real-and-consistent behavior or hidden heterogeneity/interaction effects the average was masking. Not yet run — next concrete step on `smean`/`handshake_latency_millis` specifically.
- **Real diagnostic sequence for future retrains**: train → PDP (cheap triage) → ICE only on whatever PDP flags → decide whether to trust the feature or investigate further. Not a fixed schedule — a step in the human-driven retrain/validation process.

**Deployment-phase (user-facing, real-instance, runtime) — SHAP (Tier 2) and BETA (Tier 1):**
- Explains one real flagged destination's actual verdict, using the already-validated model — completely separate code path from PDP/ICE, which never runs here.
- **SHAP** (server, Tier 2) — exact, per-instance, `TreeExplainer`. Affordable here since Tier 2 only processes the small residue Tier 1 already flagged.
- **BETA** (on-device, Tier 1) — replaces the earlier ad-hoc "furthest from typical value" placeholder. Fit once, offline, at export time (same pipeline as the tree export, one more artifact); on-device evaluation is just "which rule matched," as cheap as the tree walk itself.
- **Open, real, not-yet-decided refinement**: whether to layer LIME's sparse (K-LASSO) local linear coefficients on top of BETA's matched region, for a bit more per-feature granularity within the "why" — designed as a concept (fit offline alongside BETA, ship a small sparse-weight artifact, no live perturbation on-device) but not committed to; BETA's plain rule text alone may already be enough for what a phone notification needs. Decide once there's a concrete UI mockup to judge against, not before.
- **On-device UI plan, not yet built**: a compact per-instance bar chart (3-5 bars, magnitude + direction) in Warden's existing `AuditDetailScreen`, fed by whichever attribution numbers (SHAP server-side, or BETA/sparse-LIME on-device) are already being computed for the text explanation — a rendering task, not new ML work.

**RAM note, since it came up in the same conversation**: none of this — model, BETA rules, sparse weights, the small per-instance chart — costs meaningful memory on-device. The multi-million-row dataset and the PDP/ICE sample never ship to the phone at all; only the tiny exported model file does. The real memory considerations on-device are the VPN relay's buffers and the bounded caches (2-sample cap, 5-minute staleness cutoff, audit-log retention) already built for unrelated reasons — see `PROJECT.md`/`noxos-app/TASKS.md` for those.

## 15. The headline F1=0.9724 is real but inflated — real number on genuinely novel data is F1≈0.88, recall≈0.85

Checked directly, prompted by a real "did you leak the data" question — not classic leakage, not classic overfitting, but a third, related problem that matters just as much for trusting the number.

**Ruled out, checked directly**: (a) train/test split mechanics are clean — categorical vocabulary (`proto`) is learned from train only and reused (not refit) on test, no label information crosses the boundary. (b) Classic overfitting is not present — train-set F1 (0.9740) vs. test-set F1 (0.9724) is a gap of only 0.0016, the opposite signature of overfitting (which would show train much higher than test).

**What's actually happening**: **54.68% of test rows have an exact, byte-for-byte duplicate feature-row already in the training set** (47% of the entire 2.28M-row dataset is duplicate feature-rows). This is a direct, mechanical consequence of reducing every connection down to only 10 coarse numbers — two genuinely different real connections (different IPs, different timestamps, different everything in the richer original data) can easily collapse to the exact same `(proto, dst_port, byte counts, duration, ...)` tuple once reduced to just these 10 fields. Nothing about this is a bug in the split code; it's an honest cost of the feature-reduction decision that hadn't been accounted for in how the headline number got reported.

**Real, honest number — model performance restricted to ONLY the 206,689 test rows with no exact duplicate in training:**
```
                Full test set (incl. duplicates)   Genuinely novel rows only
Precision       0.9792                              0.9167
Recall          0.9657                              0.8519
F1              0.9724                              0.8831
```
Recall (the metric that matters most for a security tool — are real attacks actually caught) drops from 96.6% to 85.2% on genuinely novel-looking traffic. **The 0.9724 number already documented throughout `PROJECT.md`/`noxos-inference/TASKS.md` should be read as an optimistic upper bound, not the number to expect on real, never-before-seen Warden traffic** — F1≈0.88 / recall≈0.85 is the more honest estimate.

**Real follow-up, not yet done**: check whether the earlier "42-feature ceiling" comparison (the one used to justify "only 1.35 points behind full-feature" in the feature-selection work) has the same duplicate-collision problem. More columns should mean far fewer collisions — if so, the *true* gap between the 10-feature on-device model and the full model, measured fairly on novel-only data for both, is likely larger than 1.35 points. Don't trust that comparison's exact margin until this is checked the same way.

**Not a reason to abandon the current model or feature set** — the relative comparisons between feature-set sizes throughout this whole exploration used the same (equally duplicate-inflated) methodology, so they likely stayed consistent with each other even if each one's absolute number was optimistic. But the *absolute* F1/recall numbers reported anywhere in this project for the on-device model should be corrected to reflect this, and real-world validation (once Phase 1c ships and real traffic flows through the cascade) matters even more than these dataset-internal numbers suggested.

**Decided fix, 2026-09-14, dispatched to `noxos-inference`: switch to a group-based train/test split, not a row-based one.** `train_test_split` splits at the row level, which lets identical feature-patterns land on both sides — the actual root cause of the 92,702-shared-pattern problem above. Fix: build a group key per row from the exact 10 training features, then use `sklearn.model_selection.GroupShuffleSplit(test_size=0.2, random_state=42)` (or `GroupKFold`) keyed on that group, so every row sharing a pattern goes entirely to train or entirely to test, never both. After splitting: verify zero shared patterns remain (confirm, don't assume), and check the resulting class balance hasn't skewed badly (group-based splitting can distort balance if a few patterns are very frequent — flag it if so, don't silently accept it). Retrain on the corrected split, and **the resulting number supersedes 0.9724 everywhere it's documented** — not a footnote alongside it, a replacement. Full prompt text handed to the `noxos-inference` agent, not repeated here.

## 16. Group-split fix verified independently, 2026-09-14 — real number is F1≈0.98, not 0.88 or 0.9724, and here's why those don't contradict

Plain `GroupShuffleSplit` (no stratification) hit exactly the failure mode item 15's fix prompt warned about: it fixed the leak but skewed class balance badly (own test run: train attack rate 7.06% vs. test 23.43%, >3x apart) — not a trustworthy number, flagged instead of reported. Separately, the `noxos-inference` ground agent hit the same problem and switched to `StratifiedGroupKFold` on its own initiative, got a suspiciously clean-looking F1=0.9858 — user correctly got suspicious ("these values are wrong... just because you told it it should be 88") and paused the agent before trusting it, since nothing had been committed yet to check against.

**Verified independently from scratch** (own script, own grouping — label column explicitly asserted out of the group key, the one way this number could be faked): `StratifiedGroupKFold(n_splits=5)`, one fold held out as test. Result: **zero group overlap** (checked, not assumed), **class balance matches exactly on both sides** (11.09%/11.09%, same as the full dataset), 236,618 distinct test groups averaging 1.9 rows each (broad, representative sample — not a few duplicated groups dominating). **acc=0.9965 prec=0.9797 rec=0.9893 f1=0.9845** — within noise of the ground agent's independently-obtained 0.9858. Two separate implementations agree; this is a real, structurally-verified number.

**Reconciling 0.88 vs. ~0.98 — they measure different things, not the same thing twice:** the 0.8831 number (item 15) came from filtering the *original leaky split's* test set down to rows whose pattern never appeared in that same split's training set. Random row-sampling makes that filtered bucket skew hard toward **rare, low-frequency patterns** — a common pattern is nearly certain to land in an 80% random train sample by chance; a rare one might not. So 0.88 is an accidental hardest-case/rare-pattern-only metric, not a representative one. `StratifiedGroupKFold` instead builds a properly representative ~20% sample across *all* distinct patterns (common and rare alike) and retrains fresh on the complement — most held-out patterns are structurally close to things seen often in training, so the model does well on them, legitimately.

**Both numbers are honest and leak-free; they answer different questions**: 0.88 ≈ worst-case floor on the hardest sliver of genuinely novel traffic; ~0.98 ≈ realistic average-case on a representative sample of never-verbatim-seen traffic. For a deployed Tier 1 classifier, ~0.98 is the right headline number — 0.88 stays documented as a worst-case floor, not discarded.

**Not yet done**: the `noxos-inference` ground agent has not committed anything — `xgboost-classifier` branch still ends at the old `2230866` (F1=0.9724) commit as of this check. Do not treat ~0.98 as final/shipped until a real commit lands; re-verify the committed code's group-key construction directly (not from printed/narrated output) before superseding 0.9724 in `TASKS.md`. Also still open from item 15: the "boundary-precision / bucket agreement / tolerance" issue the ground agent hit right after this (context: likely an export/quantization probability-agreement check, similar shape to the earlier margin-space `base_score` bug) — not yet investigated, no visibility into it without reading the agent's actual code once committed.

## 17. Phase 1c shipped and verified for real, 2026-09-15 — but hit a genuine OS-level storage regression that blocks file-based testing, not a Warden bug

**The good news first, independently double-confirmed by two separate agents on the same live device (`cvd-1`, build `noxos_cf_x86_64_phone-trunk_staging-userdebug`, pulled from `s3://noxos-releases/cuttlefish-local/20260915-...`):** Phase 1c's priv-app integration actually works end to end. `Warden` is installed at `/system/priv-app/Warden/Warden.apk`, `privapp-permissions-warden.xml` is present, `android.permission.MANAGE_VIRTUAL_MACHINE` grants `true`, `ro.build.tags=dev-keys` (confirms the real platform key from item 16's signing work is in effect, not AOSP `test-keys`), and **Warden's installed APK signer cert SHA-256 (`950e2918...`) matches `~/.noxos-keys/platform/platform.x509.pem` exactly** — the whole cross-repo signing-key plan (OS agent generates key → app agent wires it into Gradle/CI → same key bakes into the ROM) is confirmed working, not just theoretically sound. AVF/pKVM services (`virtualizationservice`, `virtualizationmaintenance`) are up, Warden launches and stays alive.

**The real problem found next, while trying to exercise an actual in-app pKVM VM boot (a file scan):** a genuine, reproducible AOSP-level storage bug, not app behavior — full evidence chain, not a guess:
1. `sm list-volumes` shows `emulated;0` stuck **unmountable** on a fresh boot (not a race condition — reproducible).
2. `StorageManagerService.mount()` → `vold`'s mount call fails with error `-5`.
3. Root cause traced further: `IExternalStorageService.startSession()` against `com.android.providers.media.module/...ExternalStorageServiceImpl` throws `DeadObjectException: Transaction failed on small parcel`. `MediaProvider` (pid alive) is stuck reconnecting every 10 seconds forever, but the FUSE session handshake itself never completes.
4. **Consequence**: the `external_primary` MediaStore volume never comes into existence at all — breaking *any* MediaStore/SAF-based file access. This hits both of Warden's file paths equally (manual "Select & Scan File" picker, and `FileArrivalWatcher`'s auto-scan) — **Warden isn't hiding or failing anything itself; the OS's own storage stack is broken underneath it**, so neither path could ever have worked on this build regardless of Warden's own code.

**Suspected cause, not yet confirmed**: something in the recent bloat/GMS-stripping work (`PRODUCT_PACKAGES` filter-out, see item on the `v1` device overlay in `noxos-os/TASKS.md`) likely broke `MediaProvider`'s FUSE session setup — plausible since `MediaProvider` is exactly the kind of system service that bloat-stripping could accidentally destabilize, but this is a hypothesis, not confirmed. **This is squarely a `noxos-os` build issue, explicitly not something for `noxos-app`/Warden to work around** — same repo-boundary judgment call as the signing-key work in item 16.

**What was and wasn't verified on this pass**: priv-app placement/permission/signing — fully confirmed, independently, twice, by two separate agents (matching cert SHA-256 across the GitHub release APK, the installed system APK, and `noxos-os`'s generated platform key — three-way match). An actual in-app pKVM VM launch (the real point of Phase 1c) — **not yet exercised**, blocked entirely by the storage bug above, unrelated to any permission or VM-config issue. Screenshot/demo work for the faculty progress deck is stashed until this is resolved — network-side Warden screens (ACL, network monitor) don't depend on MediaStore and may still be viable to screenshot independently of this fix, but that wasn't pursued yet, paused per explicit instruction to stash and revisit later.

**Root-cause investigation, 2026-09-15 (this session, static analysis only — device unavailable/being used by other agents throughout, no live logcat pulled)**: the `noxos-app` agent's own writeup names `noxos-os` commit `b1fa370` ("strip telephony, non-Latin fonts, and remaining bloat for v1") as its best guess — **this hash is stale, not fabricated**: it's the same commit already documented in `noxos-os/TASKS.md`'s 2026-09-13 "v1 lightweight pass" entry, just renumbered to `10eab82` by some later rebase. Read that commit's full diff directly: it strips 9 telephony packages, 4 misc bloat packages, and 183 non-Latin font files via `PRODUCT_PACKAGES filter-out`. **No obvious mechanical link to MediaProvider/FUSE/storage found in that diff** — worth ruling out with a real device check, but not the strongest lead.

**Stronger lead, found in the same commit range, confirmed against real AOSP SELinux documentation** (not yet confirmed against actual device logs — the device was in use by other agents this whole session): `fa4fab8` ("satisfy AOSP's other named key roles with the same platform key") copies the new custom platform certificate onto *every* AOSP-named signing role — `media`, `networkstack`, `sdk_sandbox`, `shared`, `bluetooth`, `nfc` (then `testkey`/`cts_uicc_2021` added in `7a2ccf1`) — rather than generating/keeping distinct certs per role. Confirmed via AOSP's own SELinux docs: stock Android ships four distinct key roles (`platform`/`shared`/`media`/`testkey`), each mapped to a *different* `seinfo` tag in `/system/etc/selinux/plat_mac_permissions.xml`, keyed off the literal certificate — that's precisely the mechanism `seapp_contexts` uses to assign SELinux domains. Collapsing `media` and `platform` into an identical certificate removes that distinction; MediaProvider (which is signed under the `media` role in stock AOSP) could plausibly land in the wrong SELinux domain as a result, denied exactly the mount/FUSE-related permissions it needs — and a silent SELinux denial (process stays alive, call just fails) matches the observed symptom (`DeadObjectException`, MediaProvider pid alive, endless 10s retry) better than a crash would.

**Not yet confirmed** — needs one cheap check next time the device is free: grep `logcat`/`dmesg` around the failure for `avc:.*denied` naming MediaProvider or the FUSE/ExternalStorageService process. **If confirmed, the fix**: keep `platform` as the custom NoxOS key (load-bearing for Warden's grant, already proven working) but restore AOSP's own original stock keys for every *other* role (`build/target/product/security/{media,shared,testkey,...}.{x509.pem,pk8}`, checked into the AOSP tree already) instead of overwriting them all with the platform key — preserves the role distinction stock AOSP/SELinux depends on while keeping the one role Warden actually needs custom.

**CONFIRMED FIXED, 2026-09-15 (`noxos-os` commit `629efbb`)**: hypothesis was correct. Rebuilt with distinct stock AOSP keys restored for every non-`platform` role, verified with real evidence, not just re-testing the symptom: **no `avc` denials in the `media`/`netd`/`mediaprovider` domains**, `emulated;0` now mounts, SAF file picker works, real DNS resolution and `ping` replies confirmed. No regression on what was already working (permission grant / three-way signature match / app launch all re-confirmed still good). This closes the storage+networking half of Phase 1c's verification gap. Full detail pushed to `noxos-os/TASKS.md` (context branch, commit `cb502ed`).

## 18. New bug found immediately after the signing-key fix, 2026-09-15 — first-ever real pKVM VM boot attempt through Warden, blocked in a different SELinux domain

With storage and networking both fixed, the OS agent exercised an actual in-app pKVM VM launch through Warden for the very first time in this project's history (everything before this was permission-grant/service-presence checks, never a real boot attempt). Result: **`virtmgr` spawns but never gets a paired `crosvm` child** — blocked by `avc` denials in the `platform_app_all_virtualizationmanager` domain, specifically around `surfaceflinger`-owned socket file descriptors and a `tcp_socket` from another app. **This is a different SELinux domain than the `media`/`networkstack` roles item 17 just fixed** — a separate bug, not a leftover symptom of the same one. Not yet root-caused or fixed. Testing handed off to the `noxos-app` agent to continue.

**Unrelated hiccups from the same session, worth knowing but not architecturally significant**: the build instance was seeded from a stale snapshot with an old, uncommitted `device/noxos/cf_x86_64_phone` diff, breaking the first repo sync — fixed by discarding and retrying on the same instance. Also, the CI workflow's `watch-build` polling loop's AWS STS session expires after 1hr with no re-auth, so it shows red on any build taking over an hour regardless of actual outcome — a known false-negative in CI reporting, not a real build failure, being fixed separately. New snapshot taken: `snap-09aec6a527195e3d7`.

**Net status**: Phase 1c's non-VM half (priv-app placement, permission grant, signing, storage, networking) is now genuinely verified working, with real evidence. The actual VM-boot half — the whole point of Phase 1c — has never succeeded yet and is now blocked on a newly-found, distinct SELinux issue. Don't read "signing-key bug fixed" as "Phase 1c is done" — it closed one real gap and immediately surfaced the next one underneath it.

**Root-cause investigation on item 18, 2026-09-15 — likely `noxos-app`'s bug, not `noxos-os`'s, based on tracing the real AOSP reference macro.** The OS agent suspected a missing/uninherited sepolicy rule (framed as "worth checking against `virtualizationmanager.te`/`platform_app.te` for a missing allow rule"). Checked directly against AOSP's own canonical macro instead of guessing: `virtualizationservice_use()` in `system/sepolicy/public/te_macros` (the exact macro `composd.te` — AVF's own documented reference client — invokes) expands to:
```
domain_auto_trans($1, virtualizationmanager_exec, virtualizationmanager)
allow { virtualizationmanager crosvm } $1:unix_stream_socket { ioctl getattr read write };
allow { virtualizationmanager crosvm } $1:fd use;      # fds the CLIENT explicitly passes
allow $1 virtualizationmanager:fd use;                  # fds virtualizationmanager creates for the client
allow { virtualizationmanager crosvm } $1:fifo_file { ... };
allow $1 virtualizationmanager:vsock_socket { ... };
...
```
**No `surfaceflinger` or cross-app `tcp_socket` permission appears anywhere in this macro** — it only authorizes fds the caller itself deliberately creates and passes, or fds `virtualizationmanager` creates back for the caller. The denials being hit (`use` on 4 `surfaceflinger`-owned socket fds, `read write` on a `tcp_socket` owned by a *different app's* domain, `platform_app_36`) fall outside what this contract was ever meant to cover — not a rule AOSP forgot to inherit, but fds `virtmgr` was never supposed to be touching in the first place.

**Better-grounded hypothesis**: `virtmgr` is inheriting stray file descriptors that leaked into the VM-creation call, most plausibly (a) a `Surface`/display handle — Microdroid's headless app-payload VMs (what Warden uses for file/network scanning) shouldn't request any display capability at all, that's the separate Custom-VM-with-display feature — and/or (b) one of Warden's own live relay sockets, plausible given `NetMonitorService`/`TcpRelayManager` holds real `protect()`'d sockets open as its core job. **If confirmed, this is a `noxos-app` bug in how `MicrodroidVmSession.kt` builds its `VirtualMachineConfig`, not a `noxos-os` sepolicy gap** — adding a new `allow` rule to silence the denial would mask a real fd-hygiene bug rather than fix it. **Recommended next step, not yet done**: check `MicrodroidVmSession.kt`'s exact config construction for any unnecessary display/Surface option, and check whether Warden's relay sockets are still open/uncleaned at the moment it triggers a VM launch. Handed to whichever agent picks this up next — flagged clearly so a sepolicy patch isn't applied reflexively before checking the app side first.

**Both leads checked against the real code and ruled out, 2026-09-15 — for the specific repro tested, with one open caveat.** `noxos-app` verified directly:
- **Display/Surface**: ruled out at the code level, not just "not observed." `MicrodroidVmSessionFactory.createSession()` builds `VirtualMachineConfig.Builder(context).setPayloadBinaryName("libnoxos_payload_stub.so").setProtectedVm(false).build()` — that's the entire config. This Builder (the app-payload/Microdroid path) doesn't expose a display/Surface option at all; that only exists on `VirtualMachineCustomImageConfig.Builder`, a separate path Warden never touches.
- **Relay sockets**: ruled out *for this specific repro*, structurally. `HomeScreen.kt`'s manual "Select & Scan File" button is `enabled = !monitoringActive` — manual scan is UI-disabled whenever monitoring runs, so `NetMonitorService`/`TcpRelayManager` provably had no sockets open when this particular failure happened. Both scan paths (manual button, `FileArrivalWatcher` auto-scan) hit the identical call site (`TriggerRouter.scanFile()` → `vmSessionFactory.createSession(context).use {...}`), so it's not a code-path difference — genuinely no sockets were live for this test.
- **Open caveat, not yet tested**: auto-scan *does* run while monitoring is active, with real relay sockets genuinely open — sockets-as-a-factor isn't ruled out in general, only for the manual-scan repro actually run. Real follow-up once the more fundamental question below is settled: start monitoring, drop a flagged file into Downloads, see if auto-scan hits the same failure.

**Updated theory, given both app-side leads are clean**: the `surfaceflinger`-owned fds may be an *ambient* artifact rather than anything Warden's code explicitly requests or leaks — every Android app process holds an implicit SurfaceFlinger/windowing connection just by existing, unrelated to VM creation. If `virtmgr`'s spawn path doesn't mark inherited fds close-on-exec correctly, that ambient connection could leak in regardless of app code, which would point back to AOSP/platform fd-handling during VM creation rather than `noxos-app`. **This connects directly to the demo-app test already dispatched** (`MicrodroidDemoApp`, a normal app with the same ambient SurfaceFlinger connection, different SELinux domain than Warden's priv-app-caller domain) — if it also hits this failure, that's strong evidence for "universal platform gap, any caller domain"; if it boots clean, the ambient-fd theory needs revisiting too, since a normal app's ambient connection didn't cause a problem there. Either result narrows things meaningfully. Not yet resolved — waiting on that test's outcome.

**RESOLVED, 2026-09-15 — demo-app test came back, ambient-fd theory was WRONG, real bug is earlier and app-side.** `MicrodroidDemoApp` (Google's own unmodified reference app) built against the exact same synced tree, installed on the same live `cvd-1`, ran clean: `virtmgr_microdroid.demo` spawned a paired `crosvm_demo_vm` child (CID 2049), full real boot sequence (`VmBooted` atom, guest agent registered, payload ready), and the app's own UI showed a genuinely-computed result (`123 + 456 = 579`) — proves the OS-level AVF/pKVM machinery is healthy end to end.

**Correction, stated plainly**: the exact same `surfaceflinger`-owned-fd `avc` denials that hit Warden *also fired for this successful run* (plus one more, `/dev/dri/renderD128`/`gpu_device`) — and the VM booted fine anyway. **These denials are non-fatal in both cases, almost certainly optional GPU/display-passthrough probing that just falls back — not the blocker, a red herring.** The ambient-fd hypothesis (this session's own leading theory) is ruled out. Don't chase it further.

**Sharper target now**: Warden's `virtmgr_com.noxos.app` process spawns (confirmed earlier, pid 4441) but its own log **never reaches "Running crosvm with args"** — unlike the clean demo run, which did. So the failure is *before* virtmgr attempts to launch anything, in virtmgr's own startup/config-validation step — meaning something about the specific `VirtualMachineConfig` Warden's `MicrodroidVmSessionFactory.createSession()` sends is the actual problem, not fd inheritance, not sepolicy. **Confirmed `noxos-app`'s bug to chase, not an OS-side fix.** Checking directly (this session) whether the payload binary named in that config (`libnoxos_payload_stub.so`, via `setPayloadBinaryName(...)`) actually exists inside the currently-installed Warden APK — the jniLibs handoff (the mechanism that's supposed to bundle `noxos-payload`'s compiled `.so` into `noxos-app`'s `jniLibs/<abi>/`) was an open, unbuilt gap as of much earlier in this project; if it's still unresolved, virtmgr failing immediately on an unresolvable/missing payload binary name would explain this symptom exactly.

**Unrelated, still open**: `cvd-1` was hit by the mystery `cvd stop` two more times during this diagnostic (10:55:32→11:05:32, 11:48:39→11:58:40 — both almost exactly 10 minutes apart). Checked host crontab, systemd timers, and the container's own cron — none installed, source still unknown. 3rd/4th occurrence tonight. Not blocking (restarts are fast), but a real, unexplained pattern worth investigating properly at some point. Fix commit `e8ed1eb` is unsigned (GPG hung twice) — needs re-signing later, same recurring GPG-hang issue as elsewhere in this project.

**CONFIRMED, 2026-09-15 (this session, checked directly against the real release artifact, not a guess)**: `libnoxos_payload_stub.so` — the exact name `MicrodroidVmSessionFactory.createSession()` passes to `setPayloadBinaryName(...)` — **does not exist anywhere in the currently-installed Warden APK**. Downloaded the actual latest `noxos-app` release and inspected every ABI's `lib/` directory: only `libandroidx.graphics.path.so` and `libdatastore_shared_counter.so` are present (both unrelated AndroidX libraries) — no `noxos`-named library at all, for any architecture.

**This is almost certainly the real root cause**, and it exactly matches the symptom: `virtmgr` failing before it ever attempts to launch `crosvm` is precisely what happens if it can't resolve a payload binary name that was never packaged into the APK in the first place. It also explains why this was never caught earlier — every previous verification checked *permission* (`MANAGE_VIRTUAL_MACHINE` granted, signature matching) and *service presence* (AVF services running), never an actual VM boot attempt, until this session.

**Traces back to a known, old, apparently still-unclosed gap**: the jniLibs handoff — the mechanism meant to copy `noxos-payload`'s compiled `.so` into `noxos-app/app/src/main/jniLibs/<abi>/` before release builds — was flagged as unbuilt ("nothing does this yet") back when Phase 1c was first specced (see `PROJECT.md`'s priv-app section, and the item on automating this as CI triggered by a `noxos-payload` release). This confirms it's still unbuilt: the release APK being baked into the ROM and tested against has never had a real payload binary bundled into it at all. **Not an OS bug, not an SELinux bug — a missing build step, squarely `noxos-app`/`noxos-payload`'s to close.**

**Scope correction, confirmed against both repos, 2026-09-13→2026-09-15 (verified current, not stale)**: this is bigger than "wire up a copy step." `noxos-app` confirmed zero jniLibs directory/Gradle config exists anywhere in that repo — the `setPayloadBinaryName(...)` reference is a pure dead reference to a file never wired to exist. But `noxos-payload`'s own `TASKS.md` (checked directly, its current "Cross-repo dependencies" section, not a stale entry) says there may not even **be** a real compiled `.so` to copy yet: "Real Soong/AOSP compile and vsock I/O against a live VM are still Phase-2-gated" — every existing test of `noxos-payload`'s cheap-filter code has only ever run as a plain host binary (`clang++`+ASan), never actually cross-compiled through Soong for the real Android/Microdroid guest target. `payload_main.cpp` (the actual guest entry point using `vm_payload/api.h`) is explicitly AOSP-only and not linkable with plain `clang++` — untested end-to-end by anything that exists today.

**Also unresolved**: a naming mismatch between `libnoxos_payload_stub.so` (`MicrodroidVmSession.kt`) and `noxos_payload_stub.so`, no `lib` prefix (`vm_config.json`'s `task.command`) — both still carry the `_stub` name from before `noxos-payload` had a real EXIF parser. `noxos-payload/TASKS.md` already flags this explicitly: whoever produces the real Soong output should either match the exact existing name or do a coordinated two-sided rename, since a mismatch here fails silently at `vmm.getOrCreate()`/`vm.run()` — not at compile time, exactly the failure mode being chased.

**Real next step is therefore three pieces, not one**: (1) get `noxos-payload` a real Soong/`Android.bp` build target producing an actual Android/Microdroid-target `.so` — genuinely new build-system work, not proven yet at all; (2) resolve the `_stub` naming question, one side or the other; (3) then build the actual jniLibs handoff to copy that real artifact into `noxos-app`. This is Phase-2-scale cross-repo work, not a quick patch — flagged clearly rather than started unilaterally, since it touches `noxos-payload`'s build system which isn't `noxos-app`'s to change alone. Reported to the user for a real scope/dispatch decision rather than assumed.

**New concrete, checkable hypothesis for the guest_time/proactive-kill mystery, 2026-09-16 — found via real AOSP source research (root agent, while ROM rebuild for item 20 ran), no live boot cycle needed to check it.** Traced `virtmgr`'s actual kill paths in `android/virtmgr/src/crosvm.rs` (real upstream source, `packages/modules/Virtualization`): the only two places `kill()` fires are (a) `monitor_payload_hangup()`'s 30s/300s boot-hangup timeout — far too slow to explain a ~4-5ms kill — and (b) an **explicit external kill request** (`VmInstance::kill()`, called when the client's `IVirtualMachine` reference is torn down). `source.android.com`'s own VirtualizationService doc states the exact mechanism verbatim: *"As long as there's at least one reference to `IVirtualMachine` object then the VM continues to run... If all references to the `IVirtualMachine` object are dropped before the VM shuts down, then `VirtualizationService` automatically shuts down the VM."*

This means a ~4-5ms kill with zero crosvm log output is fully consistent with **Warden's own Kotlin code not actually holding/awaiting the VM reference long enough** — if `MicrodroidVmSession`'s `.use {}` block (in `TriggerRouter.scanFile()` → `vmSessionFactory.createSession(context).use {...}`) doesn't genuinely block/await on the VM's lifecycle (a payload callback, a blocking vsock read, `VirtualMachineCallback.onPayloadReady`, etc.) before the block body finishes, Kotlin's `use` would call `close()` almost immediately after `run()` returns — and `run()` itself is documented as async/non-blocking (starts the VM, returns quickly). The block exiting near-instantly would explicitly stop the VM before crosvm ever gets scheduled, exactly matching the observed symptom (explicit kill, not a timeout, not fd/cgroup/disk-image related). **This is a pure code-review check, not a live-instance test** — look directly at what `MicrodroidVmSession`'s scope actually does between `vm.run()` and the point its `use`/lifecycle scope closes: does it register a `VirtualMachineCallback` and block/suspend on `onPayloadReady`/a result, or does it fall through and let the block finish without waiting for anything from the VM? If the latter, that's the bug, and the fix is holding the reference open (e.g. via a callback-driven suspend/await, not letting the `use` block complete) until the VM actually finishes its work. Not yet checked against the actual current `MicrodroidVmSession.kt` source — handed to `noxos-app` to verify directly, since I don't have live code access to check the currently-checked-out file version myself.

**RESOLVED, 2026-09-16 — confirmed exactly right by reading the real source, fix shipped.** `MicrodroidVmSessionFactory.createSession()` called `vm.run()` (async, returns immediately, doesn't wait for boot) and the very next thing that happened was `session.getTransport()` → `vm.connectVsock(...)` with **zero wait, zero retry, zero readiness check**. Since crosvm hadn't even been scheduled yet, `connectVsock()` threw almost instantly, unwinding the `.use {}` block in `TriggerRouter.scanFile()`, which called `session.close()` → `vm.stop()` — an explicit stop request within milliseconds. Dead match for "explicit kill, zero crosvm output, ~4-5ms." **Cleanly explains why `MicrodroidDemoApp` worked and Warden didn't — nothing to do with signing, cgroups, disk images, or priv-app placement after all**; those were all real, carefully-checked leads that turned out to be red herrings once the actual client-side race was found.

**Fix**: registered a `VirtualMachineCallback` on the VM right after `getOrCreate()`/before `run()`, using a `CompletableDeferred<Unit>` that completes on `onPayloadReady()` (or fails on `onError`/`onStopped`/`onPayloadFinished`). `getTransport()` now awaits that deferred before ever calling `connectVsock()`. Method signatures (`setCallback(Executor, VirtualMachineCallback)`, the 5 callback methods) verified directly against the real API 35 system stub jar via `javap`. Committed `f4f85b3`, pushed straight to `main` **unsigned** (no local Gradle build — gpg-agent pinentry unavailable in this background session, user gave explicit one-day permission for unsigned commits; relying on GitHub Actions' `ci.yml` for the real build+compile check instead of a local build). Added to the outstanding unsigned-commits-to-resign list alongside `e8ed1eb`/`0599cd7`. **CI confirmed green, 2026-09-16**: run `35030277926` succeeded — `f4f85b3` builds clean on real GitHub Actions infra. Issue-1 root cause is confirmed and shipped; still gated on issue 20's sepolicy fix landing before a real device test can prove it produces actual crosvm output.

**Independent corroboration, 2026-09-16**: decompiled `MicrodroidDemoApp.apk`'s dex at the strings level and found it references `VirtualMachineCallback`, `setCallback`, `onPayloadReady`, `onPayloadStarted`, `onPayloadFinished` — the same "register callback, wait for `onPayloadReady` before touching the VM" pattern just added to `MicrodroidVmSession.kt`. Confirms this is the AVF's actual intended usage pattern (which the demo app already followed and Warden didn't), not just a plausible-sounding guess. High confidence in the fix now, still short of the real device confirmation pending issue 20.

**Gap caught before it became a blocker, 2026-09-16**: the earlier "CI confirmed green" (run `35030277926`) only exercised `ci.yml`, which builds an unsigned debug APK — not the v3-signed, platform-keystore release build the idsig fix actually depends on (`release.yml`, which only fires on a `v*` tag push, not a plain commit). There was no real testable signed artifact sitting ready. Fixed: bumped to `2.2.5`, tagged, `release.yml` run `35047047712` building the real signed release. Being staged (both host and `cuttlefish_orchestrator` container) ahead of time so there's zero prep lag once a working device is available — retest plan: sideload `v2.2.5`, launch Warden, manually drive the file-scan trigger through the UI (no adb shortcut for the picker), watch logcat/crosvm-tagged lines directly for real output (the 16-vs-0 comparison metric). Expected to take a couple minutes once a device is ready, not another 10-minute cycle.

**Readiness verified directly, not assumed**: first release download came through corrupted (truncated zip), caught and redownloaded clean. Confirmed the fix is actually compiled in by extracting `classes2.dex` directly — `MicrodroidVmSession`'s constructor now takes `(Context, VirtualMachine, CompletableDeferred)`, the exact new 3-arg signature, plus `onPayloadReady`/`payloadReady` symbols present. (Red herring noted for the record: the final APK is byte-for-byte the same size, 42939808, as the old v2.2.4 build — a real coincidence, not a stale/cached build; the ~40-line source diff is negligible against a ~40MB APK dominated by the payload `.so` and assets.) `v2.2.5` staged at `/tmp/warden-v225.apk` on both host and `cuttlefish_orchestrator` — ready to sideload/test the instant a working device is available.

**Issue 2 CONFIRMED FIXED on a real device, 2026-09-16.** Device booted clean on the fixed build (v1.2, commit `0599cd7`): `sys.boot_completed=1`, and — the actual mechanism that was broken — `cvd create` returned `"status": "Running"` immediately, not stuck on "Starting" like every prior broken run tonight. Direct confirmation on both fronts: zero `avc: denied` lines for `kmsg` anywhere in logcat (present on every prior boot), and `gceservice` now runs under `scontext=u:r:gceservice:s0:c65,c256,c512,c768` — its own dedicated domain, not the generic `platform_app` it was falling into before. Unambiguous, direct confirmation the cert-override fix did exactly what it was meant to.

**One small, separate, honestly-flagged loose end**: a different, unrelated `avc: denied { read }` on `default_prop` for `gceservice` now shows up. Doesn't block boot (device fully up, fleet status Running), looks cosmetic/benign — not claiming zero denials, just noting it's a new, minor, non-blocking item for later, not swept under the rug.

New image being snapshotted/pushed to S3 now; Warden App will get it next for the real end-to-end retest of both issues.

**BOTH ISSUES CONFIRMED FIXED END-TO-END, 2026-09-16 — first time in this project's history Warden's inner Microdroid VM actually boots, runs, and completes a full request lifecycle.** Full retest done on the AWS-fixed image with the vsock-race-fixed `v2.2.5` app:

- **Issue 2 (SELinux/boot signal)**: confirmed fixed again, independently, on this retest — `cvd fleet` hit `"status": "Running"` in ~30s, not stuck on "Starting"; `sys.boot_completed=1` immediately; `gceservice` confirmed running as `u:r:gceservice:s0`; zero real `kmsg` AVC denials (one grep hit was just the agent's own command echoed in `adbd`'s log, not a real denial — checked and dismissed correctly).
- **Issue 1 (vsock race)**: confirmed fixed. Real virtmgr timeline this run: VM created `03:29:20.617` → started `03:29:20.621` → asked to shut down `03:29:21.666` (**over 1 full second later**, not the old 4-5ms instant-kill) → exited cleanly `03:29:21.830`. The race is gone.

**New finding, surfaced only now that the race no longer masks it**: the scan result came back `"FLAGGED — SCAN DID NOT COMPLETE CLEANLY, VM payload finished before becoming ready (exit=1)"` — `libnoxos_payload_stub.so` itself exits with code 1 before ever signaling payload-ready. The payload never got far enough to hit this failure mode before, because the vsock race was killing everything first. Boot step: 94ms; total duration: 1332ms — consistent with a VM that boots normally, launches the payload, and the payload immediately errors out. **Given the binary is literally named `..._stub.so` (from `noxos-payload`'s `sync-payload.yml` pipeline), this may be an intentionally incomplete placeholder rather than a new defect** — needs checking with `noxos-payload` before being chased as a bug. Dispatched.

**RESOLVED, 2026-09-16 — real bug, not an intentional stub.** `payload_main.cpp` was never calling `AVmPayload_notifyPayloadReady()`. Verified directly against the real AOSP reference (`packages/modules/Virtualization/android/EmptyPayloadApp/empty_binary.cpp`, the same trusted reference used earlier to fix the `vm_payload.h`/`vm_main.h` include path): a working payload calls that function right after setup, before any blocking work — ours never did, which exactly matches the "exit=1 before Warden sees readiness" symptom. The `_stub` in the filename is just leftover `Android.bp` module-naming scaffolding — the EXIF parsing and both cheap filters are real, working logic, not placeholders. Fix: added the call in `AVmPayload_main()` right after `listen()` succeeds and before the blocking `accept()` (vsock server fully set up = the natural "ready" point). Committed `7b4ebfb`, unsigned (GPG/pinentry hung again, used the pre-authorized fallback), pushed to `noxos-payload` main. **Not yet verified against a real device** — needs Warden App to run the same scan again and confirm this clears the `exit=1`.

Practical upshot: the entire multi-week VM-boot saga (items 17-18) is now resolved at the platform level — Warden's UI produced a real Audit Trail entry from a real VM boot + real payload execution, which is real, screenshot-worthy evidence for the pending presentation tasks.

**Full end-to-end verification is gated on issue 20's fix landing first**: the natural next test (cut a build, sideload, trigger a real scan, check for actual crosvm output — the 16-vs-0 log-line comparison) needs a `cvd` instance that doesn't falsely declare the device dead at the 10-minute mark, otherwise a real VM boot can't be told apart from "killed by the false timeout regardless." Both fixes now need to land together before either can be confirmed working end-to-end.

**Second data point, same session, strengthens this hypothesis considerably**: while testing Warden's network-scan path, the app-testing agent hit a real connectivity/DNS failure on the same device and started working around it with manual DNS rules rather than treating it as an OS bug. Flagged this as the wrong move — `fa4fab8`'s collapsed-role list explicitly includes `networkstack`, the actual AOSP system component responsible for DHCP/DNS/connectivity validation, which has its own dedicated signing role in stock AOSP for the same SELinux-domain reason `media` does. **Two seemingly unrelated regressions (storage and networking) both touching roles that got collapsed into the same certificate is a strong structural signal they share one root cause**, not two separate bugs to chase independently. Redirected the app-testing agent away from the DNS-bypass workaround — same repo-boundary call as the storage bug: this belongs in `noxos-os`'s signing-key fix, not a `noxos-app`-side patch that would mask the real regression.

**Third data point, confirms this is a permission denial, not a config problem**: the app-testing agent had already gotten `netd` routing genuinely correctly configured on `buried_eth0` (a real `default dev buried_eth0` route confirmed in table 1015, via `ndc network create`/`interface add`/`route add`), yet `ping` still failed with "Destination Host Unreachable" even bound directly to the interface via `-I`. **A missing route or bad DNS config would not produce that failure mode on a correctly-routed, directly-bound socket — a SELinux denial on the networkstack-domain process handling it would.** Agent agreed, stopped immediately, did not add the DNS-bypass rules, documented in `noxos-app/TASKS.md` session 23 item 6. Left a harmless non-functional NAT rule/netd config on the live instance (inert, not worth unwinding). **Three independent data points (storage bug's exact failure signature, `networkstack` being a collapsed role, and this routing-correct-but-still-unreachable symptom) now converge on the same signing-key hypothesis** — still short of a direct `avc: denied` log confirmation, but strong enough to prioritize the key-role fix as the first thing to try, with the log check as verification rather than the only way to find out.

## 19. Real cross-repo build pipeline for the payload binary, dispatched and partially shipped, 2026-09-15

Following item 18's root cause (jniLibs handoff never built, `noxos-payload` never had a real Soong-compiled guest binary), three pieces of real work got dispatched and are in progress:

1. **`noxos-app` CI auto-publish pipeline — shipped.** `.github/workflows/sync-payload.yml` (commit `7ec9a40`, signed, pushed): runs every 30 min + manual dispatch, checks `noxos-payload`'s latest release against a tracked `PAYLOAD_VERSION` marker, sorts downloaded `.so` assets into `app/src/main/jniLibs/<abi>/libnoxos_payload_stub.so` by ABI substring in the filename, bumps Warden's version, commits+tags+pushes — deliberately reuses the existing (already-correct) `release.yml` rather than duplicating build/sign/publish logic, so a payload-triggered release looks identical to a human-cut one downstream. **User approved this shape directly** (full auto-publish, zero human step — the alternative "manual-trigger" option was available and explicitly not chosen). Verified for real: ran live via `workflow_dispatch` against the actual `noxos-payload` repo (correctly detected no release exists yet, exited clean, no side effects), and unit-tested the ABI-sort/version-bump logic locally against fake assets (correctly sorted fake `x86_64`/`arm64-v8a` assets, bumped `2.2.2/6` → `2.2.3/7`). Deliberately did **not** create a real test release on `noxos-payload` to avoid colliding with that repo's own in-progress release-workflow setup, or risking this same cron picking up a throwaway release and auto-publishing garbage — good judgment, avoiding exactly the kind of cross-agent collision this whole session has been sensitive to.
2. **Naming convention — locked, both sides confirmed matching.** `noxos-app`'s ABI-substring guess (`libnoxos_payload_stub-x86_64.so` / `-arm64-v8a.so`) and `noxos-payload`'s committed convention (pinned in its own `TASKS.md`) are the same thing — no mismatch, nothing to fix on either side.
3. **Real Soong/EC2 compile — dispatched, in progress.** User approved provisioning after an instance-sizing discussion (verified via AOSP's own docs: 64GB RAM is Google's official full-build minimum, someone's documented doing a full build on 8GB with real heroics and crash risk in one phase, no documented data point exists for a single-small-module targeted build specifically — reasoned estimate, not verified, that something in the ~16GB class should have real headroom). Dispatched: a new spot-fleet config sized to ~16GB RAM (`t3.xlarge`/`m5.xlarge`, flagged as a starting guess to validate) instead of `noxos-os`'s `r5.2xlarge`, reusing the existing AOSP-synced snapshot via a new EBS volume rather than a fresh multi-hour `repo sync` (told to verify the actual latest snapshot ID directly rather than trust one relayed secondhand), and the same CI-trigger pattern `noxos-os/.github/workflows/build.yml` already uses, adapted to build the Soong target instead of `m dist`.

**RESOLVED, 2026-09-15 — a real Soong-compiled payload binary exists, for the first time in this project's history.** Verified independently, not trusted from the build log: `file` confirms a genuine Android/x86-64 ELF shared object ("for Android 37", stripped); `readelf -d` shows correct `NEEDED` linkage (`libvm_payload.so` + `libc++`/`libc`/`libm`/`libdl`) and the correct `SONAME`; `nm -D` confirms `AVmPayload_main` exported as a global symbol — a real Microdroid guest entry point, not a stub. Uploaded to `s3://noxos-releases/payload-build/20260915T170054Z/libnoxos_payload_stub.so`.

**Naming question settled by real evidence, not prediction**: the compiled output genuinely is `libnoxos_payload_stub.so` (with the `lib` prefix) — confirming the `stem` fix from earlier was both correct and necessary, not cosmetic. `vm_config.json`'s `task.command` mismatch (the one flagged months ago in this repo's own `TASKS.md`, and the one this session's own earlier guidance got wrong before being corrected) is also fixed to match. No `noxos-app` change needed.

**Five real bugs found and fixed to get here, in order** (full detail + verification commands: `noxos-payload/TASKS.md` session 6): (1) an IAM tag-scoping mismatch broke the build instance's own self-service permissions; (2) `sdk_version: "current"` doesn't combine with `shared_libs: ["libvm_payload#current"]` (`libvm_payload` has no SDK-variant build) — root-caused against `EmptyPayloadApp`'s real working Android.bp; (3) OOM, twice — **correction to this session's own earlier sizing guidance**: the ~16GB (`t3.xlarge`/`m5.xlarge`) recommendation given earlier in this same investigation turned out insufficient in practice; the real fix ended up matching `noxos-os`'s own already-proven `r5.2xlarge`/64GB sizing rather than a smaller guess — worth remembering next time a "should be smaller than the full OS build" instinct comes up for a single-module Soong build; (4) a sneaky infra bug — the spot-fleet launch template's `UserData` (the actual bootstrap script instances run) was silently stale, because later fixes only ever got baked into `TagSpecifications`/`InstanceType` overrides, never re-baked into `UserData` itself, so every relaunched instance kept running the original *unfixed* `bootstrap.sh` regardless of what was actually committed to the repo; (5) `payload_main.cpp` included a header path (`<vm_payload/api.h>`) that doesn't exist anywhere in real AOSP — the real headers are bare `vm_main.h`/`vm_payload.h`, confirmed against `EmptyPayloadApp`'s actual working code before fixing.

**Also**: this proves the binary compiles and links correctly — it does **not** prove it actually boots inside a running Microdroid guest. That's still the real, unverified finish line, Phase-2-gated same as everything else VM-related. Build instance terminated, fleet scaled to zero, nothing left running.

**Real VM-boot test against v2.2.3 (real payload present) — CONCLUSIVE NEGATIVE, and it reopens the puzzle, 2026-09-15/16.** Confirmed properly this time (versionName=2.2.3, `MANAGE_VIRTUAL_MACHINE` granted=true, full logcat captured locally before the periodic device drop could wipe it): `virtmgr` still stalls at the **exact same point** as before the payload fix — same `avc` denials (surfaceflinger socket fds + a `tcp_socket` owned by a *different app's* domain, `platform_app_36`), then `"Started VirtualizationService RpcServer. Ready to accept connections"` — and nothing after. No `"Running crosvm with args"`, no `VmBooted`, no `payload_main` activity. That log line is confirmed the literal last relevant entry.

**This means the missing-payload-binary fix, while real and necessary, was never the actual root cause of the stall** — it fixed a real, separate gap (the binary genuinely didn't exist before), but wasn't what was blocking VM boot itself.

**A real contradiction worth sitting with, not glossing over**: item 18 already showed `MicrodroidDemoApp` hitting these *exact same* denials (`scontext=platform_app_all_virtualizationmanager`, same surfaceflinger-fd pattern) and booting clean anyway — which is why those denials were ruled out as non-fatal in general. If the denials alone were the blocker, the demo app should have stalled too. It didn't. So whatever is actually stopping Warden specifically must be something **different between Warden's real invocation and the demo app's**, not the denials themselves.

**Real next diagnostic questions, not yet answered**: (1) what process/app actually corresponds to `platform_app_36` — is a leftover process or another installed app holding a socket that's somehow getting entangled with `virtmgr`'s fd space specifically during Warden's launch, and is that app-slot assignment even stable/meaningful or could it be coincidental noise; (2) `virtualizationservice`'s *own* log (not just `virtmgr`'s) around the exact RPC call from Warden — is there an explicit rejection logged there that's being missed by only checking `virtmgr`'s log; (3) a byte-for-byte diff of what `VirtualMachineConfig` Warden actually constructs vs. what the demo app constructs — both use the minimal `Builder(context).setPayloadBinaryName(...).setProtectedVm(false).build()` shape on paper, but something concrete must differ since the outcomes differ. Not yet investigated — flagged as the real next step rather than reached for a new guess.

**Also not yet captured**: the exact error state from Warden's own audit database (device dropped — the recurring 10-min timeout — before it could be queried; `adb root` alone isn't enough to read app-private data on this build, needs `su 0` with `run-as` unavailable). Not essential — the logcat evidence stands on its own — but worth getting next time for the fuller picture.

**Config-diff investigation results, 2026-09-15/16**: ruled out the calling `Context` itself as a factor — `MicrodroidVmSessionFactory.createSession()` is called with `applicationContext`, not an `Activity`, traced directly from `MainActivity`'s real constructor call — so not an Activity-window-token/Surface entanglement from the caller side. Found one real, checkable (not guessed) difference: Warden's `AndroidManifest.xml` declares `MANAGE_VIRTUAL_MACHINE` but has **no** `<uses-feature android:name="android.software.virtualization_framework"/>` declaration at all, which `MicrodroidDemoApp` (AOSP's own reference integration) almost certainly does declare.

**Checked directly whether this is actually load-bearing — high confidence it is not, though not provably certain without reading `VirtualizationService`'s own implementation source.** AOSP's own AVF documentation draws a clean, explicit distinction: `hasSystemFeature("android.software.virtualization_framework")` is framed as the *runtime* check an app performs itself before attempting VM creation; `<uses-feature>` is framed purely as the compatibility-declaration mechanism (the same convention every `<uses-feature>` tag follows — app-store/device filtering, not a framework-enforced runtime gate against the calling app's own manifest). Standard Android permission/capability model doesn't have the OS service introspecting a caller's manifest tags before granting an API call — it checks the actual permission grant and hardware capability instead. **Deprioritizing this lead** rather than chasing it further; real remaining next steps are the two that need a live, stable device window: identifying what `platform_app_36` actually is, and checking `virtualizationservice`'s own log (not just `virtmgr`'s) around the exact RPC call — both blocked on the device holding still longer than the ~10-minute timeout cycle allows.

**Both checked, and the second one is a real breakthrough, 2026-09-16.**

`platform_app_36` resolved: not one app — a *shared* SELinux domain currently held simultaneously by three unrelated stock apps (`com.android.calendar`, `com.android.messaging`, `com.android.statementservice`, confirmed via `ps -Z`). The denied `tcp_socket` belongs to one of those, unrelated to Warden. Device-state noise, not a meaningful signal — matches the instinct going in.

**The real find**: going back through the saved full logcat (all tags, not just `virtmgr`/`VirtualizationService`) around the exact failure timestamp turned up two things. First, `VirtualizationService` (system-level, pid 1740) genuinely succeeded at its part — `"Allocated a VM's instance_id... for uid: 10044"`, followed only by a non-fatal, explicit skip (`"ignoring update of Secretkeeper entry as no ISecretkeeper"` — Secretkeeper just isn't configured on this build). So the system-level allocation step works fine; the failure is specifically between that allocation and `virtmgr` actually receiving/acting on it.

Second, and this is the real one: sandwiched between the 5 known `avc` denials and `virtmgr`'s `"Ready to accept connections"` line, at the exact same timestamp: **`audit_lost=92 audit_rate_limit=5` / `audit: rate limit exceeded`**. The kernel's own audit subsystem silently dropped 92 records in that exact window. **This whole investigation has been analyzing 5 denials while not knowing up to 92 more happened and were never logged anywhere.** Given the demo-app comparison already showed those 5 known denials are very likely non-fatal, the actual blocking denial is much more likely hiding in the 92 lost ones.

**Real fix found, not a guess**: checked AOSP's own official SELinux validation docs (`source.android.com/docs/security/features/selinux/validate`) directly — this exact scenario is documented: *"SELinux error messages are rate-limited after boot complete... to make sure you see all the relevant messages you can disable this by running `adb shell auditctl -r 0`."* Relayed as the real next step: run that command right after boot, before triggering the next test, to get a complete denial log instead of the rate-limited 5-of-97 view this whole investigation has been working from. Not yet run — waiting on the next stable device window.

**Ran with `auditctl -r 0`, 2026-09-16 — complete picture confirmed, but it's the same set the demo app also survived, not a new smoking gun.** `su 0 auditctl -r 0` worked (zero `audit_lost` lines afterward, vs. 92 before). Also independently confirmed the bug **predates and is unrelated to the payload work entirely** — tested against the ROM-baked `v2.2.2` (no real payload binary exists in that build at all) and got the exact same stall, no crash. Full complete sequence: 4 surfaceflinger fd denials (known) → 1 `platform_app_36` tcp_socket denial (known, confirmed device-state noise) → a `/dev/dri/renderD128`/`gpu_device` denial → a syscall record confirming `virtmgr`'s own `execve` succeeded (`exit=0`, correct argv) → `"Started VirtualizationService RpcServer"` → `VirtualizationService` allocates the instance → the non-fatal Secretkeeper skip → **nothing else**, confirmed by full log inspection past that point.

**Reconciled immediately**: the `/dev/dri/renderD128` denial isn't actually new information — it's the same one already documented earlier in this item from the `MicrodroidDemoApp` comparison ("the exact same avc denials... also fired for this successful run (plus one more, `/dev/dri/renderD128`/`gpu_device`) — and the VM booted fine anyway"). So the complete denial picture, now confirmed with rate-limiting off, is the **same set the working demo app also hit and survived** — none of them distinguish Warden's failure from the demo app's success. The "92 lost records" almost certainly were just repeats of these same known denial types (a process can generate many audit records retrying/probing one denied resource), not 92 distinct new pieces of information.

**This sharpens where the real gap actually is, rather than closing the investigation**: `virtmgr`'s RPC server comes up and reports itself ready to accept connections — then genuinely nothing happens. No evidence the actual "create/run this VM" call ever reaches it. **Real next step, more precise than anything tried so far**: check the actual code around `MicrodroidVmSessionFactory`/`MicrodroidVmSession` beyond just the `Builder` construction line — does it definitely call both `create()` *and* `run()` on the resulting `VirtualMachine`, and is that call properly awaited rather than fire-and-forget? A swallowed exception or an un-awaited coroutine around that specific call would produce exactly this symptom: `virtmgr`'s server comes up (spawned as part of `create()`), but the follow-up call that would make it launch `crosvm` never lands. Not yet checked — the real next diagnostic.

**FOUND, 2026-09-16 — the actual root cause, sitting in Warden's own audit database the entire time, not in logcat at all.** Queried `audit_entries.errorMessage` directly (needed `su 0 sqlite3 ... < file` via a pushed `.sql` file — plain shell quoting kept mangling it through the layers). The real exception:

```
android.os.ServiceSpecificException: failed to create idsig
Caused by:
  0: Fallback to v2 when v3 block not found is not yet implemented.
  1: No APK Signature Scheme block in APK Signing Block with ID: 4031998144
  2: entity not found (code -1)
```

`4031998144` = `0xF05368C4`, the real APK Signature Scheme **v3** block magic ID. **Microdroid needs to generate an "idsig" from the calling app's own APK to verify the payload before launching the VM at all** — and this AVF version's idsig generator has **no fallback to v2**, only v3. Warden's release APK has only ever been **v2-signed** (confirmed back in session 22 via `apksigner verify --verbose`: v2=true, v3=false). That mismatch is the actual, complete explanation — not sepolicy, not the GPU/surfaceflinger denials, not fd leaks, not the jniLibs handoff. None of the SELinux investigation was wrong to pursue (it eliminated real candidates and the reasoning was sound throughout), it just wasn't where this particular bug lived — Microdroid's idsig verification happens at a layer entirely below SELinux/AVC, and its failure is recorded only in the app's own audit DB, never in `logcat` or `dmesg` at all. **Nothing was ever being swallowed in Warden's own Kotlin code** — the "check whether `run()` is awaited" instinct pointed in a reasonable direction, but the real exception was just never being read from the one place it was actually written down.

**Fixed**: added `enableV3Signing = true` (plus explicit `enableV2Signing = true`) to `app/build.gradle.kts`'s release `signingConfig`. `v2.2.4` cut, tag pushed, `release.yml` running.

**Two clarifications per direct user instruction, not the agent's own call**: (1) this fix explains the VM-boot failure specifically — it does **not** explain the separate, still-unresolved periodic `cvd-1` stop (the ~10-minute pattern); that remains a distinct, open, third-party-tooling issue, untouched by this. (2) Commits are being pushed **unsigned on purpose** for a stretch going forward, batch-signing planned for later — not an oversight if unsigned commits show up.

**Not yet confirmed**: `v2.2.4`'s real release APK needs to actually build and get retested against the device before this counts as verified-fixed rather than well-diagnosed. That's the real remaining step — possibly the actual finish line for Phase 1c's entire VM-boot question.

**CONFIRMED, 2026-09-16 — the idsig fix works. `crosvm` actually launched for the first time ever, then hit a genuinely new failure.** `v2.2.4` built, installed as priv-app, retested with `auditctl -r 0` still active. The idsig error is completely gone. For the first time on Warden's own VM path: `"Running crosvm with args: [...]"` — a real, full `crosvm` command line (`microdroid_kernel`, `--cid 2049`, real composite disk images) — then `"Non-protected virtual machine ... (cid: 2049) started."` **This is past every previous blocker in this entire saga.**

It then dies almost immediately:
```
Failed to get guest CPU time: zero value is measured on elapsed CPU guest_time
Killing VM ... forcibly. Data might be corrupted!!!
crosvm(2485) exited with status signal: 9 (SIGKILL)
```
This is `virtmgr`'s own health-check logic deciding the freshly-started VM isn't alive (based on a CPU-time metric reading zero) and force-killing it defensively — not an app crash, not an `avc` denial, not idsig-related. Genuinely new territory: the first time the actual guest has run at all.

**Leading theory, real but unconfirmed**: nested virtualization. Cuttlefish itself is a VM (on AWS), so a real Microdroid guest here is genuine VM-inside-VM — `guest_time` (the field this almost certainly reads, from `/proc/<pid>/stat`: clock ticks a thread spent executing actual guest code) is a real, standard Linux kernel accounting field known to be fragile specifically in nested setups, where the L1 hypervisor has to correctly forward "entered guest mode" scheduler ticks up to L0. **Checked**: no public documentation of this exact error or `virtmgr`'s specific health-check mechanism exists — this is obscure internal AVF code. The mechanism itself is real and plausible, not confirmed for this specific case. Real next check before accepting the theory: whether the actual AWS instance type has genuine nested-virtualization CPU support enabled (not universal across EC2 families/generations) — `/dev/kvm` existing only proves KVM itself is available, not that nested guest-time accounting works correctly through it.

`/dev/kvm` confirmed present and accessible inside the guest (`crw-rw-rw-`) — ruling out a missing device node. The `ENODEV`/"No such device" error in the audit DB for this attempt is believed to be `virtmgr` trying to reconnect to the now-forcibly-killed VM's socket, a downstream symptom of the kill, not the original cause.

**Real head-to-head data, 2026-09-16 — the "accounting glitch" theory itself retracted, replaced with something sharper.** Installed `MicrodroidDemoApp` fresh on the same `cvd-1`, same rigor (`auditctl -r 0`, immediate logcat dump). It worked exactly as before: full boot, guest agent registered, payload ready, ~1 second total. Direct comparison against Warden's failing run:

1. **`crosvm` invocation args are essentially identical** between the two — same `--mem 256`, `--cpus num-cores=1`, same flag structure; only fd numbers/VM name differ (cosmetic). The config passed to `crosvm` is confirmed not the difference.
2. **The sharp finding**: the demo app's `crosvm` process logged 16 lines of its own real output (tagged `crosvm`, separate from `virtmgr`) — hypervisor creation, kernel load, block device attachment. **Warden's `crosvm` process logged zero lines of its own output, ever** — confirmed by grepping the exact process tag on both captures, not inferred.
3. Demo app's `crosvm` took ~33ms after "VM started" to print its first line, uninterrupted. Warden's kill fires within **~5ms** of "VM started" — before `crosvm`'s child process seems to get its first scheduled timeslice at all.
4. **Confirmed `virtmgr` kills proactively, not reactively**: `"Killing VM ... forcibly"` appears *before* `crosvm`'s own `SIGKILL` exit line in the log — `virtmgr` initiates the kill, it isn't reacting to `crosvm` already being dead.

Since the `crosvm` args themselves are identical, the difference has to be something about the specific invocation, not the command line. Two concrete, checkable hypotheses (not yet resolved):
- **Disk image content/I-O, not verification** — reframed from a hash-mismatch idea: if `crosvm`'s very first action (reading its own boot disk, the composite image the automation pipeline built with Warden's real payload baked in) hits a bad read immediately, the process could sit stuck before executing any real guest instruction at all — showing up as exactly zero `guest_time` not because it's dead, but because it never got past its first disk access. Concrete check: compare Warden's pipeline-built composite image's structure/size sanity against the demo app's manually-built, known-good one.
- **Per-VM resource starvation (cgroup/cpuset)** — does `virtmgr` assign per-instance CPU scheduling limits, and is Warden's specific VM instance getting starved relative to the demo app's. Concretely checkable live via `/sys/fs/cgroup` at VM-creation time, no source access needed.

Weighted the disk-image angle as the more likely first check (a concrete mechanical difference between the two runs — different image, identical config) but both are real next steps, not more speculation. Genuinely at the edge of what's diagnosable without direct `virtmgr`/AVF source access if neither pans out.

**RESOLVED, 2026-09-15 — real release published, future pipeline built, decoupled from the blocked broad CI permission.** Rather than wait on `AWS_GH_ACTIONS_ROLE_ARN` (the full fleet-launch permission scope, still not configured), the release-publish step only needed narrow S3 read access — a much smaller ask. Real release cut immediately: [`noxos-payload` v1.0.0](https://github.com/parrothacker1/noxos-payload/releases/tag/v1.0.0), asset `libnoxos_payload_stub-x86_64.so`, the actual verified artifact. (User specifically wanted `v1.0.0` over the initially-cut `v0.1.0` — the earlier release+tag were deleted and recut.)

Future-build pipeline: `.github/workflows/release-from-s3.yml` on `main` (`workflow_dispatch`, inputs `s3_path`/`tag`/`abi`, downloads from S3, runs `gh release create ... --generate-notes` — same shape as `noxos-app`'s own `release.yml`). Credentials: a **dedicated IAM user, `noxos-payload-release-ci`, scoped read-only to just `s3://noxos-releases/payload-build/*`** — narrower even than `noxos-server-ci`'s bucket-wide read, matching this project's established minimal-blast-radius pattern. IAM writes were blocked by the environment's own permission classifier, exactly as anticipated — handed the user the exact commands rather than working around the block, user reviewed and ran them directly, real access key now wired into repo secrets. Workflow is fully live, with zero dependency on the still-unresolved broader fleet-launch permission story.

Nudged `noxos-app`'s `sync-payload.yml` to trigger immediately via `workflow_dispatch` rather than wait for its next scheduled run — this is the first real, live, end-to-end test of the entire automation chain (payload release → detected → sorted by ABI → placed in `jniLibs` → app rebuilt/signed/republished).

**First real end-to-end run, 2026-09-15 — found and properly fixed one more real bug, then verified the payload binary is genuinely shipping inside a real release for the first time ever.** The detect/download/place-in-jniLibs/version-bump/commit/tag chain worked perfectly on the first real try against `noxos-payload` v1.0.0. But `release.yml` never fired — checked directly against the GitHub Actions API, zero runs triggered by either the `main` push or the version tag push. **Root cause: GitHub Actions doesn't cascade-trigger other push-triggered workflows from a push authored by the automatic `GITHUB_TOKEN`** (a deliberate anti-recursion measure) — the entire "push a tag, let `release.yml` pick it up" design was silently dead on arrival, never actually tested against a real automated push before now.

**Fixed properly, not worked around**: added `workflow_dispatch` to `release.yml` (takes a `tag` input, checks out that ref explicitly), and `sync-payload.yml` now explicitly calls `gh workflow run release.yml -f tag=...` after pushing instead of relying on a cascade that never existed. Still exactly one place owns the actual build/sign/publish logic — no duplicated logic introduced to work around the gap.

**Verified for real**: manually dispatched `release.yml` for the already-pushed tag to finish what the broken automatic run couldn't, then downloaded the actual published `v2.2.3` Warden release APK and confirmed `lib/x86_64/libnoxos_payload_stub.so` is genuinely inside it — 36,904 bytes, exact match to the real S3/GitHub-release asset size. **The real, Soong-compiled payload binary is, for the first time in this project's entire history, actually shipping inside a real published Warden release.**

**What's still not proven, and is the actual next step**: the payload being *inside* the APK isn't the same as the VM actually booting with it. The original Phase 1c question — does `virtmgr` get past spawning and actually launch a paired `crosvm` this time — still needs a live device test against this exact `v2.2.3` build. That's the real finish line, now finally within reach for the first time.

**arm64 build done, 2026-09-15 — verified the same rigorous way, shipped in the same release.** `noxos-os` only defines a `noxos_cf_x86_64_phone` product, no NoxOS-specific arm64 target — since `noxos_payload_stub` has zero NoxOS-specific dependencies (no device tree, no platform keys), built it against stock AOSP's `aosp_cf_arm64_phone-trunk_staging-userdebug` instead, a clean substitution. Result: `libnoxos_payload_stub-arm64-v8a.so` — genuine ELF ARM aarch64, independently verified the same way as the x86_64 build (`readelf -d`: correct `NEEDED libvm_payload.so` + standard libs, correct `SONAME`; `AVmPayload_main` exported). Uploaded alongside the x86_64 asset — `v1.0.0` now ships both ABIs.

**Pipeline generalized properly for multi-ABI, one real bug caught along the way**: `build-payload.sh` was uploading to a bare filename in S3 — would have silently collided between ABIs sharing the same prefix once a second ABI existed. Fixed: takes an ABI env var now, names uploads accordingly. New `build-all-abis.sh` builds both ABIs in one instance run under one shared S3 prefix; `bootstrap.sh` calls that instead of the single-ABI script, and — applying the UserData-staleness lesson from earlier in this same session correctly this time — actually re-baked into a new launch template version (v6), not just committed to the repo. `release-from-s3.yml` updated to take an `s3_prefix` and publish everything under it as one multi-asset release instead of one file at a time.

**Worth remembering for later, unrelated but real**: hit a classic `pkill -f` self-match bug mid-session — killing a process by grepping its command line over SSH matched the kill command's own invocation text and killed the SSH session instead of the target. Fixed by listing PIDs explicitly rather than pattern-matching. Noted in `noxos-payload/TASKS.md` in case it recurs elsewhere in this project. All build instances terminated, all fleets confirmed at zero capacity — no ongoing cost.

**Naming discrepancy found and resolved, 2026-09-15 — real mismatch, own earlier claim corrected along the way.** Two peer agents disagreed on whether Soong auto-prepends `lib` to a `cc_library_shared` module's output filename (Warden App said yes, NoxOS Payload work — mid-build, about to find out empirically — said no). **Checked Soong's actual source directly** (`cc/library.go`'s `getLibNameHelper`): no automatic prefix exists — the module name becomes the output filename verbatim unless an explicit `stem` property overrides it. `noxos_payload_stub` (the current, unprefixed Android.bp module name) would therefore genuinely produce `noxos_payload_stub.so`, not `libnoxos_payload_stub.so`.

Initially downgraded this to "non-blocking" reasoning that `noxos-app`'s `sync-payload.yml` always renames whatever it downloads to a fixed literal `libnoxos_payload_stub.so` in `jniLibs/` — true, and that part still holds for the APK-packaging side. **But this missed a second, separate mismatch, caught by `noxos-payload` itself**: `vm_config.json`'s `task.command` also said the bare `noxos_payload_stub.so`, and that field governs what filename the Microdroid launcher looks for *inside the VM's own payload image* — completely unrelated to how `noxos-app`'s CI renames a downloaded release asset. **Own error, corrected here**: earlier guidance told `noxos-app` that `task.command`'s bare-name convention was "already correct, nothing to rename" — that was an over-generalization from one AOSP example whose module simply didn't have a `lib`-prefixed name to begin with, not an actual rule that `task.command` avoids `lib` prefixes. The real rule is just "`task.command` must match the actual compiled filename." Since the real output is about to become `libnoxos_payload_stub.so` (the `stem` fix), `vm_config.json` genuinely needed updating too — and until this fix, it was silently out of sync with `MicrodroidVmSession.kt`'s hardcoded expectation, exactly the failure class (`vm.run()`-time, not compile-time) this whole investigation started from.

**Fixed, pushed to `noxos-payload` main (`322402a`)**: `stem: "libnoxos_payload_stub"` added to the Android.bp module, and `vm_config.json`'s `task.command` updated to `libnoxos_payload_stub.so` to match. All four touch points (Android.bp stem, `vm_config.json`, `MicrodroidVmSession.kt`, `sync-payload.yml`'s rename target) are now consistently `libnoxos_payload_stub.so`. Empirical verification in progress: the in-flight build (`i-0d14955ae63d56193`) had already cloned `main` before this fix landed, so it'll complete as a clean "before" data point confirming the no-auto-prefix finding either way, then a second build will confirm the fix produces the correctly-named artifact.

## 20. The `cvd stop` mystery — CONFIRMED FIXED, 2026-09-16, on a real booted device

Multiple ground agents independently hit the shared Cuttlefish instance (`cvd-1`) being stopped every ~10-20 minutes throughout the day, interrupting active testing. Checked host crontab, systemd timers, and the container's own cron across two separate agents — none installed, source unexplained at the time.

**Root-caused directly, 2026-09-15, via the orchestrator's own logs** (`docker logs cuttlefish_orchestrator`, and `/run/cuttlefish/operator.log` inside the container): every single "Device connected" event is followed by "Error reading from device: EOF" at **exactly 10:00:00 later**, consistently, going back to 06:41 that morning — not an occasional glitch, a hard, deterministic pattern all day. Checked the operator process's actual launch command and every config file (`/etc/default/cuttlefish-operator`, its systemd unit, its environment) — no timeout flag or config knob exists anywhere in this deployment. **Initial read: a hardcoded idle-read timeout inside the `android-cuttlefish` project's own `operator` binary** — third-party tooling behavior, not this project's code, not fixable from here.

**Correction, 2026-09-16, after actually checking the real upstream source rather than resting on the initial inference.** Traced the exact log line to real, unmodified Cuttlefish code — `frontend/src/operator/operator.go` line 573, inside `deviceEndpoint()`:
```go
if err := c.Recv(&msg); err != nil {
    log.Println("Error reading from device: ", err)
    return
}
```
Genuinely upstream, not something this deployment introduced. **But checked every plausible source of an explicit 10-minute timeout — `main.go`, `operator.go`, `devices.go`, `jsonservers.go`, and the container's actual nginx config — and found none.** Nginx doesn't even proxy the operator's own device/controller endpoint (it proxies `host_orchestrator` on a different port instead). No `SetReadDeadline`, no `http.Server{ReadTimeout/IdleTimeout}`, no matching duration constant anywhere checked.

**Better-grounded reading of the same evidence, not yet confirmed**: the error is specifically `EOF` on a *read* — that means the *other end* (the device/guest side) closed the connection, not the operator applying a server-side timeout against it. That points toward the disconnect being initiated by the device/webrtc process itself on some internal ~10-minute cycle, not the operator package. Not yet checked: `device/google/cuttlefish`'s own webrtc source, the more likely real location of this mechanism. No external community reports of this exact symptom found via search either — may be too specific to surface generically, or genuinely undiscussed.

**Major correction to the whole framing, 2026-09-16 — per the user, this did NOT happen in v0/v1, only started after baking Warden into the ROM.** This reframes the entire investigation: not confirmed pre-existing third-party Cuttlefish behavior after all — very likely **caused by something in Phase 1c's own changes** (Warden as a baked-in priv-app, its signing, or its runtime behavior), not an unrelated upstream quirk. Retracting the "not this project's bug, not fixable from here" framing until this is actually ruled out.

**Leading hypothesis, concrete but unconfirmed**: Warden's entire core function is a `VpnService`-based network monitor that intercepts *all* device network traffic. That's exactly the kind of thing that could plausibly interfere with the Cuttlefish guest's own WebRTC connection back to the host operator, since both compete for the same device network path — an interaction that simply couldn't occur in v0/v1, since Warden wasn't baked in or running at all then. No external source confirms this specific interaction (checked, nothing found) — this is reasoned, not verified.

**Proposed clean empirical test, not yet run**: boot a fresh instance with Warden baked in as before, but **never launch Warden at all** — leave the device idle at the home screen. Watch the operator log for the usual ~10-minute EOF pattern. If it still happens with Warden never opened, that rules out Warden's *active* runtime behavior (VPN, monitoring) as the cause and points toward something more passive (the priv-app placement or signing change itself). If it does *not* happen, that strongly implicates Warden's active runtime specifically — and a follow-up test (launched but VPN monitoring not started, vs. monitoring actively running) would narrow it further, isolating whether merely having the app open matters or specifically its VPN interception does. In progress — idle experiment running, result pending the ~10-11 minute window.

**Two related findings surfaced along the way, 2026-09-16**: (1) the `signature|privileged` OR-relationship theory practically confirmed, with an honest methodological caveat — sideloading `v2.2.4` over the existing priv-app install via plain `adb install -r` (no verity/remount dance) kept `MANAGE_VIRTUAL_MACHINE` granted=true and the VM-launch attempt proceeded normally. But `dumpsys` still shows the package carrying the `PRIVILEGED` flag, likely inherited from its prior system-app history — so this test doesn't cleanly isolate "signature alone, no priv-app history" the way `MicrodroidDemoApp`'s case does (never a priv-app, ever, granted purely on signature match). Both together still add up to real confidence, just not a fully clean isolation on this one. (2) Re-ran the crosvm-kill test against this same sideloaded, non-priv-app-placed install: **identical failure, identical signature** (killed ~4ms in, zero `crosvm` output) — this rules out priv-app placement/install method as a factor in the crosvm-kill bug specifically. Happens the same way regardless.

**Real counter-evidence against the VPN hypothesis, surfaced before the idle-test result even landed — good discipline, not confirmation bias.** Across this entire session's testing, Warden's `VpnService`/`NetMonitorService` was **never once successfully activated** — every test used only the manual "Select & Scan File" path, which doesn't touch it at all; "Start Monitoring" either failed (no network access for most of the session) or was simply never triggered once the network fix landed, since testing was entirely focused on the VM-boot path. **Every single crash/EOF happened with Warden's VPN inactive, not once with it running.** If VPN-competes-with-WebRTC were the actual mechanism, it shouldn't have fired during any VPN-inactive testing — it fired constantly. This is real evidence *against* the active-runtime hypothesis, leaning the explanation back toward something more passive: priv-app placement itself, or the signing change, rather than anything Warden actively *does* at runtime. Not conclusive alone — the idle-launch test (Warden present but literally never opened at all) is the cleaner, still-pending confirmation, since even "installed but never monitoring" still means the app process exists and could matter passively.

**Superseded reframe, 2026-09-16 — idle-launch test's "guest freeze" theory was WRONG, corrected within the hour by the same agent.** Initial read of the idle-launch test (kernel log going silent at guest-uptime ~17-37s) was that the guest never finished booting. **Disproved directly**: a live `adb shell` into the "frozen" device on the reset+recreate instance showed it fully booted and healthy — `sys.boot_completed=1`, `dev.bootcomplete=1`, bootanim stopped, real uptime advancing past 105s, fully responsive. What actually goes quiet is the **host's kernel-log console pipe**, at the point the guest normally reduces console verbosity post-boot — not the guest itself.

**Real root cause, confirmed via direct kernel.log evidence**: `cvd`'s own boot-completion detection never fires, so fleet status stays stuck on "Starting" for the full 10 minutes even though the device is provably booted — then `run_cvd` declares `VIRTUAL_DEVICE_BOOT_FAILED` and kills a perfectly healthy device. This is also what produces the `operator.log` EOF — same event, not an independent disconnect bug (that part of the original theory still holds; only the "why detection never fires" changed). Boot-completion signaling to the host is `com.android.google.gce.gceservice`'s (`CuttlefishService.apk`, stock AOSP vendor priv-app) job, done by writing the `VIRTUAL_DEVICE_BOOT_COMPLETED` marker to `/dev/kmsg` — and SELinux is denying that write:
```
avc: denied { write } for comm=".gce.gceservice" name="kmsg" dev="tmpfs" scontext=u:r:platform_app:s0:c512,c768 tcontext=u:object_r:kmsg_device:s0 tclass=chr_file permissive=0 app=com.android.google.gce.gceservice
```
`gceservice` is running under the generic `platform_app` domain instead of its own dedicated one (which normally carries `kmsg` write access).

**Very likely the same bug class as the already-fixed signing-key regression** (`fa4fab8`/`629efbb`, item 17/18's root cause): `629efbb`'s own commit message states plainly that `seapp_contexts` assigns process domains off the literal signing cert, and that collapsing AOSP's named key roles onto the new custom platform key broke `media`/`networkstack` this exact way. That fix restored AOSP stock keys for 8 other roles but deliberately kept `platform` on the new custom NoxOS key (Warden needs this). `gceservice` is a platform-signed vendor prebuilt, so it's plausibly hitting the identical cert/seinfo mismatch class, just previously undetected because it manifests as a silent 10-minute timeout rather than an obvious crash. **Not yet confirmed against the real device-tree/sepolicy source** — needs checking: (1) what cert `gceservice`'s prebuilt actually carries vs. what `platform` now resolves to, (2) whether a stale `mac_permissions.xml`/`seapp_contexts` entry is still keyed to the old platform cert, or a missing package-specific domain override is needed. Dispatched to `noxos-os` (has the actual tree). **Also flagged as relevant to issue 1** (the virtmgr/crosvm-kill mystery, still open below) — worth checking whether Warden's own VM process is *also* running under an unexpectedly generic SELinux domain for the same cert/seinfo-mismatch reason, a much more concrete mechanism than the earlier cgroup/cpuset guess. **Caveat, explicitly flagged by the reporting agent**: this issue-1 cross-link is a hypothesis only, not yet checked against real evidence — the agent lost the device to the boot-fail kill before reaching the SELinux-domain check on virtmgr/crosvm. Don't treat it as confirmed.

**Direct confirmation, 2026-09-16, zero ambiguity left**: installed Warden v2.2.4, launched `MainActivity`, watched it render in logcat (zygote fork, activity displayed, profile install) at 21:23:52 — a fully working, interactive device. At 21:25:13 (~10:47 after the 21:14:26 boot start), `cvd fleet` flipped to `"status": "Boot Failed"` and adb got refused — **cvd killed a device that had been running and rendering a launched app seconds earlier.** This is conclusively a host-side false-timeout/detection bug; there is no guest-side problem to chase further. Fix is dispatched and pending `noxos-os`'s sepolicy investigation; each retest cycle costs ~11 min of dead time, so further live instance cycling is paused until there's a concrete fix (new image, or a specific `seapp_contexts`/`mac_permissions.xml` change) to verify against.

**Exact mechanism confirmed against real AOSP source, 2026-09-16, fix committed — same bug class as `fa4fab8`/`629efbb`, different victim.** `system/sepolicy/private/mac_permissions.xml` has an unconditional `<signer signature="@PLATFORM"><seinfo value="platform"/></signer>` rule; `@PLATFORM` resolves to whatever `PRODUCT_DEFAULT_DEV_CERTIFICATE` is — the same cert-substitution mechanism `fa4fab8` broke for `media`/`networkstack`/etc. `CuttlefishService`'s `Android.bp` has no `certificate:` override, so once the platform cert changed, it started matching `@PLATFORM` too and got `seinfo=platform` instead of the `seinfo=default` its dedicated rule in `device/google/cuttlefish/shared/sepolicy/vendor/seapp_contexts` actually needs to land it in the real `gceservice` domain — falls through to generic `platform_app`, which lacks the `/dev/kmsg` write rule, exactly matching the observed denial. Checked: `CuttlefishService` is the only vendor priv-app in Cuttlefish's sepolicy relying on `seinfo=default` this way — isolated, not a systemic pattern across other vendor apps.

**Build/verification timeline, for the record**: full rebuild took 2h49m (10830 ninja steps), succeeded clean, uploaded to S3. First local boot attempt failed for an unrelated reason — `super.img` was corrupted during local extraction (a background download task got killed by a low-memory event), not a ROM bug. Re-fetching directly rather than via a background task; real yes/no on the gceservice fix still pending as of this note.

**Fix**: `PRODUCT_CERTIFICATE_OVERRIDES += CuttlefishService:aosp-testkey` added to `noxos_cf_x86_64_phone.mk` — gives the vendor prebuilt back a non-platform-matching cert (same approach as `629efbb`, not a raw sepolicy `allow` rule, which would've masked the same bug class again). Format confirmed against `build/soong/android/config.go`'s `OverrideCertificateFor`, a real AOSP mechanism. Committed `0599cd7`, pushed to `main`, **unsigned** (GPG hung; falls under this project's established overnight fallback — ask, get yes, commit unsigned, re-sign later — same pattern as `e8ed1eb` above). **Not yet fully verified**: a full rebuild is running now on the diagnostic instance to confirm the fix lands end-to-end before this is declared actually fixed; Warden App will get the new image once it's confirmed.

**Issue 1 cross-link checked and not confirmed**: looked for Warden's VM process landing in a similarly-wrong SELinux domain via the same cert/seinfo-mismatch mechanism — did not find it. Issue 1 (virtmgr/crosvm-kill) is a separate, still-open mechanism, not explained by this fix.

**Unsigned commits outstanding, needing re-signing later**: `e8ed1eb` (line above) and now `0599cd7` — both under the same GPG-hang fallback.

**Gap closed, 2026-09-15 (later the same day)**: the "whoever notices, recovers by tearing down" theory doesn't hold up — direct, exact-second timestamp correlation across two completely independent log files shows the EOF disconnect itself, not any agent's reaction to it, is what kills the VM. Cross-referenced every `operator.log` "Error reading from device: EOF" timestamp against `launcher.log`'s `VIRTUAL_DEVICE_BOOT_FAILED`/`crosvm exiting with success` events for the same instance: they match **to the exact second**, every time (e.g. both logs show `18:18:34` and `18:30:13` independently) — across 16 total EOF events spanning the entire day, including stretches of multiple hours where no agent was actively connected or issuing any commands at all (12:09→17:53, over 5 hours idle, same exact-10:00:00-then-crash pattern resumed the moment a connection was re-established). No human/agent action is required to turn the disconnect fatal — it's mechanically the same event or a direct, automatic cascade, not a benign signal that only becomes a problem if someone reacts badly to it. Practical upshot unchanged (still not fixable from this project's side, still not worth chasing further as a project bug), but the theory of *why* it's fatal is now actually right, not just a plausible guess — and it means "don't intervene when you see it disconnected" wouldn't have helped; the guest was always going to die at the 10-minute mark regardless of what any agent did.

## 21. On-device model download fails, 2026-09-16 — S3 403, and a real discrepancy with PROJECT.md's "GitHub Releases" claim

User hit "Start Monitoring" on the now-working live device and got "Couldn't load the threat model — check your connection and try again." `Warden App` traced it directly: `ModelUpdateManager.kt` fetches `BuildConfig.MODEL_MANIFEST_URL = "https://noxos-releases.s3.amazonaws.com/on-device-models/network-classifier/manifest.json"` on every "Start Monitoring" tap. Curled that exact URL directly: real HTTP 403 `AccessDenied` from S3 — request reached AWS fine (not a device connectivity problem; ruled out explicitly, ICMP being filtered on this Cuttlefish network is separate/expected and doesn't affect HTTP). The app's own error message is honest but misleading — it blames "your connection" when the real cause is server-side (object never uploaded, or uploaded without public-read ACL).

**Real discrepancy worth resolving**: `PROJECT.md` states the model pipeline uses "real GitHub Releases hosting" — but the actual `BuildConfig.MODEL_MANIFEST_URL` in the shipped app points to an S3 bucket, not GitHub Releases. Either the app's URL is stale/wrong, or the pipeline moved to S3 at some point and `PROJECT.md`'s line is now outdated documentation, or the S3 path is a second channel that was simply never populated. Not yet resolved — dispatched to `noxos-inference` (owns the model artifact/pipeline per `PROJECT.md`'s "Status clarification, 2026-09-14") to clarify which is canonical and fix whichever is wrong.

Not something `noxos-app` can fix from their side — no S3 write access to that bucket, and no visibility into whether the trained model file exists anywhere yet. Until resolved, "Start Monitoring" is expected to fail — a known limitation, not a regression from tonight's VM-boot/SELinux fixes.

**Clarified directly by the user, 2026-09-16**: GitHub Releases was always the intended canonical hosting for this model (matches `PROJECT.md`'s "real GitHub Releases hosting" line) — the S3 URL in `BuildConfig.MODEL_MANIFEST_URL` is stale/wrong, most likely left over from before the pipeline settled on GH Releases. **User-directed plan**: (1) immediate stopgap tonight — push the model+manifest to the same S3 path the *currently installed* app build already points to (with public read ACL) so testing can continue without needing a new APK, verify the 403 clears; (2) proper fix — `noxos-app` updates `BuildConfig.MODEL_MANIFEST_URL` to the real GitHub Releases URL, bundled into a new release alongside the quarantine-not-happening bug (see below).

**Final architecture decision, superseding the above, 2026-09-16 — user is fine with S3 after all, but it needs a real update-check structure, not just a static file.** Verbatim intent: a `latest/` path holding the current model plus its SHA256 digest, and the app compares that digest against what it already has to decide whether to download an update — not GitHub Releases after all. Concretely: `s3://noxos-releases/on-device-models/network-classifier/latest/manifest.json` (or equivalent) must contain the current model reference **and** its SHA256, publicly readable; `ModelUpdateManager` fetches that manifest and compares the digest to its locally cached model's digest, only re-downloading on a mismatch. `PROJECT.md` already claims "real checksum verification on the app side" exists — if so this may mostly be an app-side URL/wiring fix plus an inference-side publish-structure fix, not new logic on either side. Dispatched to both `noxos-inference` (publish to this structure for real) and `noxos-app` (verify/wire the digest-compare logic against it).

**Unblocked directly, 2026-09-16 — real root cause of the 403 found, and the real existing model published for real, without waiting on `noxos-inference` to come back online.** User explicitly directed using whatever model already exists rather than waiting further ("use the defective model for this now .. we already have a model built right .. use that for now .. which will give wrong results but let's use it"). Checked directly with local AWS CLI access (root agent has real credentials, `aws sts get-caller-identity` confirmed): the S3 bucket `noxos-releases` has `BlockPublicAcls`/`IgnorePublicAcls` enabled — **any `--acl public-read` on upload is silently ignored**; public read only works via an explicit bucket policy, and the existing policy only covered `full/*` and `patches/*`, not `on-device-models/*`. This is the real, structural cause of the 403 — not a missing upload, a missing policy statement. Fixed by extending the existing `PublicReadForOTA` bucket policy statement to also cover `arn:aws:s3:::noxos-releases/on-device-models/*`, matching the existing pattern exactly rather than inventing a new mechanism.

The model itself already exists and is real, not a placeholder: `noxos-inference`'s own GitHub Releases (`ondevice-model-latest` tag, `gh release view`) already has a genuine trained-model artifact (`model.json`, `manifest.json` with `version`/`sha256`/`modelUrl` fields already correctly populated, `METRICS.md`) — confirms `PROJECT.md`'s "real GitHub Releases hosting" claim was accurate for the artifact itself, just never mirrored to the S3 path the app actually reads from. Downloaded it, verified the claimed sha256 matches the real file exactly, republished to `s3://noxos-releases/on-device-models/network-classifier/latest/{manifest.json,model.json}` with `modelUrl` rewritten to point at the S3 copy. **Verified live end-to-end**: both URLs return HTTP 200 with no auth, and the sha256 of the actually-served `model.json` matches the manifest's claimed digest exactly. Not necessarily "wrong results" as the user assumed — this looks like the same real trained model referenced elsewhere in the project (F1≈0.98 per item 16's corrected number), just never having reached this exact path before; whether this specific training run is the latest/best one is unconfirmed without `noxos-inference` back online, but it's real and verified-consistent, not garbage.

**Checked precisely rather than trusted, 2026-09-16 — `PROJECT.md`'s claim was half-true.** Checksum verification existed only as post-download integrity checking (verify downloaded bytes match the manifest's claimed sha256) — the actual "should I re-download" decision was gated on an integer `version` field, with no locally-persisted digest to compare against at all. Genuinely needed new logic, not just a re-point. Fixed, commit `b7864de`: added a `sha256` field to the persisted local `Meta`, changed the update-gating condition from `manifest.version <= meta.version` to `manifest.sha256 == meta.sha256` (case-insensitive); kept the post-download integrity check as-is, kept `version` around since `ModelUpdateResult.Updated` still returns it. Checked the existing `ModelUpdateManagerTest` suite by inspection — nothing pins the old version-gating behavior, shouldn't regress; CI running to confirm. `BuildConfig.MODEL_MANIFEST_URL` not yet re-pointed — checked the real `latest/manifest.json` URL directly, still HTTP 403, not live yet. One-line change ready the moment it is.

**New bug, same session, 2026-09-16**: user reports flagged files are not actually being quarantined. `PROJECT.md` describes a real, previously-built `QuarantineManager` (session 14: physical move out of `MediaStore.Downloads` into app-private storage, triggered by a flagged verdict) — this is a regression or a wiring gap somewhere in that path, not a new feature to build from scratch. Dispatched to `noxos-app` to investigate and fix.

**RESOLVED, 2026-09-16 — real, precise wiring gap, not a regression from tonight, apparently never worked for the manual-scan path.** `MainActivity` has two scan entry points: `handleAutoScan()` (triggered by `FileArrivalWatcher` on an untrusted app dropping a file into Downloads) correctly calls `quarantineManager.quarantine(...)` on any non-`Success` result — but the local `scanFile()` bound to the "Select & Scan File" button, **the only path tested all session, tonight's included**, just ran the scan and posted a notification, never calling `quarantine()` at all. A straight omission, not a disconnected trigger or broken condition. Fixed by mirroring `handleAutoScan`'s exact quarantine logic into manual `scanFile()`. Committed `3cfb6fd`, unsigned, `ci.yml` verifying now.

**Honestly-flagged, pre-existing, unrelated caveat**: `QuarantineManager.quarantine()`'s `contentResolver.delete(uri,...)` step (removing the original after copying to app-private storage) was written against `MediaStore.Downloads` URIs, which is all `handleAutoScan` ever sees — a file picked via the manual picker from a different document provider (Google Photos, a cloud provider) may not always delete successfully. The copy into quarantine still succeeds either way; only the delete-original step could be flaky by provider. Not fixed now (unrelated to the actual bug, not reported as an issue) — noted for later.

**SHIPPED AND CONFIRMED LIVE, 2026-09-16 — v2.2.6 released with all three fixes** (quarantine `3cfb6fd`, digest-gated model updates `b7864de`, model URL re-point `f09429c`, plus the vsock fix carried from `v2.2.5`). `noxos-app` independently re-verified the S3 manifest before shipping — computed the served `model.json`'s SHA256 itself and confirmed it matched the manifest's claimed digest, not just trusting the root agent's report.

**"Start Monitoring" ran end-to-end successfully for the first time in this project's history.** No more "check your connection" error — model loaded silently and correctly. A real Android VPN consent dialog appeared, was accepted, and system logs confirm a genuinely established VPN: `Vpn: Established by com.noxos.app on tun0`, `ConnectivityService` validated it, a real `tun0` interface at `10.0.0.1/32`, a persistent foreground-service notification. UI confirms it too: home screen shows "MONITORING — 0 CONNECTIONS INSPECTED", button reads "Stop Monitoring", VPN key icon in the status bar. Screenshots saved: `vpn_consent_dialog.png`, `monitoring_active.png`.

**Every open item from tonight is now closed**: SELinux boot-signal bug, vsock-race bug, quarantine wiring gap, model download/digest logic, and now a full live confirmation that network monitoring itself works end-to-end — both the file-scan and network-monitoring halves of Warden are genuinely functional on real hardware for the first time.

## 22. Two real findings from user questioning the Safe/Flagged label, 2026-09-16

User asked why one test connection showed "Safe" vs others "Flagged," and whether flagged traffic actually triggers a VM check. `noxos-app` traced both through the real code rather than guessing:

1. **`OnDeviceNetworkClassifier.kt` is dead code — never called anywhere in the app.** Confirmed via grep: zero call sites outside its own file and its tests. The Safe/Flagged label users see is a simple heuristic in `NetMonitorService` (flagged = relay failed OR ACL cache says flagged), not the ML classifier at all. Tonight's model-download fix (item 21) makes the model loadable, but nothing in the live dispatch path actually consumes its output for network traffic yet. This matches `PROJECT.md`'s own "Status clarification, 2026-09-14" note (`OnDeviceNetworkClassifier` is built and tested but nothing in the live dispatch path calls it yet — that's the cascade-wiring task, specced not built) — so this is a known, already-documented gap, not a new regression, but the user should be aware the "Safe/Flagged" label is not currently ML-informed at all.

2. **Flagged traffic does trigger a real VM check** — `NetworkSampleVmDispatcher` polls every 30s and launches an actual Microdroid VM via the same `MicrodroidVmSessionFactory` path as file scanning. Confirmed firing in logcat, but hit the identical payload-stub `exit=1` bug documented in item 18/21 (`WardenNetworkSampleVm: ... VM payload finished before becoming ready (exit=1)`). **This means the payload-stub bug blocks both file scanning and network-traffic VM checks, not just files as previously assumed.** Already fixed upstream (`noxos-payload` commit `7b4ebfb`), `v1.0.1` build still running on EC2 as of this note — once it lands and syncs in, both the file-scan and network-VM paths need retesting, not just file-scan.

Not yet resolved: whether `OnDeviceNetworkClassifier` being unwired is expected at this stage (deferred cascade-wiring work, matches the existing spec) or something the user wants prioritized now — flagged to the user rather than assumed either way.
