# app/src/main/res/drawable/ic_launcher_foreground.xml

## Root `<group>` scale (0.82x)

Scaled to fit the adaptive-icon safe zone (66dp guaranteed-visible circle within the 108dp
canvas). Measured: hexagon circumradius 180 viewport units vs. ~156 safe radius — fixed
with a 0.82x scale about the icon's center. Same fix mirrored in the source SVG at
`assets/branding/ic_launcher_foreground.svg` (root NoxOS meta-repo).
