# noxos-os — Task List

Last updated: 2026-09-12. Read [`../README.md`](../README.md) (hub) and [`../PROJECT.md`](../PROJECT.md) (architecture, source of truth) first for cross-repo context — this file is `noxos-os`'s own task history, split out from the old combined `TASKS.md` (now just an index at `../TASKS.md`) so a session working only on this repo doesn't need the other four repos' history. See also this repo's own [`README.md`](README.md) and [`CUTTLEFISH.md`](CUTTLEFISH.md).

Session numbers below are this repo's own chronology (AWS/build-infra work), tracked somewhat independently from `noxos-app`'s session numbers since a lot of sessions touched only one repo or the other.

## Cross-repo pointers

- v1 was confirmed booted and running Warden for the first time on 2026-09-12 — the boot/test work itself (adb checks, installing Warden on it) is in [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md) session 11 item 1. This file only has the OS-build side (the v1 artifact itself finishing compilation on 2026-09-10/11).
- Phase 1c (jniLibs handoff, Warden priv-app integration) is cross-repo — see also [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md) and [`../noxos-payload/TASKS.md`](../noxos-payload/TASKS.md).
- The "Resolved — AVF compile blocker" episode (Gradle `trigger-router` module, `android.system.virtualmachine.*` `@SystemApi` issue) is `noxos-app` content, not this repo — see [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md).

## What happened, in order

**2026-09-10, Phase 1b resolved — first real device ever booted in the local Cuttlefish container.** `cuttlefish_orchestrator` had been torn down since the previous session; brought back up via `setup-cuttlefish-local.sh --start` (image already built, so just a container relaunch). The 2026-09-06 v0 artifacts (`~/Downloads/noxos-v0/aosp_cf_x86_64_phone-img-ubuntu.zip` + `cvd-host_package.tar.gz`) were copied into the container (`docker cp`) and extracted — `unzip` isn't present in the container image, used `bsdtar -xf` on the zip instead (works fine, no new dependency). **The actual invocation, never worked out before**: `cvd create --product_path=<extracted image dir> --host_path=<extracted host package dir>` — both flags take plain extracted-directory paths, no `--config_file`/fetch-from-AB needed for a local build. One real path mistake made and fixed live: the host package tarball extracts flat (`host/bin/...`), not nested (`host/host/bin/...`) — `cvd create` fails fast with a clear "does not contain cvd_internal_start/launch_cvd" error if the path is one level too deep, easy to self-diagnose. Booted clean on the first correct attempt (`assemble_cvd` logged two "invalid geometry magic signature" warnings on the logical-partition metadata, non-fatal — device still reached "Virtual device booted successfully"). Confirmed alive via the container's bundled `adb` (`/root/v0/host/bin/adb connect localhost:6520` — the host's `/usr/bin/adb` also works against the same forwarded port): `ro.build.version.release`=17, `ro.product.name`=`aosp_cf_x86_64_phone`. This closes the last open half of Phase 1b — the remote-build/local-test split is now proven end-to-end for v0; v1 (the custom device overlay) still needed its own separate verification once its own AOSP build succeeded (see below — it has, 2026-09-10/11).

**Session 8** (2026-09-04) — relaunched the compile fleet and wired GitHub Actions to drive it:
1. Confirmed the prior session's teardown finished clean: volume `vol-04f36bb86126e3ab8` back to `available`, `snap-0b048190ff832d2a9` (sync-complete checkpoint) `completed` 100%.
2. Relaunched the fleet from the same `fleet-config.json`, but pinned `LaunchTemplateSpecification.Version` to `"$Latest"` instead of the old hardcoded `"1"` — the old fleet never picked up the v2/v3 bootstrap fixes (device-detection, systemd-run detachment, `HOME` inheritance) because `modify-fleet` can't repoint a launch template post-creation. New fleet: `fleet-b42c4be0-64de-4dc5-919d-b26ecc60ddaf`, instance `i-010a424de5a26096a` (r5n.2xlarge). Bootstrap runs unattended from here — attaches the volume, `repo sync`, `build.sh` (now with the `trunk_staging` lunch-combo fix), uploads to S3, scales the fleet to zero, snapshots, terminates. The interrupt-watcher/maintain-type/snapshot-on-reclaim design from session 7 already covers spot interruption failsafe; nothing new needed there.
3. **Added GitHub Actions as the trigger + status surface**, replacing the old non-functional `runs-on: self-hosted` stub in `noxos-os/.github/workflows/build.yml` (that stub assumed a self-registering runner that was never built). New design needs no Lambda/SNS/EventBridge: `workflow_dispatch` → `launch-fleet` job (OIDC-assumed role, `aws ec2 create-fleet --cli-input-json infra/fleet-config.json`, now committed to the repo) → `watch-build` job (same role, polls `aws s3api list-objects-v2` on `noxos-releases/cuttlefish-local/` and the fleet's target capacity every 5 minutes in a loop, `timeout-minutes: 600`) — pass/fail shows up natively in the Actions run, no separate watcher service to maintain. Pushed as `ad37ef9`.
4. IAM side (OIDC provider + `noxos-gh-actions-role` + inline policy scoped to `ec2:CreateFleet`/`RunInstances`/describes + `iam:PassRole` limited to `noxos-rom-compile-role` + read-only S3 on `noxos-releases`) is written to `knowledge-graph/setup-gh-actions.sh` but **not yet run** — `aws iam create-open-id-connect-provider` is blocked by Claude Code's auto-mode classifier same as every other IAM write this project has hit, needs the user to run it once via `!`. The script also sets the `AWS_GH_ACTIONS_ROLE_ARN` repo secret via `gh` at the end if `gh` is authenticated locally.
5. SSH access to the fleet's instances needs `~/Downloads/noxos-key.pem` (user `ubuntu`) — not `~/.ssh/id_ed25519` (the shell's default identity, which isn't the keypair the launch template actually uses). Worth remembering next time a live box needs checking.
6. **User caught a real bug in the volume-reuse logic**: `bootstrap.sh` only reused `vol-04f36bb86126e3ab8` directly when the fleet happened to land in its exact AZ; a cross-AZ launch created a fresh volume from the latest snapshot but never cleaned up the old one — a slow leak of orphaned EBS volumes every time the 3-subnet fleet lands somewhere new. Fixed: every boot now always creates a fresh volume from the latest snapshot and sets `DeleteOnTermination=true` via `modify-instance-attribute` right after attach, so the volume is discarded automatically whenever the instance dies (success, spot reclaim, doesn't matter) — snapshots are now the only persistence layer, no named volume to keep in sync. Needed `ec2:ModifyInstanceAttribute` added to `noxos-rom-compile-role`'s inline policy (user ran it manually, same classifier block as every other IAM write). Shipped as launch template `lt-0b31a12283756a7a3` v4.
7. **Diagnosed a real (but non-fatal) slowdown, live, via cron-scheduled checks**: `apt-get install awscli git` was taking 40+ minutes instead of the expected few. Root cause confirmed via `curl -v`/`ss`/`/proc/net/dev`: `us-east-1.ec2.archive.ubuntu.com` DNS round-robins across many backend IPs, and at least one (`98.86.242.16`, hit twice) completes the TCP handshake but sends zero bytes back — apt was waiting out the full default 120s `Acquire::http::Timeout` every time it landed on that IP before retrying. Confirmed real progress was still happening (log line count and `ss` connection state both moved between checks), so did not touch the live, `set -e`-guarded cloud-init process — killing the synchronous `apt-get` child would have aborted the whole bootstrap script instead of just retrying. Fixed at the source instead: added `/etc/apt/apt.conf.d/99noxos-fast-fail` (`Acquire::http::Timeout "10"`, `Acquire::Retries "5"`) to `bootstrap.sh` before the `apt-get` calls, shipped as launch template v5 — applies to the next fresh launch (including a mid-build spot replacement of the current instance), no risk to the current run.
8. Set up a session-scoped recurring check (`CronCreate`, every 15 min, job `ad4c757b`) that SSHes into the current fleet instance, diffs log/systemd/disk state against the last check, and only escalates (snapshot, live diagnosis, fix, restart) on genuine stalls/errors — routine progress just gets a one-line status. Session-only, dies with this Claude Code session, auto-expires after 7 days regardless.
9. **The cron check actually caught a real failure**, ~1h14m into the same boot: `apt-get install -y awscli git` finally hit enough `503 Service Unavailable` responses from the flaky mirror backend (`52.203.112.74`, item 7 above) on one retry pass that it gave up entirely (`E: Unable to fetch some archives`), and since `bootstrap.sh` runs under `set -euo pipefail`, that killed the whole cloud-init user-data script — confirmed via `cloud-init status --long` (`status: error`, `scripts_user` module failed). Nothing else had run yet (no volume attach, no mount, no units), so no build progress was lost — the effective cost was ~1h14m of wasted apt-fetch time, all from the same underlying flaky-mirror issue already diagnosed and fixed for *future* launches in item 7's v5 template. This instance had already launched on the older, slower-timeout bootstrap before v5 existed, so it still hit the full brunt of it. Took a defensive snapshot (`snap-0c38cb37aa0e3582e`, completed) before touching anything even though the volume wasn't attached yet — cheap insurance per the standing check protocol. Backed up the dead attempt's log to `/var/log/noxos-bootstrap-attempt1.log` on the box, copied the already-fixed local `bootstrap.sh` (the v5 content, apt fast-fail config included) over via `scp`, and relaunched it as a detached `systemd-run --unit=noxos-bootstrap-retry --collect` unit so it survives the SSH session and isn't tied to cloud-init (which had already permanently marked itself failed and won't retry on its own). No template/fleet changes needed — this was a one-off manual recovery of an instance that predates the v5 fix; the fleet's *next* launch (or a spot replacement of this one) already gets the fast-fail config for free.
10. **User correctly identified a structural problem**: every restart re-pays the entire ~30-60min apt/JDK/AOSP-deps install from scratch (confirmed twice in a row — item 9's failure, then a genuine spot interruption at 15:52 that replaced `i-010a424de5a26096a` with `i-026e46723ef66492e` mid-setup, again before any sync/build work began). If interruptions land during that window often enough, real build progress never happens. Fix: bake a custom AMI with the OS + all deps pre-installed, so future boots (including spot replacements) skip straight to volume-attach/sync/build. Checked AWS's real `get-spot-placement-scores` data first before considering a cross-region move (user's other suggestion) — all 5 candidate regions (us-east-1/us-east-2/us-west-2/eu-west-1/ap-south-1) scored identically (9/10) for this instance-type pool at this target capacity, so a region move was correctly ruled out as effort with no expected benefit; the AMI fix addresses the actual bottleneck regardless of region.
11. **AMI baking, done in two passes at the user's request** (they didn't want to risk losing ~40min of in-progress downloads waiting for a "clean" stopping point): baked `ami-00d0975a482292b8d` as an insurance checkpoint mid-`apt-get install` (wired into launch template v6), then let setup continue.
12. **That continuation hit a fourth real bug**: immediately after `setup-instance.sh` finished (JDK 17 + AOSP deps + `repo` tool all successfully installed — the expensive part), the very next line, `git config --global --add safe.directory '*'`, died with `fatal: $HOME not set` — cloud-init's script-execution context (unlike a normal login/sudo shell, confirmed via `sudo bash -c 'echo $HOME'` which *does* show `/root`) genuinely never sets `$HOME`, so this line has silently been latent in every version of `bootstrap.sh` since the start; it was never observed before because every previous run needed manual recovery *before* reaching this line anyway (device-detection bug, apt failures, etc.), so this was the first time a run ever got this far unmodified. Under `set -e` this killed the whole cloud-init script again, right at the finish line, and would have thrown away the just-completed installs on the next interruption/restart. Fixed the root cause (`export HOME=/root` added right after `set -euo pipefail` at the top of `bootstrap.sh`) and recovered the live instance surgically rather than restarting: manually ran `git config --global --add safe.directory '*'` with `HOME` set, reconstructed `watch-interrupt.sh`/`resume-build.sh` from the (now-fixed) local template with this instance's real volume ID (`vol-0ef4cf53dbd7a1489`) and instance ID substituted in, and launched both as `systemd-run --setenv=HOME=/root` units as before — `noxos-watcher`/`noxos-build` both came up `active`, `repo sync` started. Zero lost work.
13. Baked a second, superseding AMI (`ami-0aa111eb1f0bcbc94`) from this now-fully-set-up, HOME-fixed instance and shipped it as launch template **v7** (`ImageId` + the `HOME`-fixed `bootstrap.sh` together) — this is the real "golden" base image going forward. `ami-00d0975a482292b8d` (the mid-setup insurance one from item 11) is superseded but left in place, negligible storage cost. Every future launch (fresh fleet or spot replacement) should now go from cold boot to `repo sync` starting in well under a minute, instead of 30-90+ minutes. Tagged it `Name=noxos-compile-image` at the user's request (note: EC2 doesn't allow renaming an AMI's actual `Name` attribute after creation, only tagging — the underlying `Name` stays `noxos-rom-compile-base-20260904-2325-clean`, but the `Name` tag is what shows up when browsing/searching the console).
14. Cron interval changed from 15 min to hourly (fires at :18 past) per user request — true "every 45 min" isn't expressible in plain 5-field cron since 45 doesn't divide 60, so went with the user's stated fallback of "or like 1hr" instead of a lopsided 45/15 alternating gap.
15. **5th and 6th real bugs, both found by the very next spot interruption** (fleet replaced `i-026e46723ef66492e` with `i-093265a615e7d098f` at 18:18 UTC): (a) the "clean" AMI from item 13 was baked *while the AOSP data volume was still attached at `/dev/sdf`*, so `create-image` captured that block device mapping too — every future launch now auto-attaches a stray volume (restored from a snapshot frozen at imaging time) at `/dev/sdf`, colliding with `bootstrap.sh`'s own explicit `attach-volume` call to the same device (`InvalidParameterValue: Attachment point /dev/sdf is already in use`), killing the boot under `set -e` before the watcher/build units could launch. Fixed by adding an explicit `{"DeviceName":"/dev/sdf","NoDevice":""}` override to the launch template (v8) so the AMI's baked-in mapping is always suppressed, regardless of what state the source instance was in when imaged. Recovered live: found the orphaned volume `bootstrap.sh` had already correctly created from the latest snapshot but couldn't attach (`vol-03cb46693f241866b`), detached the stray AMI-sourced one and deleted it, attached the correct one, confirmed the AMI had correctly baked in `/root/noxos-os`, the `HOME`-fixed git config, and the `repo` tool (no need to redo setup), and launched the watcher/build units by hand with this instance's real IDs.
16. (b) **The previous build attempt silently "succeeded" with no real work done**: the snapshot taken right before `i-026e46723ef66492e` was interrupted was labeled `noxos-aosp-src build-complete snapshot` (`snap-0eb0b669eb05c621c`) — meaning `resume-build.sh` actually reached its post-`build.sh` completion block — but inspecting that snapshot's `/mnt/aosp/out/` showed only `lunch`'s `dumpvars-*` config-detection files (~2 minutes of activity), no `build.ninja`, no `out/dist/`, nothing resembling a real `m dist` compile. Root cause unrecoverable this session: `build.sh`'s actual terminal output only ever lived in `/var/log/noxos-build.log` on that instance's root volume, which is gone forever now that the instance is `terminated`. **This is exactly the gap the user then asked to close**: added a `push_logs`/`trap ... EXIT` to `bootstrap.sh` (covers any setup-phase failure) and to the generated `resume-build.sh`/`watch-interrupt.sh` heredocs (covers build failures and the 2-minute interrupt window) that ship `/var/log/noxos-*.log` to `s3://noxos-releases/logs/<instance-id>/<UTC-timestamp>[-interrupt|-build]/` on every exit path. Also moved `bootstrap.sh` into `noxos-os/infra/bootstrap.sh` and committed it (`d764129`) — it had only ever lived in the gitignored `knowledge-graph/`, per the user's request to keep operational scripts in sync with GH. Shipped as launch template **v9**. Backfilled logs for the then-running instance manually since it launched before this fix existed.
17. **7th bug, found by yet another interruption (3rd in ~90 min)**: the pre-baked AMI ships `/root/noxos-os` already cloned, so `bootstrap.sh`'s plain `git clone` failed with "destination path already exists" on every fresh-from-AMI boot, killing the script under `set -e` right before the units launch — same class of bug as item 15(a), same root cause (baking an AMI captures more state than just "packages installed"). Fixed by falling back to `git -C /root/noxos-os pull --ff-only` when the checkout already exists (also means a stale AMI keeps picking up infra-script fixes pushed after it was baked). Shipped as **v10**, committed (`418e862`). Confirmed the log-push trap from item 16 actually works in production: the failed instance's logs landed in S3 automatically (`s3://noxos-releases/logs/i-0e851bbd8a2464065/...`) without any manual intervention.
18. **User correctly pushed back on a misreading of the spot placement score** ("every region has a 9/10 score to get reclaimed?") — clarified: the score is 0-10 *confidence of getting/keeping* capacity, higher is better, not a reclaim-risk score. All regions scoring 9/10 means the pool is already about as safe as it gets today, not that every region is equally bad.
19. **User asked to stick with spot but reduce reclaim risk by instance-type choice.** Checked `get-spot-placement-scores` per individual instance type (not the combined pool) in us-east-1: every single r5/r6/r7-family 2xlarge type scored only 1-3/10 on its own (r6i.2xlarge worst, at 1) — it's the *combined, diversified pool* that scores 9/10, not any individual "safer" type. So the real lever is more diversity, not a better pick. Widened the fleet from 5 instance types x 3 AZs (15 pools) to 9 types x 6 AZs (54 pools) — added `r5b`/`r6in`/`r7i`/`r7a` and 3 more AZs (`us-east-1d/e/f`), all still 2xlarge/64GB so cost stays in the same tier. Committed `infra/fleet-config.json` (`8f3a946`). Recreated the fleet (old `fleet-b42c4be0-...` deleted, new `fleet-972120b9-8273-4e17-a114-3b7f7184fc70`) since `LaunchTemplateConfigs`/`Overrides` can't be changed on a live fleet. Took a defensive snapshot (`snap-0cd1f0c6369b2ae39`) first. Hit two transient issues during the swap, neither fatal: several types aren't supported in the new AZs (`launchSpecUnusable`, fleet just skips those combos) and a brief `MaxSpotInstanceCountExceeded` right after tearing down the old fleet (likely the account's 16 vCPU spot quota not having released yet) — resolved on its own within ~30s once other launch specs were tried. New instance (`i-000d3aa89c2760716`, r5.2xlarge) booted clean on the first try with **zero manual intervention** — no clone collision, no device collision, straight to `repo sync` — confirming all of v10's fixes hold up on a genuinely fresh fleet.
20. **Root cause of the "fake success" bug finally found and fixed** (this is what items 16(b)/17 flagged as unrecoverable — recovered on the 4th occurrence with a hands-on, step-by-step manual reproduction instead of racing S3 log uploads against self-termination). At the user's suggestion, stopped trying to catch the automated `resume-build.sh` unit live (it self-terminates ~2 min after `lunch` starts, too fast to intercept reliably even after disabling `aws` mid-flight got blocked by the classifier) and instead: killed the auto-launched `noxos-build` unit immediately on a fresh instance, then ran `sync.sh`/`lunch`/`build.sh`'s exact commands by hand over SSH, one at a time, watching real output directly. Found the actual error on the first manual `lunch` attempt: `build/make/core/release_config.mk:88: error: fopen failed` / `Error getting 'pre product config' release config: exit status 1` — **and `lunch` still returns exit 0**, treating this as non-fatal. Ruled out red herrings via direct testing before concluding: not a file-descriptor limit (ulimit -n 1024 vs raising it to 524288 made no difference), not disk/inode exhaustion (16% inodes used, 60% disk used), not a race between concurrent manual attempts (a single, fully isolated clean run reproduced it identically). Added a `$(warning ...)` debug line directly into `release_config.mk` to dump the actual path Make was trying to write (`out/soong/release-config/maps_list-aosp_cf_x86_64_phone.txt`) and confirmed `mkdir -p` on that exact directory succeeds (`rc=0`) immediately beforehand — yet the very next line's `$(file >...)` write still fails. The real cause: **the entire pipeline has been running `sync.sh`/`build.sh` as root** (bootstrap.sh/resume-build.sh run under cloud-init/systemd-run, both root by default), but `/mnt/aosp` has been owned by `ubuntu:ubuntu` since the very first manual sync back in session 7 — and AOSP's build tooling explicitly warns about this every single run (`Process will be UID/EUID=0 ... will have user root-level access to files`) but doesn't hard-fail on it; instead something in the release-config generation path silently breaks under root. Confirmed directly: re-ran the identical `lunch` command as the non-root `ubuntu` user and it succeeded completely clean, printing full config (`BUILD_ID=CP2A.260605.016`, `TARGET_PRODUCT=aosp_cf_x86_64_phone`, etc.) with zero errors. This explains all three prior "instant success" builds — `lunch` silently degraded, `m dist` had nothing valid to build against, returned 0 almost instantly, and `resume-build.sh` (also under `set -e`, also fooled by the 0 exit code) proceeded straight to snapshot+terminate. **Fixed**: `resume-build.sh` now does a one-time `chown -R ubuntu:ubuntu /mnt/aosp` (cheap insurance against a snapshot ever picking up root-owned files, e.g. from this exact debugging session) and runs `sync.sh && build.sh` via `sudo -u ubuntu -H bash -c "..."` instead of directly as root — also captures `build.sh`'s real exit code explicitly (`set +e` around it) instead of trusting `set -e` to catch a failure it was structurally blind to, dumps `out/dist`/`out/` listings into the log either way, and now pushes logs to S3 *before* any fleet/termination calls (not relying on the EXIT trap, which doesn't get enough time to complete before a self-initiated `terminate-instances` tears down the network) — and leaves the instance up for inspection instead of terminating if `build.sh` ever does fail for real. Shipped as launch template **v11**, committed (`b97fcf7`), and manually applied to relaunch the current instance's build correctly. This was the actual blocker preventing any real compile progress all session — everything before this was infrastructure hardening around a build that could never have succeeded.
21. **Two more small fixes needed after the ubuntu-user switch**: `/root/noxos-os` isn't reachable by a non-root user at all (`/root` is `700` by default) — `sync.sh`/`build.sh` failed instantly with `Permission denied`. Relocated the checkout to `/opt/noxos-os` (universally readable), shipped as **v12** (`e759ac3`). Then hit a second, subtler one: `/etc/.repo_gitconfig.json` (a system-wide cache `repo` writes for a git-superproject check) had been created as root during earlier testing and was baked into the AMI, so `ubuntu` got `PermissionError` trying to rewrite it. Fixed by unconditionally deleting it before each run (it's just a cache, regenerates fine) — shipped as **v13** (`065b56d`).
22. **The build is finally doing real work.** After all of the above, `lunch` succeeded completely cleanly for the first time all session (`BUILD_ID=CP2A.260605.016`, full config, zero errors) and `m dist` moved into genuine compilation — Soong's own Go tooling built first (292 steps), then `soong_build` ran at 667% CPU / 14GB RAM generating the real ninja graph from `Android.bp`, followed by actual `siso`/ninja compilation. This is the first genuine compile progress in the entire session — everything before this point (fleet, AMI, snapshots, GH Actions, all the bug fixes) was infrastructure that could never have produced a real build until the root-cause fix landed.
23. **User asked about pre-warming the EBS volume** (small t3 instance does the snapshot lazy-load warm-up, hands off to the big compute instance) to cut out the ~1hr "Finalizing sync state" I/O-bound stall seen this run. Flagged the real constraint: EBS volumes are AZ-locked, so a volume pre-warmed in one AZ can't attach to the fleet if it lands in a different one of the 6 diversified AZs — would mean giving up the diversification that lowers interruption risk. Proposed the AWS-native alternative (Fast Snapshot Restore on checkpoint snapshots) as simpler than custom Lambda/t3 orchestration, but user chose to leave it as-is for now given interruptions should be rarer with the wider pool anyway. Not implemented - noted here in case it's revisited later.
24. **Created a `context` branch on the root `noxos` meta-repo** (not `noxos-os`) at the user's request, force-added the entire local `knowledge-graph/` (normally gitignored on `main`) and pushed it there for backup/sharing. **Real gotcha hit twice**: switching branches with `git checkout <branch>` resyncs every tracked path to that branch's committed state — since `knowledge-graph/` is tracked on `context` but not on `main`, checking out `main` deletes it from disk entirely (recovered via `git checkout context -- knowledge-graph/ && git reset HEAD knowledge-graph/`), and checking out `context` *from* `main` overwrites any newer local edits with whatever was last committed to `context` (this cost one round of freshly-written `TASKS.md` entries, since they were written after the first `context` commit and before a second `checkout context` clobbered them — recovered by just rewriting them, since they were fresh in-session). **Lesson for future sessions**: never `git checkout context` (or back to `main`) as a casual branch switch on this repo while `knowledge-graph/` has any uncommitted-on-`context` edits — either commit-and-push the `knowledge-graph/` update *before* switching away from the branch that has the current edits, or (better, not yet done) set up a separate `git worktree` for the `context` branch so the two branches never share a working directory and can't clobber each other.
25. **First real spot interruption during actual active compilation, and a newly discovered gap.** `i-088c7bda3768e7e24` got genuinely reclaimed by AWS (`StateTransitionReason: Service initiated`, not our own script) at 23:49:14 UTC, after reaching **10% (26,782/255,297 ninja steps)** of real compilation — confirmed via the interrupt-watcher's S3 log push (`.../20260904T234718Z-interrupt/noxos-build.log`, 3.7MB, full ninja progress history). The watcher correctly fired and kicked off `create-snapshot` (`snap-0b926ba68f566b11a`) at 23:47:21, ~2 min before termination, exactly as designed. **The gap**: that snapshot was still `pending` (still copying — a real multi-GB delta from ~66 minutes of compile output takes real time to finish) when the fleet's replacement instance (`i-0fd029bad060b7076`) booted moments later. `bootstrap.sh`'s snapshot lookup correctly filters on `status=completed` (as it should — restoring from an in-progress snapshot would be unsafe/inconsistent), so it fell back to the next-latest *completed* snapshot instead, which predates the real compile entirely (`/mnt/aosp/out` on the new instance is 144MB, not the 60GB+ that 10% progress produced). **Net effect: ~66 minutes of real compile progress was lost**, not because anything malfunctioned, but because the interrupt-to-replacement cycle (~2-3 min) can be faster than a large snapshot's actual completion time once the build is producing substantial output. This is a real, not-yet-fixed architecture gap worth addressing in a future session (e.g., some way to have the replacement wait for or prefer a still-completing-but-newer snapshot once it finishes, or extend how long the fleet takes before replacing). Not fixed live tonight per user's own "don't attempt open-ended novel debugging at this hour, just capture the evidence" instruction — the replacement instance bootstrapped cleanly with zero manual intervention otherwise (units active within 3 minutes of boot, matching v13's design), so recovery itself worked correctly, just from an older restore point.
26. **v0 achieved — the actual finish line.** The recovered build (from item 25) ran all the way to **99.997% (250,880/250,887 ninja steps)** before failing on the very last target: OTA package generation ran out of disk (`zip2zip: no space left on device` writing a temp file during payload signing), *after* every real v0 deliverable had already been produced and zipped — system image, `target_files`, symbols, proguard-dict, all present in `out/dist/`. Tried to push through: grew the volume live once already (400→450GB, `resize2fs`, zero downtime — confirmed working), but AWS blocks a second `modify-volume` while the first is still `optimizing` (a real, unavoidable cooldown, climbed ~3%/8min so a full wait would've been 1-2h); tried freeing space by uploading the already-built `symbols.zip`/`proguard-dict.zip` to S3 and deleting the local copies, but ninja just treats them as missing outputs and regenerates them, eating the freed space right back — that approach doesn't work for anything ninja tracks as an output, only for genuinely orphaned scratch files. **Decision (user's call, presented as a real tradeoff via AskUserQuestion): skip OTA entirely.** It's a side effect of this device's `m dist` rule, not something explicitly requested, and full OTA generation was already a separate, deliberately-paused concern (`generate-ota.sh`, waiting on its own future architecture session) — reclassified as v1 scope. Took the v0-complete snapshot manually (`snap-0f8e85ea66f313465`, description "v0 build-complete... OTA package skipped") with all real artifacts present. Fixed the actual root cause for next time: `bootstrap.sh` now always provisions volumes at **500GB** (`--size 500` on `create-volume`, regardless of the source snapshot's size) with a `resize2fs` right after mount to grow the filesystem into the full capacity — shipped as **v14** (`5ea7a01`, later rewritten to `cebac40`/signed — see below). Cleaned up the interim insurance-backup zips from `s3://noxos-releases/cuttlefish-local/v0-backup/` is *not* done (left there, harmless, small).
27. **Git commit hygiene fixed.** The 8 commits made this session (`ad37ef9`..`5ea7a01`, GH Actions through the 500GB fix) had accidentally picked up a `Co-Authored-By: Claude Sonnet 5` + `Claude-Session:` trailer from a stale system instruction, breaking from this repo's actual established convention (plain `type: message`, confirmed against every earlier commit back to project start — no trailer, ever). Rewrote all 8 with `git filter-branch --msg-filter` (strips the trailer, keeps everything else identical), then `git rebase 6b69e39 --exec 'git commit --amend --no-edit -S'` to GPG-sign each one with the user's existing key (already trusted in the local keyring, no passphrase prompt needed — agent had it cached), force-pushed the corrected history to `origin/main`. Set `commit.gpgsign true` globally so every future commit signs automatically without needing `-S`. New commit hashes: `2b14fd0`, `9d5cc53`, `1cd77e3`, `95fc393`, `1a30432`, `c9721a7`, `dec4f2d`, `ae4d16b` (was `5ea7a01`, now signed).
28. **Tonight's teardown (session 8 closing state, 2026-09-05 late)**: user wants to download the v0 artifacts tomorrow and actually boot/verify them in the local Cuttlefish container before deciding the snapshots are done with — so nothing snapshot-related gets touched tonight. What *did* happen tonight: fleet `fleet-972120b9-8273-4e17-a114-3b7f7184fc70` scaled to target-capacity 0 (`--excess-capacity-termination-policy no-termination`, no automatic termination — done separately), instance `i-0fd029bad060b7076` explicitly terminated (which also deletes its volume, `DeleteOnTermination=true` was set at attach time), so **no compute or storage is billing overnight** except snapshot storage. Live snapshot state as of teardown: `snap-0b048190ff832d2a9` (sync-complete, permanent), `snap-05ae7c62011272e71` (80% checkpoint, still needed as a fallback until v0 is verified), `snap-0f8e85ea66f313465` (v0-complete, still `pending`/copying in the background — that's fine, the point-in-time capture already happened, only the background copy-to-durable-storage is still running and will finish on its own regardless of the fleet/instance being gone). **Plan for next session**: download/verify the v0 build in the local Cuttlefish container; once confirmed good, delete the v0 snapshot's now-unneeded fallbacks (the 80% checkpoint, and reconsider whether the v0 snapshot itself is still needed once verified locally or should stay as the AWS-side archival copy); then decide on the next actual work item (v1 planning: proper OTA architecture, cross-repo integration, etc.).

29. **Session 9 (2026-09-06): v0 verified downloadable, v1's first real attempt found four real bugs, none of them fixed cheaply — this is the session to internalize before starting v1 compute again.** In order:
    - Deleted the now-superseded 80% checkpoint snapshot (`snap-05ae7c62011272e71`) once the v0-complete snapshot was confirmed `completed`/100%.
    - Scaled the fleet back to target-capacity 1 to start the v1 build (`LUNCH_TARGET=noxos_cf_x86_64_phone-userdebug` baked into `bootstrap.sh` v15) — **the fleet was set to `Version: "$Latest"` on its launch template, but the instance that actually launched booted on v14, not v15**, despite v15 existing (verified correct) before the fleet was scaled up. Root cause not fully pinned down (likely an eventual-consistency race in EC2 Fleet's `$Latest` resolution) — worked around by explicitly pinning the fleet's `LaunchTemplateConfigs` to a literal version number (`aws ec2 modify-fleet --launch-template-configs 'LaunchTemplateSpecification={LaunchTemplateId=...,Version=N}'`) every time the script changes, never trusting `$Latest`/`$Default` again for this fleet. That `modify-fleet` call only works while the fleet is `active`/`fulfilled` at its current capacity - it returned `FleetNotInModifiableState` once and just needed a retry after the capacity settled.
    - That misdirected instance spent **~10.5 hours re-building stock v0** instead of v1 - but this time it actually succeeded end-to-end, OTA included, on the 500GB volume (no disk-space failure like item 26) - a genuine bonus artifact, snapshotted as `snap-09e66ccb4baff4efb` ("v0 rebuild-complete, working OTA, 500GB") and its `cvd-host_package.tar.gz`/`aosp_cf_x86_64_phone-img-ubuntu.zip` copied into `s3://noxos-releases/full/20260906-aosp_cf_x86_64_phone/` for download (verified byte-exact against S3 with `stat`/`head-object` after an aria2c 16-way-parallel download corrupted the img zip by 196 bytes on the first attempt - re-downloaded single-stream-safe and it matched exactly). Deleted the now-obsolete `cuttlefish-local/v0-backup/` partial zips (~5.6GB, symbols/proguard-dict only, superseded by the complete artifacts).
    - **That instance's own `resume-build.sh` never got to snapshot/terminate itself** - it hit the exact `aws ec2 modify-fleet --target-capacity 0 --no-terminate-instances` syntax bug (fixed in v15's *next* boot, but this instance was already running v14's baked-in copy) and died with `set -e` right after uploading, leaving the instance running unattended. Caught via a direct check, not automation - **lesson: BUILD_EXIT-gated steps that run unconditionally after the actual work (fleet scale-down, snapshot, terminate) need to be individually fault-tolerant (`|| true` or explicit error handling), not just wrapped in one `set -e` block, because a failure in step N stops N+1 and N+2 from ever running even though they're unrelated and both still needed.**
    - Relaunched pinned to v15 (correct script confirmed via `describe-instance-attribute --attribute userData`) - hit a **second bug**: `lunch noxos_cf_x86_64_phone-userdebug` → `Invalid lunch combo` - AOSP's current `lunch` requires the 3-part `<product>-<release>-<variant>` format, matching what `build.sh`'s own default already used. Fixed to `noxos_cf_x86_64_phone-trunk_staging-userdebug` in `bootstrap.sh` and in `AndroidProducts.mk`'s `COMMON_LUNCH_CHOICES`. This time the failure-handling worked exactly as designed - `BUILD_EXIT` captured, instance left up for inspection, nothing silently lost.
    - Since `chown`+`repo sync` were already done on that instance, re-ran the build directly via SSH + a hand-rolled `systemd-run` unit (self-sufficient, survives SSH/session ending) instead of re-provisioning from scratch - hit a **third bug**: `device/google/cuttlefish/vsoc_x86_64_phone.mk` no longer exists - `android-latest-release` restructured the cuttlefish device tree into per-arch subdirectories at some point after the v1 overlay was written back on 2026-08-31. Real path confirmed against the synced tree's own `AndroidProducts.mk` (not guessed): `device/google/cuttlefish/vsoc_x86_64/phone/aosp_cf.mk`. Fixed in `noxos_cf_x86_64_phone.mk`.
    - Retried, hit a **fourth bug**: kati failed on `PRODUCT_ARTIFACT_PATH_REQUIREMENT` - `system/etc/init/noxos.rc` and `system/media/bootanimation.zip` (our only two `PRODUCT_COPY_FILES`) are "outside" the GSI artifact path requirement inherited from `generic_system.mk`. Stock `aosp_cf_x86_64_phone` never hits this since it adds no custom copy-files of its own - it only surfaces once a device tree installs anything. Fixed by adding both paths to `PRODUCT_ARTIFACT_PATH_REQUIREMENT_ALLOWED_LIST`.
    - User called time on the live-fix-and-retry loop at this point (**"today's time was wasted doing useless work"**) - fair, four bugs in a row each costing a build restart is exactly the kind of thing that should have been front-loaded into one audit pass instead of discovered live, one at a time, on paid compute. Terminated the instance (had been up since 10:03, most of it either mid-build or sitting idle after failures) and fleet scaled back to 0.
    - **Did the audit that should have happened first, for free, against `aosp-mirror/platform_frameworks_base`'s GitHub mirror** (no AWS spend needed for this kind of check - a real lesson for next time: verify inherited/overlaid resource names and paths against a public source mirror *before* burning compute on a live build, not after): found a **fifth, real bug** that hadn't surfaced yet because it doesn't fail the build, it just silently does nothing - `overlay/frameworks/base/core/res/res/values/colors.xml` overrides `accent_device_default_light`/`accent_device_default_dark`, both removed from AOSP in favor of the Material You 13-step tonal palette (`system_accent1_0`..`system_accent1_1000`). Replaced with a real computed teal ramp (constant HSL hue=174°/sat=59%, varying lightness per AOSP's own tonal steps - a manual approximation, not Google's exact HCT algorithm, documented as such in the file) for `system_accent1_*` only; accent2/accent3 left inherited rather than guessed.
    - Also fixed two efficiency issues found while re-reading `bootstrap.sh` end to end: the unconditional `chown -R ubuntu:ubuntu /mnt/aosp` ran on *every* boot regardless of whether the volume was already ubuntu-owned (true for any volume restored from a snapshot taken after a successful ubuntu-run build) - now skipped via a `stat -c '%U'` check first, since walking a 400+GB tree just to no-op every file's ownership was real, avoidable time. And the orphaned-`out/dist`-export step (added in v15 to rescue v0's un-uploaded artifacts) hardcoded the S3 label `aosp_cf_x86_64_phone` - now derives it from the actual filename in `out/dist` so it won't mislabel a future build as v0.
    - **GPG signing intermittently timed out mid-session** (`pinentry-gtk` launched but never got a response, `gpg: signing failed: Timeout`) - user said to stop retrying and just commit unsigned for now (`git commit --no-gpg-sign`), will sign later themselves. Commits `9b6f68f`, `c743284`, `a7a28a4` are unsigned; `8f197c9` (the final audit-fixes commit) is also unsigned. **These need a manual `git rebase --exec 'git commit --amend --no-edit -S'` pass once GPG/pinentry is working again**, matching the pattern used in item 27.
    - **Estimated spend for the whole session**: ~18 `r5.2xlarge` compute-hours across two instances (23:18 09-05 → 10:00 09-06, then 10:03 → ~17:20 09-06) at spot rates (~$0.15-0.20/hr per the pricing this project already established) - roughly **$3-4 in compute**, plus low-single-digit dollars for EBS/S3/transfer. Cost Explorer's 24-48h lag means this can't be confirmed exactly yet, but nothing points to a runaway bill.
    - **Live AWS state at end of session**: fleet at target-capacity 0, no running instances. Snapshots: `snap-0b048190ff832d2a9` (sync-complete, permanent), `snap-0f8e85ea66f313465` (v0-complete, OTA-skipped, kept for now), `snap-09e66ccb4baff4efb` (v0 rebuild-complete, working OTA, 500GB - the best available resume point for the next v1 attempt). v0 build fully downloaded and byte-verified at `~/Downloads/noxos-v0/` on the user's laptop.
    - **Explicit next-session plan**: do NOT relaunch a real compile until every fix above has been double-checked in one pass (the five bugs found tonight, plus a look for anything else of the same shape - stale AOSP paths/resource names, GSI artifact-path conflicts elsewhere in the overlay). Once satisfied, relaunch pinned to whatever the next launch template version actually is (never trust `$Latest`), restoring from `snap-09e66ccb4baff4efb`.

30. **Session (2026-09-10/11): v1 finally built successfully — real, verified artifacts exist.** In order:
    - v0 verified for real in local Cuttlefish (docker was finally up): extracted `cvd-host_package.tar.gz` + the img zip into `host_path`/`product_path` dirs, `cvd create` booted it, confirmed via adb (`sys.boot_completed=1`, correct fingerprint `aosp_cf_x86_64_phone/vsoc_x86_64:Baklava/.../userdebug/test-keys`). One non-fatal `liblp` warning about `super.img` geometry, didn't block boot.
    - Found the launch template's `chown`-skip fix (from session 9's audit) had been committed to git but **never actually baked into a live launch template version** - v16 (the one the fleet was still pinned to) predates it. Created v17 with the current script, verified byte-identical, re-pinned the fleet to it explicitly (not `$Latest` - that bug from session 9 is why explicit version pinning is now standard practice here).
    - First real launch on v17 hit a genuine, no-fault race: `modify-fleet --launch-template-configs` and `modify-fleet --target-capacity-specification` both intermittently need a retry with `FleetNotInModifiableState` errors right after a prior modify - not a bug, just needs a short wait+retry, now expected behavior rather than a surprise.
    - Real compilation progress tracked hourly (later 10-minutely) via a cron that SSHes in and reads `/var/log/noxos-build.log` directly (previous sessions only checked S3/fleet state externally, which couldn't show real ninja step percentages) - this is now the standard way to watch these builds, not just polling S3.
    - **First attempt crashed at 96% (51308/52978 steps) on genuine, confirmed `No space left on device`** - the disk emptied faster than the hourly disk-watchdog cron could catch (30GB free → 0 in under an hour, during the final img/OTA packaging burst) and `siso`/ninja's own logging crashed trying to write its own error log, triggering a cascade of "action cancelled when ninja exited" across every other in-flight target - those cascade failures are cosmetic noise, not independent bugs; the one real root cause was the disk. **Recovered, not restarted from scratch**: grew the volume 500GB→620GB (deliberately more than the standing "+50GB" rule since 50 had already proven insufficient - flagged this deviation explicitly rather than silently overriding the standing instruction), `resize2fs`'d live, re-ran `build.sh` via a fresh self-sufficient `systemd-run` unit (same survive-the-session pattern as session 9's manual recovery). Ninja correctly recognized almost everything was already done - the retry only needed **1810 steps**, not 53,000 - confirming build-artifact persistence across a crash works exactly as designed when the volume itself survives.
    - Retry hit a scare: ninja's own "may be stuck" watchdog fired every 5 minutes for ~50 minutes straight on a single step, `gen_sbom` (SBOM generation). Diagnosed live rather than assumed: checked the process's actual CPU-time accumulation (climbing slowly but genuinely, not zero) and confirmed via `ps`/soong.log it was doing real (if slow) work, not deadlocked - likely slow because this `out/` tree now holds shared Soong metadata from two different product builds (the earlier `aosp_cf_x86_64_phone` rebuild plus this one) rather than a clean single-product tree. **Deliberately did not kill it** despite the temptation - force-killing a near-complete ninja run was exactly what caused the disk-space incident's cascade, and all the real deliverables (`cvd-host_package.tar.gz`, the img zip) were already confirmed present on disk before `gen_sbom` even finished, so there was nothing left actually at risk. It finished on its own: `ninja: 52m07.41s Build Succeeded: 1810 steps`.
    - **Result, confirmed for real**: `snap-0fbe2b8339b3d92f2` ("v1 (noxos_cf_x86_64_phone) build-complete"), and `s3://noxos-releases/cuttlefish-local/20260910-noxos_cf_x86_64_phone-trunk_staging-userdebug/` holds the real `cvd-host_package.tar.gz` (897MB) and **`noxos_cf_x86_64_phone-img-ubuntu.zip`** (1.57GB) - the first time this artifact has ever existed. Not yet downloaded/boot-tested locally at time of writing - see 2026-09-12 (below / cross-repo pointer) for that.
    - Started drafting the live OTA-signing Lambda (`noxos-server/lambda/ota_handler.py` + tests) per the user's spec — see [`../noxos-server/TASKS.md`](../noxos-server/TASKS.md) for that piece.
    - **Explicit next-session plan (user's own framing): v2 bakes the full app in system + possible v1 fixes.** Also open from this session: decide whether to deploy the drafted OTA Lambda now that a real v1 build exists to test it against, and the two efficiency-question marks noted mid-session (the `PRODUCT_PACKAGES` filter-out list's `BasicDreams`/`PrintSpooler`/`LiveWallpapersPicker`/`EasterEgg` names couldn't be confirmed against the current tree via GitHub mirror lookups - harmless if wrong since `filter-out` on nonexistent names is a silent no-op, but still unconfirmed).

**2026-09-12: v1 confirmed booted and running Warden.** Booted as a second Cuttlefish instance (`cvd-2`, `adb -s 127.0.0.1:6521`) alongside the original v0 instance, confirmed via `ro.product.name=noxos_cf_x86_64_phone`. This closes out item 30 above's "not yet downloaded/boot-tested locally" gap. The actual testing (installing Warden, verifying it runs identically to v0) is `noxos-app`-side work — see [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md) session 11.

**2026-09-12/13: thorough v1 lightweight-mod verification (fresh laptop download, byte-verified), one real bug confirmed, one real gap found and fixed at the source.** Deleted the old v0 local download (`~/Downloads/noxos-v0/`, 2.2GB) per user's request before pulling v1 fresh — `aws s3 cp` (direct HTTPS 403'd, `cuttlefish-local/` isn't public) to `~/Downloads/noxos-v1/`, both files SHA256-matched against S3. Booted as a fresh third Cuttlefish instance (`cvd-2` again, prior one gone with the container restart) at `/root/v1/{host,product}`, same layout convention as v0. Checked every claimed v1 modification individually rather than assuming they all worked together just because the build succeeded:
- **Working, confirmed**: device identity (`ro.product.brand=noxos`, correct fingerprint), `noxos.rc` init overlay (present *and* actually executed — `dmesg` shows the kmsg line firing at boot), single-locale trim (`ro.product.locale=en-US`), 3 of 4 `PRODUCT_PACKAGES` filter-outs (`BasicDreams`/`LiveWallpapersPicker`/`EasterEgg` all absent), dexpreopt `speed` filter (confirmed via `dumpsys package dexopt` on `Settings.apk`: `[status=speed] [reason=prebuilt]`), and the custom boot animation — pulled the on-device file back off and it's byte-for-byte (SHA256) identical to the repo's `branding/bootanimation.zip`, extracted frames confirm it's the real NoxOS hexagon logo animating (fade-in), not the stock AOSP spinner.
- **Bug #1, confirmed real, root-caused (mechanism, not exact culprit)**: `PrintSpooler` is still installed (`/system/app/PrintSpooler/PrintSpooler.apk` present) despite being in the device overlay's `filter-out` list — this is the item 30/72 "unconfirmed" flag. Traced properly against real source rather than guessed: `platform_build`'s only `PRODUCT_PACKAGES += PrintSpooler` site is `target/product/handheld_system.mk`, in the exact same block as `BasicDreams`/`EasterEgg` (both of which our `filter-out` *does* correctly strip, confirmed on-device) — so the filter mechanism itself is sound. Confirmed via AOSP community discussion (Google Groups) this is a known, well-documented class of bug: `filter-out` only stops a package from being requested directly in `PRODUCT_PACKAGES`; if some other still-installed package's own `Android.bp` lists it under `required:` (Soong's `LOCAL_REQUIRED_MODULES` equivalent), the module gets pulled back in regardless. Checked the two most likely specific culprits directly (PrintSpooler's own `Android.bp`, and `frameworks/base/services/print`'s system-server module) — neither shows an obvious `required:` edge, so the *exact* dependent module isn't pinned yet. Deliberately stopped guessing further through AOSP source rather than burn more time with no guaranteed payoff (tried a full `platform_frameworks_base` clone to grep broadly — it's a genuinely huge repo, killed it as not worth the wait for what's ultimately a minor bloat item). **Real fix path for next session**: `out/soong/module-info.json`'s `required_by` field on a real build gives a definitive, zero-ambiguity answer in seconds — check that first before any more static-source guessing. No standard AOSP `PRODUCT_PACKAGES_REMOVE` exists to force this regardless of `required:` edges; a non-standard build-system patch for one bloat package isn't worth adopting. Open.
- **Bug #2, confirmed real and fixed**: the teal accent-color overlay (`overlay/frameworks/base/core/res/res/values/colors.xml`, from session 9) doesn't actually show up anywhere on-device — checked visually (Settings, Wi-Fi toggle) and numerically (sampled the toggle's actual pixel RGB: `(78,94,139)` → HSL hue ≈224°, stock blue — nowhere near our teal's 174°). Root cause, confirmed against the real AOSP source (`aosp-mirror/platform_frameworks_base`'s `SystemUI/.../theme/ThemeOverlayController.java`): Android 12+'s Monet engine generates `system_accent1_*` at runtime as a `FabricatedOverlay` derived from the **wallpaper's dominant color** (`WallpaperColors`/`ColorScheme`), which always takes priority over whatever static value ships in `colors.xml` — a plain resource override was structurally the wrong mechanism, not a typo or missing step. The real, standard override point (verified against the same source tree, `core/res/res/drawable-nodpi/default_wallpaper.png` — a `java-symbol` AOSP devices commonly override via `DEVICE_PACKAGE_OVERLAYS`, same mechanism already wired for this device) is the **default wallpaper image**, since Monet derives its palette from whatever wallpaper is active. Added `overlay/frameworks/base/core/res/res/drawable-nodpi/default_wallpaper.png` — a flat `#34CBBC` fill (our brand teal, the exact `system_accent1_500` value already defined in the existing colors.xml ramp, for internal consistency) at 1080x1920, no `.mk` changes needed since `DEVICE_PACKAGE_OVERLAYS` already points at this tree. Left the existing `colors.xml` override in place — not dead code, it's the correct fallback for the (rarer) non-Monet code path (`mIsMonetEnabled == false`) inside the same controller. **Not yet verified on-device** — needs the next real compile to confirm Monet actually extracts teal as intended from a flat-color wallpaper (a legitimate open question: palette-extraction algorithms sometimes want luminance variance to compute confident swatches; a 100%-uniform image is the simplest case but untested here).
- Also fixed, unrelated but caught while answering "do we need to manually set fleet capacity": `noxos-os/infra/fleet-config.json` (the GitHub Actions path, `.github/workflows/build.yml`) still pinned `Version: "$Latest"` on the launch template — the exact eventual-consistency bug from session 9 item 29, never actually fixed in the GH Actions config even though the manually-run fleet learned the lesson. Confirmed harmless *right now* (resolves correctly to v17) but a live landmine for a future run right after a template bump. Pinned to `"17"` per user's explicit instruction — **remember to bump this file every time the launch template changes**, GH Actions won't self-update it.
- Confirmed answering the user's actual question: no, GH Actions does **not** need manual fleet-capacity intervention — `fleet-config.json`'s `TargetCapacitySpecification.TotalTargetCapacity: 1` is baked in and `create-fleet` launches at that capacity directly, separate from (not reusing) the long-lived manually-managed `fleet-972120b9-...` this project has been driving by hand all session.
- **Next session**: fix bug #1 (trace and remove the PrintSpooler re-inclusion path), and when the next real compile happens, confirm the wallpaper-derived teal accent actually renders (this entry's fix is unverified until then). User's framing stands: compile is deferred, not urgent — "let's see when."
- Also signed the two commits from this entry that GPG had previously timed out on (`8f197c9`, `a7a28a4`, from session 9) in the same rebase pass as this session's own two — all four now have good signatures, pushed with `--force-with-lease`.

**2026-09-13: v1 lightweight pass — Wi-Fi-first, no cellular target, confirmed by the user.** Discussed options before touching anything (per user's explicit "discuss then implement" instruction): proposed font trimming, a wider `PRODUCT_PACKAGES` bloat pass, mainline/APEX module review, an ART compiler-filter tradeoff, and asked directly whether cellular was a real target since that gates how aggressively the telephony stack itself could be cut. User's answers: Wi-Fi-first, no cellular, ever (for now) — keep `speed` for the ART filter (no size/speed tradeoff wanted there) — do the rest — and explicitly make sure it doesn't affect Warden. Implemented in one commit (`b1fa370`, `noxos_cf_x86_64_phone.mk`):
- **Telephony stack removed** (`ONS`, `CarrierDefaultApp`, `CallLogBackup`, `com.android.cellbroadcast`, `CellBroadcastLegacyApp`, `CarrierConfig`, `Dialer`, `ImsServiceEntitlement`, `preinstalled-packages-platform-telephony-product.xml`) — real names pulled from `platform_build`'s `target/product/telephony_system.mk`/`telephony_product.mk`/`telephony_system_ext.mk` (all three genuinely inherited via `aosp_cf.mk`), not guessed. Deliberately kept `EmergencyInfo` (also telephony-gated in `telephony_system_ext.mk`) — it's a lockscreen safety feature (medical ID/emergency contacts), not a functional telephony dependency, and cutting user-safety info isn't in the spirit of bloat-trimming regardless of the no-cellular call.
- **Non-Latin font set removed — the actual biggest win.** `PRODUCT_LOCALES=en_US` only trims string resources, not font files; the real font list lives in `external/noto-fonts/fonts.mk` (fetched directly from `android.googlesource.com` since this project isn't mirrored on GitHub) as 191 individually-named `PRODUCT_PACKAGES` entries — genuinely filterable via the same mechanism already proven to work. Kept only 8: `NotoColorEmoji(Flags).ttf` (emoji, wanted regardless of locale), `NotoSansSymbols-Regular-Subsetted(2).ttf` (general symbol/dingbat fallback, small, not script-specific), and the four Latin `NotoSerif-*.ttf` faces. Filtered the other 183 — every CJK, Arabic, Devanagari, Hebrew, Thai, and dozens of historic-script font file AOSP ships by default. Real, meaningful size win (Noto's non-Latin coverage commonly runs 150-250MB) — the single biggest lever in this whole pass, and one the earlier `en_US` locale restriction never actually touched.
- **Remaining small bloat**: `PacProcessor`, `MusicFX`, `DeviceAsWebcam`, `BluetoothMidiService` — confirmed present in `handheld_system.mk`, unused, small individual wins.
- **Checked against Warden's actual dependencies before touching anything**: none of this touches `VpnService`, AVF/`packages/modules/Virtualization`, or priv-app/signing — all separate inheritance paths, untouched. The one real, disclosed tradeoff: Warden's traffic/file audit logging could encounter non-Latin text (a foreign-domain URL, a non-Latin filename from a download) that now renders as tofu boxes instead of native glyphs — doesn't break Warden's function, just a display gap, called out explicitly rather than silently accepted.
- **Investigated and correctly ruled out** a proposed "mainline APEX trimming" angle (specifically `com.google.android.adservices`) — checked `updatable_apex.mk` and `generic_system.mk` directly, confirmed it's not part of vanilla AOSP's inherited chain at all (it's a Google-signed/GMS-adjacent module, and this project has no GMS to begin with) — not a missed opportunity, just an inapplicable candidate correctly identified as such before wasting a filter-out entry on it.
- Also signed this session's own commit cleanly (GPG cooperated this time, no `--no-gpg-sign` needed).
- **Not yet verified on-device** — same caveat as the accent-color fix: needs the next real compile to confirm nothing critical broke (a missing font referenced by `fonts.xml`'s fallback chain is a known low-risk pattern other ROMs use successfully, but not verified in this exact AOSP version yet; telephony removal could theoretically surface a hard-required dependency somewhere not yet traced, though nothing found suggests it will).

**2026-09-13: GitHub Actions CI finally actually works — root cause was a genuinely unusual GitHub OIDC token shape, not the assumed "IAM was never set up."** Triggered `build.yml` via `gh workflow run` to compile this session's lightweight-strip changes. First run failed immediately at the `configure-aws-credentials` step (`Not authorized to perform sts:AssumeRoleWithWebIdentity`) — every previous session's notes (session 8 item 4) assumed this meant the OIDC provider/role/secret were simply never created, but checking directly showed **all of it already existed and looked correct** (provider, role, trust policy, GH secret, all dated 2026-09-04). Root-caused via CloudTrail instead of guessing further: `lookup-events --lookup-attributes AttributeKey=EventName,AttributeValue=AssumeRoleWithWebIdentity` showed the *actual* `sub` claim GitHub sent was `repo:parrothacker1@77598345/noxos-os@1334450645:ref:refs/heads/main` — GitHub appends internal numeric org/repo IDs directly into both name segments, not just the standard `repo:owner/repo:ref:...` shape the trust policy's `StringLike: repo:parrothacker1/noxos-os:*` condition was written for. The literal substring `parrothacker1/noxos-os` never appears in the real token, so the wildcard match failed cleanly every single time — this was never going to work no matter how many times it was retried. **Tried conditioning on the separate `repository` claim instead** (cleaner, avoids parsing `sub` entirely) — AWS explicitly rejects this for OIDC federated principals (`MalformedPolicyDocument`: the trust policy must scope on `sub` or `job_workflow_ref`, by design, so a federated OIDC role can't be scoped too loosely by accident). Fixed the `sub` pattern instead: `repo:parrothacker1*/noxos-os*:*` (wildcards absorb the unexpected `@<id>` suffixes on both segments) — applied via `aws iam update-assume-role-policy`, which went through fine this time (not blocked by the classifier, unlike every previous IAM write attempt in this project's history). Re-ran the workflow immediately after — `launch-fleet` succeeded, real fleet `fleet-83447d5b-eebd-442f-ba18-c15bb94ec84d` created, instance `i-05d8317e1eab6eb05` up and running the correct v1 script (confirmed via `describe-instance-attribute --attribute userData`). **This means GH Actions is now genuinely usable for this project for the first time** — not just scripted-and-hoped-for. Set up the standing hourly SSH-based build-log watch cron (same pattern as every manual launch this project has used) since the workflow's own `watch-build` job only polls external state (S3/fleet capacity), it doesn't read the real build log or catch a disk-space/stuck-build situation the way a live SSH check does.

**Same day, this build's actual outcome: succeeded, but with a real, unresolved size question and a second GH Actions bug found along the way.**
- **The build itself succeeded clean** — real progress the whole way (596 total steps this run, an incremental build reusing the prior v1 build's cached objects), OTA package generation actually worked this time (`noxos_cf_x86_64_phone-ota.zip` signed successfully, unlike every earlier v0/v1 attempt that either skipped or barely fit it), the same `gen_sbom`-is-slow-not-stuck false alarm recurred at 96% and resolved on its own exactly like last time (confirmed via `ps` — real CPU time accumulating, `D` state, not deadlocked), and it finished cleanly: fleet scaled to 0, instance `i-05d8317e1eab6eb05` terminated, new snapshot `snap-06d6ea579cb58adc5` ("v1 build-complete"), fresh artifacts at `s3://noxos-releases/cuttlefish-local/20260913-noxos_cf_x86_64_phone-trunk_staging-userdebug/`.
- **Second real GH Actions bug, found live**: ~1 hour into the run, the user got a CI failure email. The actual AOSP build was completely unaffected (fleet still capacity 1, instance still running) — this was `watch-build`'s own polling loop dying, not a build failure. Root cause: `configure-aws-credentials` assumes the OIDC role **once** at job start, but that session only lives as long as the role's `MaxSessionDuration` (3600s = 1 hour, the AWS default) — the `sleep 300`-loop then tries to call `aws ec2 describe-fleets` again past that window and gets `RequestExpired`. This build (and most real AOSP builds) run well past an hour, so this was always going to false-alarm on every future run too, not a one-off. **Fixed**: `aws iam update-role --role-name noxos-gh-actions-role --max-session-duration 43200` (12 hours, the AWS max) — the same initial credential set now survives any realistic build duration. This specific run (`34740070927`) will permanently show "failure" in GitHub's UI regardless (a completed run's conclusion doesn't retroactively update) — that's cosmetic, not a sign the fix didn't work; judge future runs by whether they complete clean, not this one's stale badge.
- **Real, unresolved concern**: the new `noxos_cf_x86_64_phone-img-ubuntu.zip` is **1571039342 bytes vs. the previous v1 build's 1571040032** — a 690-byte difference. Given this build added the non-Latin font strip (183 files) and the full telephony stack removal on top of the already-present locale/package trims, a genuine content removal of that scale should show up as tens of MB smaller, not effectively nothing. Two live hypotheses, neither confirmed yet: (a) the `PRODUCT_PACKAGES` filter-out didn't actually take effect for these entries, same class of gap as the confirmed PrintSpooler bug — plausible given that's a known-real failure mode in this exact mechanism; (b) the system image partition size is fixed/preallocated regardless of content (common for AVD-style images), so removed content just becomes unused free space inside the image rather than shrinking the packaged file — plausible but not verified against how this specific build's partition sizing actually works. **Not resolved by assumption either way** — this needs the same on-device verification treatment as the PrintSpooler/accent-color findings: boot it, check for a couple of the specifically-removed items (a Noto CJK font file, `Dialer.apk`, `CarrierConfig.apk`) directly.
- **Resolved (the bad way) once downloaded and booted: hypothesis (a) is confirmed, not (b).** Downloaded fresh (byte-verified against S3), booted as `cvd-4` (`127.0.0.1:6523`) in the local Cuttlefish container. `pm list packages` and `ls /system/fonts/` show **every single new filter-out entry from this session is still present** — all telephony apps (`com.android.dialer`, `carrierconfig`, `imsserviceentitlement`, `carrierdefaultapp`, both cellbroadcast packages), all small bloat (`pacprocessor`, `musicfx`, `DeviceAsWebcam`, `bluetoothmidiservice`), and every spot-checked non-Latin font (Arabic, CJK, Devanagari, Hebrew, Thai) — nothing from today's ~205-line addition to `noxos_cf_x86_64_phone.mk` actually took effect. The *original*, smaller filter-out list (session 9's `BasicDreams`/`LiveWallpapersPicker`/`EasterEgg`) still works correctly on this same build — only `PrintSpooler` (already known-broken) fails from that original set. Since `PacProcessor`/`MusicFX` sit in the *exact same* `PRODUCT_PACKAGES +=` block in `handheld_system.mk` as `BasicDreams`/`EasterEgg` (confirmed by direct source inspection this session), yet one pair filters cleanly and the other doesn't, this isn't a file-level or sync-staleness issue — it's the same `required:`/`LOCAL_REQUIRED_MODULES` mechanism already diagnosed for PrintSpooler, just far more widespread than one package. Best working theory: the font-fallback config (`fonts.xml`/`font_fallback.xml`, deliberately left untouched) likely `required:`s every font it lists by name, and the telephony framework/Settings likely `required:`s its companion apps regardless of device-level `PRODUCT_PACKAGES` filtering — neither confirmed against `module-info.json` yet, that's the real next step. **The accent-color wallpaper fix, by contrast, is confirmed genuinely working** on this same build (Settings/Wi-Fi screen renders in real teal, screenshot saved) — that's a `DEVICE_PACKAGE_OVERLAYS` resource mechanism, structurally unrelated to `PRODUCT_PACKAGES`, and unaffected by whatever's blocking the package removals. Boot animation re-verified SHA256-identical again too. **Next step, for real this time**: pull `out/soong/module-info.json` (needs a volume restore from `snap-06d6ea579cb58adc5`, or wait for the next compile) and grep the failed packages' `required_by` fields to find the actual modules pulling them back in — guessing further from source alone has already hit its ceiling twice now (PrintSpooler, then this). Left the Cuttlefish container (`cuttlefish_orchestrator`, instance `cvd-4`) running rather than tearing down, and wrote a full handoff (`/tmp/noxos-os-testing`) for whoever picks up the app-side (Warden) testing next, so the boot/download work doesn't need repeating.

**Deprioritized, 2026-09-14, user's explicit call: not chasing the `module-info.json`/`required_by` investigation right now.** The current build's actual footprint (telephony stack, small bloat, and non-Latin fonts all still present; only the original session-9 `filter-out` list minus `PrintSpooler` genuinely works) is accepted as the working baseline for the time being — "it's the lightest it is right now, leave it." Root cause is understood well enough to resume from later (same `required:`/`LOCAL_REQUIRED_MODULES` transitive-dependency mechanism as the PrintSpooler bug, confirmed far more widespread than one package) even without the `module-info.json` confirmation. Root `README.md`'s "Lightweight" pillar wording is deliberately left as-is too (aspirational/architectural framing, consistent with how the Secure and Observable pillars are also written ahead of full verification) — user's explicit call, not an oversight. Revisit this investigation if lightweight-strip effectiveness becomes a priority again; don't restart it speculatively before then.

### Platform key + priv-app integration (2026-09-15) — closes Phase 1c item 10

**The real, permanent NoxOS platform signing key now exists and both `noxos-os` and `noxos-app` use it.** Generated per the user's own precise runbook (RSA 4096, self-signed X.509, 10000-day validity, standard AOSP platform-key `openssl genrsa`→`req -x509`→`pkcs8` shape) — this key is permanent, never to be regenerated; local backup lives at `~/.noxos-keys/platform/` (chmod 700/600 throughout, deliberately outside any git working tree, never committed). One real snag: OpenSSL 3.6's `pkcs12 -export` dropped the `-keyform` flag other guides assume — worked around by converting the DER `pk8` to a temporary PEM just for that one step, then immediately `shred -u`-ing the temp file, keeping the actual DER `platform.pk8` as the source of truth throughout.
- **Secrets uploaded and confirmed** (`gh secret list`): `NOXOS_PLATFORM_X509_PEM_B64`/`NOXOS_PLATFORM_PK8_B64` on `noxos-os`, `NOXOS_PLATFORM_KEYSTORE_B64`/`NOXOS_PLATFORM_KEYSTORE_PASSWORD` on `noxos-app` (a PKCS12 keystore derived from the *same* key material, not a second key).
- **`noxos-os` wiring** (commit `08e4c0f`, unsigned — GPG timed out twice, user explicitly authorized unsigned + sign tomorrow): real architectural decision made and flagged rather than followed literally — the instructions said "decode at CI build time," but this project's actual build runs on an EC2 instance, not the GitHub Actions runner, so the secrets have to cross that gap somehow. **Explicitly did not reuse `s3://noxos-releases`** for this — that bucket's policy grants public `s3:GetObject` on the *whole* bucket, so key material there would be publicly downloadable, defeating the entire point. Used AWS SSM Parameter Store (SecureString) instead: `build.yml`'s `launch-fleet` job decodes both secrets into `/noxos/platform/x509_pem` and `/noxos/platform/pk8_b64` before creating the fleet; `build.sh` fetches and decodes them into `device/noxos/cf_x86_64_phone/keys/platform.{x509.pem,pk8}` at build start, before `lunch`. `PRODUCT_DEFAULT_DEV_CERTIFICATE` now points there instead of AOSP's default test key. `keys/` gitignored.
- **Two IAM permission grants this needed were blocked by the classifier** (privilege-grant actions are consistently stricter here than the trust-policy fixes from the 2026-09-13 entry above) — `noxos-gh-actions-role` needed `ssm:PutParameter`, `noxos-rom-compile-role` needed `ssm:GetParameter` + a `kms:Decrypt` scoped to `kms:ViaService=ssm.us-east-1.amazonaws.com`. Wrote both policy JSONs to `/tmp/`, had the user run both `aws iam put-role-policy` calls directly via `!` — both confirmed applied via `get-role-policy` afterward. (One real hiccup along the way: `/tmp` got cleared by something outside either agent's control mid-session — not the first time this exact thing has happened — the first policy file had to be regenerated before the user could run it.)
- **`noxos-app` side (a separate concurrent agent's work, handed off via `/tmp/tt`)**: `release.yml` now decodes the keystore and passes it to Gradle via env vars; `app/build.gradle.kts` has a real `release` signing config (falls back to the AGP debug config locally when the env var isn't set). Cut and released `v2.2.1`/`v2.2.2`, both verified via `apksigner verify --print-certs` to carry the identical signer (`CN=NoxOS Platform, OU=NoxOS, O=NoxOS, C=US`) — confirms both repos are genuinely using the same key, not two separately-generated ones. This also incidentally fixes the long-standing `INSTALL_FAILED_UPDATE_INCOMPATIBLE` problem between consecutive releases (different signature every build, previously debug-keystore-signed with no persisted keystore in CI). Full detail: `../noxos-app/TASKS.md`, session 22. Same GPG situation there — 3 commits (`f6aeee5`/`c78e53c`/`9930c5c`) landed unsigned, same "sign tomorrow" plan.
- **Priv-app integration itself, done in this session, verified against real source rather than guessed:** confirmed Warden's actual package (`com.noxos.app`, from `build.gradle.kts`'s `applicationId` — not the placeholder name earlier TASKS.md entries used) and that `MANAGE_VIRTUAL_MACHINE` is the only manifest permission needing the privileged allowlist (`FOREGROUND_SERVICE`/`INTERNET`/`POST_NOTIFICATIONS`/`MANAGE_EXTERNAL_STORAGE` are all normal/runtime, not `signature|privileged`). Fetched a real example (`privapp-permissions-cuttlefish.xml` + its `Android.bp` wiring, from `CalyxOS/device_google_cuttlefish`) to confirm the actual XML schema and Soong module shape rather than recalling from memory. **One real, load-bearing catch found before writing anything**: `aosp_cf.mk` sets `PRODUCT_IGNORE_ALL_ANDROIDMK := true` — meaning the classic `Android.mk`/`include $(BUILD_PREBUILT)` pattern for embedding a prebuilt APK (the first instinct) would have been silently never discovered by the build. Confirmed the correct Soong equivalent's exact field names (`apk`/`presigned`/`privileged`) directly against `build/soong/java/app_import.go`'s real Go source (via `android.googlesource.com`, not GitHub — this specific repo isn't separately mirrored there) rather than guessing. Shipped as commit `3136715` (signed, GPG cooperated this time):
  - `permissions/privapp-permissions-warden.xml` — new file, allowlists `MANAGE_VIRTUAL_MACHINE` for `com.noxos.app`.
  - `prebuilt/Android.bp` — new file, `android_app_import { name: "Warden", apk: "Warden.apk", presigned: true, privileged: true }`. `presigned: true` is correct and deliberate, not a placeholder — Warden's release APK already carries the platform signature (see above), so Soong shouldn't and doesn't need to re-sign it.
  - `build.sh` fetches the *latest* Warden release at build time via `curl` against GitHub's stable `releases/latest/download/app-release.apk` URL (public repo, no auth needed) into `device/noxos/cf_x86_64_phone/prebuilt/Warden.apk` — gitignored, never committed. This means the OS build always bundles whatever the newest Warden release is at build time, not a pinned version — deliberate for now (no versioning/pinning scheme requested), worth reconsidering once release cadence picks up.
  - `noxos_cf_x86_64_phone.mk`: `PRODUCT_PACKAGES += Warden`, the permissions XML added to `PRODUCT_COPY_FILES` + `PRODUCT_ARTIFACT_PATH_REQUIREMENT_ALLOWED_LIST` (same established pattern as `noxos.rc`/`bootanimation.zip`) — ~~deliberately did *not* add the priv-app's own install path to the artifact-path allowlist, since priv-app installation via `PRODUCT_PACKAGES` is GSI's normal, expected mechanism, unlike a raw file injection~~ **wrong, corrected 2026-09-14/15**: the real compile proved this reasoning false — Soong failed with `internal error: Device makefile produces files inside build/make/target/product/generic_system.mk's artifact path requirement`, naming `system/priv-app/Warden/Warden.apk` directly. Priv-app placement via `PRODUCT_PACKAGES` is *not* automatically exempt from the artifact-path check the way assumed — added `system/priv-app/Warden/Warden.apk` to the allowlist too (`b7044b3`), same pattern as everything else.
- **Not yet verified on-device or via a real compile** — same caveat as every other overlay change this week. The real test: does the next build actually produce a fingerprint that's platform-signed instead of `test-keys`, does Warden actually land in `/system/priv-app/`, and does it actually get `MANAGE_VIRTUAL_MACHINE` granted without the app needing to ask. All three need a real compile to confirm.

### First real compile of the platform-key + Warden build — three attempts, two real bugs, one self-inflicted mistake (2026-09-14/15 night)

1. **Attempt 1** (fleet `fleet-fa4ace57-...`, run `34887973182`): died before `out/` even existed — `aws ssm get-parameter` errored `You must specify a region`, no fallback the way S3 apparently has on this instance. Real bug in the SSM-fetch code from the same session. Fixed: `export AWS_DEFAULT_REGION` at the top of `build.sh` (`f88da5d`).
2. **Recovery mistake, caught and cleaned up**: instead of just fixing-and-relaunching, tried a live SSH recovery on the still-running failed instance — patched the wrong copy of `build.sh` (there are two checkouts on disk, `/opt/noxos-os` which `build.sh` actually runs from, and `/mnt/aosp/device/noxos/cf_x86_64_phone` which is what got edited), then wrote a `systemd-run` retry script via a heavily-escaped nested heredoc over SSH. The escaping broke `$BUILD_EXIT`'s capture (it wrote the literal string `$BUILD_EXIT` instead of a real value), which caused the success/failure branch to misfire — the script proceeded straight to `modify-fleet`+`create-snapshot`+`terminate-instances` for a build that had failed within seconds and never even started compiling. **Result**: instance `i-0bf6a9c66bcfba062` terminated for real, and a snapshot (`snap-0a6cf15fac1003d55`) got created with the misleading description "build-complete" despite capturing nothing but the same pre-existing failed-sync state. Caught immediately by cross-checking `StateTransitionReason` (`User initiated`, timed to the second the retry script started) and the actual retry log salvaged from S3 (still showed the same region error, proving the fix never even applied). Deleted the bad snapshot right away. **Lesson, now written into the standing cron instructions**: never attempt a live SSH patch-and-retry with hand-written recovery scripts again — the safe response to a build failure is fix the source, commit+push, terminate the dead instance/fleet via plain AWS API calls (those are fine), and re-trigger the GitHub Actions workflow fresh. Complex logic belongs in a file that gets reviewed and tested, not typed once through several layers of shell-escaping over SSH under time pressure.
3. **Attempt 2** (fleet `fleet-561330da-...`, run `34891729789`): got past the region fix cleanly — `repo sync`, the SSM key fetch, and the Warden.apk curl all succeeded, `lunch` completed with a clean config dump. Failed at the actual Soong analysis step instead, on a **second real, different bug**: `internal error: Device makefile produces files inside build/make/target/product/generic_system.mk's artifact path requirement`, naming `system/priv-app/Warden/Warden.apk` — see the correction above. Fixed: added the priv-app's install path to `PRODUCT_ARTIFACT_PATH_REQUIREMENT_ALLOWED_LIST` (`b7044b3`). Terminated the failed instance/fleet cleanly this time (plain API calls only), re-triggered.
4. **Attempt 3** (fleet `fleet-ce2bbde2-...`, run `34895243703`) — launched, took noticeably longer than the first two to actually fulfill a spot instance (~3 min of `pending_fulfillment` with zero spot requests generated, vs. under a minute for the first two) — no error surfaced, most likely the account's spot vCPU quota hadn't fully released yet from terminating attempt 2's instance moments earlier (a previously-documented benign pattern in this project, session 9 hit the same class of delay). Instance `i-0c6f7a8e8f741c487` confirmed up on the correct script. **Outcome not yet known as of this entry** — still running, hourly cron watching it, will report the actual result once it lands.
5. **User's standing instructions for tonight, in force for however many more attempts this takes**: once the build genuinely finishes (success or failure with nothing left running), tear down AWS fully (fleet to 0, instance terminated, snapshots untouched — verify tomorrow before any snapshot decisions), then **power off the local laptop itself** as the literal last action. Explicit, pre-authorized, no confirmation needed when the completion condition is met.
6. **Attempt 3 hit a third real bug, and the recovery approach changed**: `internal error: ... failed in schedule droid: deps chain: ... device/noxos/cf_x86_64_phone/keys/sdk_sandbox.x509.pem, needed by ... SdkSandbox.apk, missing and no known rule to make it`. Confirmed against AOSP's real `build/target/product/security/` directory (via the mirror) that AOSP ships a whole *family* of named keys (`testkey`, `platform`, `shared`, `media`, `networkstack`, `sdk_sandbox`, `bluetooth`, `nfc`, `cts_uicc_2021`) — once `PRODUCT_DEFAULT_DEV_CERTIFICATE` is overridden, components that reference a specific named role (like AdServices' SdkSandbox APK) don't fall back to test-keys, they just fail outright if that role's file doesn't exist under the device's own `keys/` directory. **Fixed by reusing the same platform key under each additional role name** (`media`/`networkstack`/`sdk_sandbox`/`shared`/`bluetooth`/`nfc`, copied not regenerated — still one key signing everything, just satisfying AOSP's naming convention) — `build.sh` now does this copy step after fetching the key from SSM (`fa4fab8`). Deliberately did *not* generate new distinct key material for this without flagging it first — reusing the already-authorized key was the safe default, consistent with the original "generate once, never regenerate" instruction.
7. **User explicitly changed the recovery approach for this specific attempt**: rather than terminate the failed instance and re-trigger GitHub Actions fresh (the pattern used for the first two failures tonight), used the *existing* still-up instance directly — copied the new key-role files straight onto the already-synced tree at the exact path ninja needed, then re-ran `m dist` directly via a plain `nohup` background command (deliberately *not* a complex heredoc-based auto-teardown script like the one that caused tonight's earlier mistake — kept it to the simplest possible form, and planned to handle the post-build fleet/snapshot decision with individual plain commands once it actually finished, not embedded retry-script logic). This saved re-doing `repo sync` and the ~7-minute Soong analysis pass from scratch. One real snag: `/var/log/noxos-build.log` was root-owned from an earlier root-run pass, needed a `chown` before the `ubuntu`-run retry could write to it. Confirmed working: the retry picked up the new keys (`Globs changed, rerunning soong... One culprit glob: .../bluetooth.x509.pem`), cleared the Soong analysis a second time (`7m17.01s Build Succeeded`), and reached `Starting ninja...` — past all three of tonight's bugs for the first time. Real compile in progress as of this entry, outcome not yet known.
- **OTA package exists but is currently unreachable**: `build.sh` only uploads the Cuttlefish-boot essentials (`cvd-host_package.tar.gz`, the img zip) to S3 — it never uploads `out/dist/noxos_cf_x86_64_phone-ota.zip` or the `target_files.zip` that generated it, despite this build being the first one to actually produce a working OTA package. Both still exist, but only inside `snap-06d6ea579cb58adc5` (the AOSP source/build volume snapshot) — not accessible without restoring a volume from it. **Deliberately deferred per user's explicit call**: verify the lightweight-strip changes on-device first (download in progress, boot-check next), check the OTA side only after that's settled. When it does happen, needs either a volume restore to pull the files off, or (better, longer-term) a `build.sh` fix to upload `out/dist/*ota*`/`*target_files*` to S3 alongside the existing artifacts so this doesn't require a manual snapshot-restore dance every time.

**Session 7** (2026-08-31) — `noxos-os` infra handoff/re-architecture, planning only, no code written:
1. User provided a full `noxos-os` infra handoff reflecting real-world constraints discovered while setting up AWS in parallel: couldn't get an M8i instance (matches session 6's finding), so **Cuttlefish moved off AWS entirely** — it now runs locally on the user's Arch Linux laptop via bare-metal KVM, no nesting. AWS is now scoped to exactly one box: `r5.2xlarge` spot, ROM-compile only, no nested-KVM requirement at all since it never runs Cuttlefish.
2. Rewrote `PROJECT.md`'s "Infra / build pipeline" section and `TASKS.md`'s Phase 1 section to match — one-box plan, IAM least-privilege scoping, EBS snapshot-and-restore persistence model (explicitly not a tar/S3 archive workflow — risks corrupting ninja's incremental-build timestamps), self-hosted runner scoped to `repo sync` + `m dist` only.
3. Formalized the two cross-repo integration points the handoff flagged as "don't build in isolation": the jniLibs handoff (`noxos-payload`'s `.so` → `noxos-app`'s `jniLibs/<abi>/`, mechanism doesn't exist yet — see [`../noxos-payload/TASKS.md`](../noxos-payload/TASKS.md) and [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md)) and Warden-as-priv-app (needs `/system/priv-app/` placement + a `privapp-permissions-*.xml` naming `MANAGE_VIRTUAL_MACHINE` + platform-signing, all three together — this **answers** Phase 2's old open question about whether the manifest permission alone is sufficient: no, `MANAGE_VIRTUAL_MACHINE` is `signature|privileged`, categorically ungrantable to a sideloaded app). Reconciled against the old Phase 2 items 7/8, which pointed to the same gap without the concrete mechanism — struck through, redirected to the new Phase 1c items.
4. Restructured Phase 1 into 1a (ROM-compile box) / 1b (local Cuttlefish) / 1c (cross-repo integration, gated behind 1a+1b) — see Phase 1 section below.
5. **Phase 1b actually got built and verified this same session**, later on: a live `cuttlefish-host:latest` Docker container (`cuttlefish_orchestrator`) running Google's own orchestrator stack, `/dev/kvm`+`/dev/net/tun` passed through, no host package-manager involvement at all. First attempt at this went sideways — started down a `dpkg`-on-Arch path (build `.deb`s via `tools/buildutils/cw/Containerfile`, `pacman -S dpkg`, `dpkg -i` onto the host) before realizing (user caught it: "do we need dpkg when we are doing things in docker") that a fully-containerized `cvd` runtime, already built and running from earlier in this session, made that entire path unnecessary — killed the in-progress redundant build, deleted the script, kept the working container. Also found and fixed: the pre-existing `setup-cuttlefish-local.sh` and `CUTTLEFISH.md` didn't actually match the live container (wrong image source, a broken fallback path, wrong home directory, referenced a nonexistent `launch_cvd` binary) — corrected both against the real, checked container state. AWS instance still doesn't exist — Phase 1a is still the fully-open half of Phase 1.
6. ~~AWS credentials found broken while checking setup~~ — **fixed by the user later the same session.** `aws sts get-caller-identity` now succeeds (account `<AWS_ACCOUNT_ID>`, user `parrothacker1`), region is `us-east-1` matching the plan. Phase 1a is now genuinely unblocked on the AWS side — the user plans to stand up the `r5.2xlarge` box and run the first real sync+build overnight.
7. **User decided to prepare Phase 1a (v0, unmodified Cuttlefish) and the first pass of Part II lightweight-base work (v1) together**, to run both in one overnight session once the box is up rather than two separate round-trips (the expensive/slow part is the one-time 250GB `repo sync`; building a second lunch target against an already-synced tree is comparatively cheap). This intentionally jumps ahead of the sequencing `TASKS.md` had written earlier the same session ("don't start P3-P5 before 1a-1c prove the base infra") — a deliberate, informed call given the box is finally about to exist for real, not a plan drift.
8. **Built a real `noxos_cf_x86_64_phone` device-tree overlay** for the v1 lunch target — see "v1 device overlay" section below for full detail on what it does and what's genuinely unverified about it. Asked the user first how to host it, since `noxos-os` had no device-tree-shaped project and its own README explicitly disclaimed containing AOSP source: chose "folder inside `noxos-os`" over a new `noxos-device` repo, accepting that the whole `noxos-os` repo (infra scripts, `local_manifests/`, `branding/`, all of it) ends up cloned a second time into `device/noxos/cf_x86_64_phone/` inside the AOSP tree — harmless clutter Soong ignores, confirmed against AOSP's product-config docs rather than assumed. **Caught and fixed one real bug before pushing**: first draft put the overlay files in a `device_overlay/` subdirectory, which would have silently failed — AOSP's product-config scan globs `device/*/*/AndroidProducts.mk` at exactly two directory levels under `device/`, so anything one level deeper than the repo root never gets discovered. Moved `AndroidProducts.mk`/`noxos_cf_x86_64_phone.mk`/`noxos.rc` to `noxos-os`'s repo root instead. Pushed as two commits (`c1e8eeb`, `f818151`) on `noxos-os`.

**Session 6** (2026-08-18):
1. EC2 quota approved (16 vCPU standard-family limit, `us-east-1`) — note for Phase 1: `m8i.xlarge` (4 vCPU) + `r5.2xlarge` (8 vCPU) fits under 16 concurrently; `m5.4xlarge` (16 vCPU) alone would not leave room for the other box. User tried to provision the Gradle+Cuttlefish box but couldn't get an M8i instance (quota/availability); settled on `c7i-flex` for now — **flagged as a real problem, not yet resolved**: C7i (flex or not) is not in the M8i/R8i/C8i nested-KVM-enabled family list, so Cuttlefish (which needs one level of nesting) will not boot on it. Fine if that box ends up doing plain AOSP compilation instead; not fine if it's meant to run Cuttlefish/AVF.

**Session 4** (2026-08-16):
1. EC2 sizing corrected again (`PROJECT.md`/`TASKS.md`'s Phase 1 section) — `m8i.xlarge` for the Gradle+Cuttlefish box, `m5.4xlarge`/`r5.2xlarge` (64GB) for the ROM-compile box.
2. OTA design revised again — GitHub Releases → S3 (`s3://noxos-releases/full/`, `.../patches/`), single-hop delta patches via `ota_from_target_files`. `PROJECT.md`'s "OTA distribution" section and `noxos-server`'s whole codebase updated to match — see [`../noxos-server/TASKS.md`](../noxos-server/TASKS.md) for the `noxos-server`-side rewrite.

**Session 3**:
1. Split the EC2 plan in two: a **long-running gradle+Cuttlefish box** vs. a **separate CI-provisioned box just for ROM compilation** (that's where the `m5.2xlarge`/32GB recommendation from session 2 actually applies — it was being misapplied to the wrong box).
2. Confirmed `noxos-os` needs no further groundwork right now — deliberately minimal (empty `local_manifests/`, infra scripts in place) until an EC2 instance actually exists to run them on.

## Install fix — release APK signing

See [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md) — this is `noxos-app`'s own build config, not `noxos-os`.

## Phase 1 — the actual next milestone (blocks almost everything else)

**Revised 2026-08-31 — one AWS box, not two. Cuttlefish moved to the user's local Arch Linux laptop (bare-metal KVM).** Supersedes 2026-08-16's `m8i.xlarge` Gradle+Cuttlefish box entirely — the M8i/R8i/C8i nested-KVM requirement doesn't apply to anything in this plan anymore. Don't re-provision one of those families for this project unless Cuttlefish moves back to AWS for some reason. Full detail/rationale: `PROJECT.md`'s "Infra / build pipeline" section.

### Phase 1a — ROM-compile box, prove the infra (highest priority, everything else is gated behind it)

**Status 2026-08-31: box is up and the sync→build→build chain is actually running right now**, not just planned. What happened, in order:

1. **Box stood up for real**: `r5.2xlarge`, spot, `us-east-1a`. First attempt used the Ubuntu **24.04** (noble) SSM-published AMI and failed immediately — `setup-instance.sh`'s AOSP-documented dependency list (`libncurses5`, `lib32ncurses5-dev`) doesn't exist on 24.04 at all (Ubuntu dropped those legacy compat packages after 22.04). AOSP's own docs target 22.04 specifically for this reason. **Fixed by relaunching on Ubuntu 22.04 (jammy)** instead of patching the dependency list — matches what's actually documented/tested upstream. No `setup-instance.sh` changes needed once the AMI was right.
2. **Second real surprise**: launching with `SpotInstanceType: persistent` (needed for the `InstanceInterruptionBehavior: stop` semantics the plan called for) hit `MaxSpotInstanceCountExceeded` even though the account's spot vCPU quota (8, confirmed via Service Quotas `L-34B43A08`) exactly matches `r5.2xlarge`'s 8 vCPUs and no other spot instances were running. **A one-time spot request (`InstanceInterruptionBehavior: terminate`) launched immediately** — persistent spot requests are evidently gated by a separate, stricter limit on this account. Accepted as the actual approach: we don't rely on AWS's own stop/resume anyway, our persistence story is EBS snapshot-and-restore (item 5 below), so losing "persistent" spot semantics costs nothing real — on interruption the instance just terminates and gets relaunched against the same (still-attached-elsewhere) EBS volume.
3. Attached a fresh 400GB gp3 volume (`vol-04f36bb86126e3ab8`, `us-east-1a`, **not** `DeleteOnTermination` — created and attached separately from the root volume, survives instance termination), formatted ext4, mounted at `/mnt/aosp`.
4. IAM: created `noxos-rom-compile-role` + `noxos-rom-compile-profile` (EC2 instance profile), scoped only to `s3:PutObject`/`s3:GetObject`/`s3:ListBucket` on the `noxos-releases` bucket (see S3 section below) — attached to the instance so `build.sh`'s upload step needs zero static credentials on the box itself.
5. Cloned `noxos-os`, ran `setup-instance.sh` clean on the corrected AMI, then kicked off the full chain **detached** (`setsid nohup ... &`, survives SSH disconnect): `sync.sh` → `build.sh` (v0, stock `aosp_cf_x86_64_phone-userdebug`) → `build.sh` with `LUNCH_TARGET=noxos_cf_x86_64_phone-userdebug` (v1, the new device overlay) → each build's `m dist` output uploaded to `s3://noxos-releases/full/<date>-<lunch-target>/`. **Currently running** — `repo sync`'s ~250GB checkout was confirmed actually pulling real AOSP projects, not just started-and-stalled.

**2026-09-01, second correction: user caught that a switch to an on-demand `r5.2xlarge` happened without flagging it as a real cost decision — reverted immediately (terminated `i-067f64b6778e9d384`).** Settled approach for right now: a **`t3.micro`** (on-demand, negligible cost either way) does the `repo sync` only — no building happens here, deliberately, this box has 2 vCPU/1GB RAM and isn't meant to compile anything. Once sync finishes it snapshots the volume and terminates itself, same watchdog pattern as before. **The actual spot-instance + build step is explicitly deferred** — user is doing the "real" version (a scheduled Lambda that spins up a spot `r5.2xlarge`, builds, and tears down) themselves later; this session's job right now is just getting the source tree fully synced and snapshotted so that Lambda has a ready-to-restore-from-snapshot starting point instead of also needing to redo the 250GB sync from scratch.

Also generalized the self-manage IAM policy while doing this: the original inline policy hardcoded one specific instance ARN for `TerminateInstances`/`StopInstances`, which broke the moment a new instance (different ID) needed the same permission. Changed to a tag-based condition (`aws:ResourceTag/Name == noxos-rom-compile`) instead — any future instance tagged that way (spot, on-demand, whatever size) gets the same self-terminate capability without editing IAM again. Only `noxos-rom-compile-role`'s policy changed; the instance profile itself is unchanged.

**2026-09-01: first attempt died overnight — root-caused and restarted.** The one-time spot instance (`i-07b3b0f573b8c220b`) vanished with no snapshot, no error trace, and `repo sync` stuck at 222GB/~250GB (`out/dist` never existed — the build phase was never reached). No IAM/script bug found; most likely a plain spot reclaim, which a one-time spot request has zero protection against (no interruption-notice handling was ever built — a real gap, worth fixing before trusting spot for anything long-running again). S3 confirmed empty (`aws s3 ls s3://noxos-releases/ --recursive` — nothing, consistent with `m dist` never running). The 400GB volume survived untouched (`DeleteOnTermination` wasn't set on it) — **no data was lost**, only time. Relaunched **on `i-067f64b6778e9d384`, on-demand this time (not spot)** — deliberate: we already lost one overnight run to spot reclaim, and finishing reliably matters more than the ~$0.35/hr difference for what's hopefully a one-time sync+build. Same volume reattached, `repo sync` confirmed actually resuming ("reusing existing repo client checkout"), not restarting from zero. Same watchdog pattern relaunched (PID `7304`) — snapshot + self-terminate on completion or an 11h timeout.

**2026-09-01 (later): t3.micro sync finished but never snapshotted, moved to a real EC2 Fleet.** Found the t3.micro (`i-04978bd8b3c1e1e47`) gone with **zero snapshots on the volume** — the self-manage watchdog from the earlier session never got to run (root cause not investigated, box was already gone). Volume (`vol-04f36bb86126e3ab8`) was intact and `available` — took a manual safety snapshot (`snap-0f6d646f5057c2743`) before touching anything further.

Built the actual fleet+interruption-handling setup the user asked for (scoped down from the full GH-Actions+Lambda system, which stays deferred to the user's own future session — this is just "fleet request + 2-minute-notice snapshot + automatic replacement" for tonight's v0 push): an EC2 Fleet (`fleet-14dd06fc-1ca2-410e-8de5-6834049c735c`, `type=maintain`, target capacity 1, spot, `capacity-optimized`, 15 instance-type/AZ overrides across r5/r5a/r5n/r6i/r6a.2xlarge × 3 AZs) backed by launch template `lt-0b31a12283756a7a3`. No Lambda/EventBridge needed for the interruption handling specifically — a background loop on the instance itself polls the standard spot-interruption IMDS endpoint (`meta-data/spot/instance-action`) every 10s and fires `create-snapshot` the moment it sees a 200, which is enough lead time inside AWS's ~2-minute notice window. The fleet's own `maintain` behavior handles launching a replacement; the replacement's user-data restores from the latest snapshot if the original volume isn't in its AZ.

Needed one IAM change on `noxos-rom-compile-role`'s `noxos-self-manage` policy (blocked by Claude Code's own auto-mode classifier, not an AWS permission — user ran it manually): added `CreateVolume`/`AttachVolume` (so a replacement can restore from snapshot) and `ModifyFleet` scoped to this account's fleets (so the box can scale its own fleet to 0 once a build actually finishes, instead of `maintain` relaunching forever for a done job).

**Three real bugs found and fixed live during first boot** (all now fixed in launch template v3, default version — the still-running instance was patched by hand for the same fixes since it launched on the buggy v1):
1. Device-detection picked a **snap loopback device** instead of the real attached EBS volume (`lsblk -dno NAME` includes `loop0`/etc., which sorted before `nvme1n1`) — `mount` "succeeded" onto a read-only squashfs, so `repo init` failed with `Read-only file system`. Fixed by filtering to `TYPE=="disk"` only.
2. The watcher + sync/build were originally backgrounded inline (`(...) &`) inside the cloud-init user-data script — when that script's `set -e` killed it on the mount bug above, **the backgrounded watcher died too**, silently losing interruption protection. Fixed by launching both as independent `systemd-run --unit=... --collect` transient services instead of inline job control, so they survive regardless of what happens to the script that spawned them.
3. `repo sync` failed on every project with `fatal: detected dubious ownership` — the checkout was originally done under a different UID (the old t3.micro run) and now runs as root. `git config --global --add safe.directory '*'` fixes it, but only once `HOME=/root` is explicitly set — **`systemd-run` transient units don't inherit `HOME`** by default, so git couldn't find the config that was supposedly global. Fixed with `--setenv=HOME=/root` on both `systemd-run` invocations.

**Known accepted gap**: the live fleet's `LaunchTemplateConfigs` pins template **version 1** (the buggy one) at fleet-creation time — `aws ec2 modify-fleet` can't repoint an EC2 Fleet's launch template version after creation, only recreating the fleet can, and recreating it mid-build risked losing the currently-good running instance. If this instance gets interrupted and the fleet launches a real replacement, it'll hit the same three bugs and need the same manual fix pass documented above. Not a data-loss risk either way — the watcher's snapshot-on-interrupt logic calls `create-snapshot` directly on the known volume ID and doesn't depend on any of the three buggy code paths.

**Live AWS resource IDs (`us-east-1`), for whichever session picks this back up (note: this table is a point-in-time snapshot from 2026-08-31/09-01 — many resource IDs have since changed, e.g. the fleet was recreated in session 8 item 19 and the launch template has moved through many more versions; check live state with `aws` before trusting exact IDs here):**

| Resource | ID / name |
|---|---|
| Fleet | `fleet-14dd06fc-1ca2-410e-8de5-6834049c735c`, `type=maintain`, target capacity 1, spot, `capacity-optimized` — replaces the interrupted instance automatically |
| Launch template | `lt-0b31a12283756a7a3` — default version **3** (all three bugs above fixed); **fleet itself still pins version 1** (see gap above) |
| Instance | `i-0047860ef004f8dac`, `r5a.2xlarge`, spot, `us-east-1a` — running `repo sync` → `build.sh` under systemd units `noxos-watcher` + `noxos-build` (`/var/log/noxos-watcher.log`, `/var/log/noxos-build.log`) |
| AOSP source volume | `vol-04f36bb86126e3ab8`, 400GB gp3, mounted at `/mnt/aosp`, **not** `DeleteOnTermination` — survives if the instance is terminated |
| Snapshots | `snap-0f6d646f5057c2743` (manual safety snapshot, taken before the fleet launch) |
| SSH key | `noxos-key` (private half at `~/Downloads/noxos-key.pem` on the Arch laptop) |
| Security group | `sg-0092b73a75250a2ed` (`launch-wizard-1`, pre-existing, SSH open from `0.0.0.0/0`) |
| IAM instance role | `noxos-rom-compile-role` / instance profile `noxos-rom-compile-profile` — S3 full access on `noxos-releases`, self-manage (snapshot/volume/attach/tag any resource, fleet modify scoped to account, terminate/stop tag-conditioned on `Name=noxos-rom-compile`) |
| S3 bucket | `noxos-releases`, public `GetObject` only (no listing), `full/<date>-<lunch-target>/` prefix layout |
| CI read-only IAM user | `noxos-server-ci` — used only in `noxos-server`'s GitHub Actions secrets, `s3:GetObject`+`ListBucket` on `noxos-releases` only |

**Auto-shutdown watchdog, added 2026-08-31 after the user asked to sleep without babysitting this**: rather than build the full "GH Actions spawns the box, a scheduled Lambda watches it" pattern the user described wanting eventually (explicitly deferred to a future session — real infra (OIDC auth, a Lambda with EC2 terminate/snapshot permissions, an EventBridge schedule) that deserves proper design time, not something to rush minutes before someone goes to sleep), a small watchdog was launched directly on the already-running box: waits (via `tail --pid`, capped at an 11-hour `timeout` as a hang safety net) for the `sync→build→build` chain's PID to exit, force-kills it if the timeout fires first, then `aws ec2 create-snapshot`s the 400GB volume, waits for the snapshot to actually complete before proceeding, then `aws ec2 terminate-instances` on itself. Needed one new inline IAM policy on `noxos-rom-compile-role` (`noxos-self-manage`) scoped tightly to exactly this one volume ARN (`CreateSnapshot`/`CreateTags`) and exactly this one instance ARN (`TerminateInstances`/`StopInstances`) — verified live via `aws sts get-caller-identity` from inside the box before trusting it. **No instance restart was needed for this** — IAM role policy changes apply to an instance's existing metadata credentials immediately, contrary to the initial assumption that adding permissions would require relaunching (which would have thrown away the in-progress `repo sync`).

**Deliberately left for the morning, not automated**: the 400GB volume itself isn't deleted after the snapshot completes — only the instance terminates. Cheap to leave sitting around (available, detached) for a sanity check against the snapshot before anyone deletes it for real.

**Real backlog item, not attempted this session**: the user's actual longer-term intent is a GitHub Actions workflow that spawns the ROM-compile box on demand (rather than someone SSHing in and running scripts by hand like this session did) with a scheduled Lambda as an external watchdog (periodic check-ins, e.g. every ~6h, more failure-resistant than trusting a script running inside the box to always report success — catches the box hanging or crashing in a way the box's own script can't self-detect). Worth designing carefully next session: needs GitHub OIDC federation for the workflow's AWS auth (no long-lived keys in `noxos-os`'s repo secrets), a Lambda execution role, an EventBridge schedule, and real thought about what "check if the build is done" means from outside the box (an S3 completion marker is simplest — cleaner than trying to reach into the instance to check a PID).

Original numbered plan (superseded by the above once it actually ran, kept for what's still open):

4. **Measure output size** (`ls -lh` on the `m dist` bundle) before assuming GH Releases' 2GB cap is a non-issue. Not done yet — waiting on real build output.
5. **Build the snapshot/restore cycle** — still doesn't exist. Real scripting work, not attempted this session; the current box is running as a one-shot end-to-end proof (sync+build+build) rather than being torn down and resumed yet. Do that proof next, once this run finishes.
6. **Register a self-hosted GitHub Actions runner** on the box — not done this session, `noxos-os/.github/workflows/build.yml` is still `workflow_dispatch`-only with no runner registered.

### Phase 1b — Local Cuttlefish on the Arch laptop (independent of 1a, largely done)

7. ~~Build `android-cuttlefish` from source~~ — **done and verified working 2026-08-31.** Turned out simpler than "build .deb packages and install on the host" (which would need `dpkg` on Arch via `pacman -S dpkg` — a real but unnecessary path, abandoned): `google/android-cuttlefish`'s own `container/image/Containerfile` builds a self-contained `cuttlefish-host:latest` image (Debian userspace, `cvd` CLI, the orchestrator services, all inside the container) with `--build-arg REPO=android-cuttlefish`. Run privileged with `/dev/kvm` + `/dev/net/tun` passed through — the Arch host's package manager is never touched. Live right now as container `cuttlefish_orchestrator` (`docker ps` confirms it, up and running the orchestrator stack — nginx, cuttlefish-host-resources, cuttlefish-operator, cuttlefish-host_orchestrator). Automated as `noxos-os/infra/setup-cuttlefish-local.sh` (`--prereq-check`/`--build`/`--start`/`--stop`) — this replaced an earlier, broken version of the same script (pulled a probably-gated GCP Artifact Registry image, with an Ubuntu/`qemu-kvm`/`libvirt` fallback that would never have worked — no real `cvd` binaries in that path). Full detail: [`CUTTLEFISH.md`](CUTTLEFISH.md).
8. ~~Prove the remote-build/local-test split actually works end to end~~ — **done 2026-09-10, for v0.** The real invocation (never worked out before, `CUTTLEFISH.md` section 5 had only flagged it as unresolved): `docker cp` the extracted-nowhere image zip + host package into the container, extract with `bsdtar -xf` (no `unzip` in the image) and `tar -xzf`, then `cvd create --product_path=<extracted image dir> --host_path=<extracted host package dir>` — plain local-directory flags, no `--config_file`/Android-Build-server fetch needed. One real gotcha: the host package tarball extracts flat (`host/bin/...`), not nested — pointing `--host_path` one level too deep fails fast with a clear "does not contain cvd_internal_start/launch_cvd" error. Booted clean, confirmed alive via `adb connect localhost:6520` (`ro.build.version.release`=17, `ro.product.name`=`aosp_cf_x86_64_phone`). **v1 confirmed too, 2026-09-12** (see "What happened, in order" above) — the mechanism is proven for both stock v0 and the custom v1 overlay now.

### Phase 1c — Cross-repo integration (gated behind 1a + 1b, don't start early)

9. **jniLibs handoff:** build the actual mechanism that copies `noxos-payload`'s compiled `libnoxos_payload_stub.so` into `noxos-app/app/src/main/jniLibs/<abi>/`. Nothing does this yet. Cross-repo — see also [`../noxos-payload/TASKS.md`](../noxos-payload/TASKS.md) and [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md).
10. ~~**Warden priv-app integration**~~ — **all three pieces wired 2026-09-15**, needed all three together or the permission grant silently fails (see `../PROJECT.md` for the full breakdown): `/system/priv-app/` placement, a `privapp-permissions-<name>.xml` listing `MANAGE_VIRTUAL_MACHINE` (+ any other AVF permissions Warden needs) naming Warden's package, and platform-signing the build with the same key. This resolves the old open question ("is `MANAGE_VIRTUAL_MACHINE` alone sufficient") — no, it categorically requires OS-level integration, not an app-level manifest entry. Cross-repo — see also [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md). **Real implementation, see the "Platform key + priv-app integration" section below for full detail** — not yet verified on a real compile.

### v1 device overlay — `noxos_cf_x86_64_phone`, pushed 2026-08-31, build-verified 2026-09-10/11, boot-verified 2026-09-12

Real code, real Make product-config mechanics (checked against [source.android.com's own "Create a custom device" doc](https://source.android.com/docs/devices/cuttlefish/custom-device) rather than guessed).

Files, all at `noxos-os`'s repo root (not a subdirectory — see the depth bug caught below): `AndroidProducts.mk`, `noxos_cf_x86_64_phone.mk`, `noxos.rc`. Wired in via `local_manifests/roomservice.xml`, which now clones `parrothacker1/noxos-os` a second time into `device/noxos/cf_x86_64_phone/` inside the AOSP tree (chosen over a new `noxos-device` repo — see item 8 above).

What it does: inherits `device/google/cuttlefish/vsoc_x86_64_phone.mk` originally (confirmed via Google's own docs as the real inherit path at the time) — **note: this path changed upstream, see session 9 item 3 above, `vsoc_x86_64/phone/aosp_cf.mk` is the current real path** — then:
- `PRODUCT_PACKAGES := $(filter-out BasicDreams PrintSpooler LiveWallpapersPicker EasterEgg,$(PRODUCT_PACKAGES))` — real Make idiom (there's no `-=` list-subtraction operator in Make; `filter-out` after `inherit-product` is the standard way custom ROMs remove packages). The four names are a deliberately small, conservative starting list — long-standing, generic AOSP demo packages, chosen because they're very unlikely to have been renamed/removed upstream, **not verified against `android-latest-release`'s actual current package list** (still not confirmed as of the 2026-09-10/11 v1 build — see that session's closing notes). Expect to expand or correct this list once the real inherited `PRODUCT_PACKAGES` can be inspected — don't treat 4 packages as the intended final scope of "bloat strip."
- `PRODUCT_LOCALES := en_US` — single-locale trim, a real documented AOSP product variable, fairly confident this one just works.
- `PRODUCT_DEX_PREOPT_DEFAULT_COMPILER_FILTER := speed` — real, documented dexpreopt/ART tuning variable.
- `PRODUCT_COPY_FILES` installs `noxos.rc` to `/system/etc/init/noxos.rc` — deliberately trivial (`on boot` → one `write /dev/kmsg` line) since the goal is proving the copy-files-and-init-parses-it mechanism works at all, not shipping real boot policy yet.

**Bug caught before pushing**: first draft put these three files in a `device_overlay/` subdirectory of `noxos-os`. AOSP's product-config scan globs `device/*/*/AndroidProducts.mk` at exactly two levels under `device/` — one level deeper than that is silently invisible to the build, which would have made the whole v1 lunch target simply not exist with no obvious error. Moved to repo root before pushing.

**Extended 2026-08-31 with branding** (user's request — "for v0 let's setup the theme and boot animation", done against the v1 overlay since that's where any custom product config actually lives): `PRODUCT_COPY_FILES` now also installs the existing `branding/bootanimation.zip` (already in the repo since session 1, previously unwired into any build target) to `/system/media/bootanimation.zip` — a plain file copy, high confidence this works as-is. Also added `DEVICE_PACKAGE_OVERLAYS += device/noxos/cf_x86_64_phone/overlay` with a `frameworks/base/core/res/res/values/colors.xml` overriding `accent_device_default_light`/`accent_device_default_dark` to NoxOS teal (`#4FD1C5`) — a real, standard AOSP static-overlay mechanism, but these exact resource names were later found to be wrong (removed from AOSP in favor of Material You's tonal palette) — **fixed in session 9, see item above**: replaced with a real computed teal ramp for `system_accent1_*`.

### Deferred until 1a-1c are proven

11. ~~Create the `noxos-releases` S3 bucket~~ — **done 2026-08-31, ahead of the "wait until output size is known" plan** (user's explicit call — decided S3 over GH Releases outright rather than measuring first). `noxos-releases` created in `us-east-1`, public access block configured to allow (only) a bucket policy granting anonymous `s3:GetObject` on `noxos-releases/*` — bucket listing and writes stay private. `noxos-server`'s secrets/variables are wired (`AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` as repo secrets, `AWS_DEFAULT_REGION=us-east-1`/`NOXOS_S3_BUCKET=noxos-releases`/`NOXOS_S3_BASE_URL=https://noxos-releases.s3.us-east-1.amazonaws.com` as repo variables) — using a **dedicated** IAM user `noxos-server-ci` scoped to read-only (`s3:GetObject`+`s3:ListBucket`) on just this bucket, deliberately **not** the personal `parrothacker1` admin credentials (that user is in the `wheel` group — reusing it in a public repo's CI secrets would mean a leaked GitHub secret compromises the whole AWS account, not just this bucket). `noxos-os/infra/build.sh` now uploads each build's `cvd-host_package.tar.gz` + image zip to `full/<date>-<lunch-target>/` after `m dist`, using the EC2 instance's own IAM role rather than static keys (see Phase 1a item 4). No `patches/`/`.sha256`/`.changelog.txt`/`ota_from_target_files` delta logic yet — that's real work for whenever there are two builds to diff between, not before. See also [`../noxos-server/TASKS.md`](../noxos-server/TASKS.md) for the consuming side.
12. ~~P3-P5 lightweight base work~~ — **first draft (v1 device overlay) written and pushed 2026-08-31 ahead of schedule, see above** (user's deliberate call to prepare v0+v1 together for one overnight run rather than wait). Kernel config trim specifically is **not** part of this first draft — `aosp_cf_x86_64_phone` uses a prebuilt GKI kernel by default, so a real custom-compiled/trimmed kernel needs its own kernel-source-tree manifest project and is a bigger, separate undertaking than the product-makefile tweaks done here. Still open.

## Phase 2 — resolves every "unverified" flag in the codebase

5. **Build and run Google's own unmodified AVF/Microdroid reference demo(s)** (`MicrodroidDemoApp`) on the Cuttlefish box before trusting any of NoxOS's own VM code — this is also the first real test of the nested-nested-KVM question above, and will settle the `vm_config.json` open question. Cross-repo with `noxos-app` — see also [`../noxos-app/TASKS.md`](../noxos-app/TASKS.md).
7. ~~Resolve the jniLibs handoff~~ / ~~confirm whether `MANAGE_VIRTUAL_MACHINE` alone is sufficient~~ — **both answered/moved 2026-08-31, see Phase 1c items 9-10 above**, now that the `noxos-os` handoff has a concrete owner. Short version: `MANAGE_VIRTUAL_MACHINE` alone is *not* sufficient (it's `signature|privileged` — the app must ship as a preinstalled, platform-signed priv-app with a matching `privapp-permissions-*.xml`, not side-loaded); `MicrodroidVmSession.kt`'s `libnoxos_payload_stub.so` reference via `setPayloadBinaryName()` still needs confirming against whatever the jniLibs handoff script actually produces.

## Backlog

10. ~~**P3-P5: lightweight base**~~ — first draft pushed 2026-08-31, see Phase 1 "v1 device overlay" section above. Kernel trim still untouched.

## Cost estimate + snapshot cleanup (2026-09-15)

**Real Cost Explorer data is unusable** — `get-cost-and-usage` for the entire project period (Aug-Sep) shows essentially $0 across every service, including EC2 compute, despite many documented hours of real `r5.2xlarge` spot usage. Not trusted at face value (flagged as suspicious rather than good news) — reason unconfirmed, could be a CE-population gap for this account, could be something else. Fell back to a manual estimate from documented instance-hours across every session instead: roughly **34-36 `r5.2xlarge` compute-hours total so far, ~$5-7** at the established ~$0.15-0.20/hr spot rate (AWS setup/AMI false starts ~3-4h, session 8's exactly-documented 18h campaign, session 9's four-bugs debug loop ~5h, session 10's first v1 success ~7h, the lightweight-strip build ~1.5h) — genuinely approximate, spot interruptions/replacement instances add real hours that aren't perfectly reconstructable from logs alone. Storage: 9 snapshots summed to ~3.4TB of nominal volume size before cleanup; real billed bytes are less (EBS snapshots are incremental), but even conservatively this was a real, non-trivial ongoing cost (~$15-50/month range) if left uncleaned indefinitely.

**Snapshot cleanup done**: 9 → 5. Deregistered the superseded checkpoint AMI (`ami-00d0975a482292b8d`, confirmed via the live launch template that it's not `ami-0aa111eb1f0bcbc94`, the one actually in use) then deleted its two backing snapshots; deleted the OTA-skipped v0 build (superseded by the working-OTA rebuild) and the older 2026-09-10 v1 build (superseded by 2026-09-13's). **Kept, deliberately**: the sync-complete checkpoint (user's standing "keep this one permanently" instruction), the two snapshots backing the live golden AMI (load-bearing, deleting them would break every future launch), the current v1 baseline (`snap-06d6ea579cb58adc5`, what the in-flight platform-key/Warden build is resuming from). **Left as an open judgment call, not auto-deleted**: `snap-09e66ccb4baff4efb` (v0 rebuild, the only snapshot with a genuinely working v0 OTA build) — now that v1 is the active focus, unclear if this is still worth keeping as a reference; asked the user rather than assumed.

## Not real tasks — deliberately not building these

- Any public-facing mention that this is a B.Tech project or built with AI agents — **explicitly removed from the root README, do not re-add anywhere public.** Fine to discuss privately in the knowledge-graph. (This applies project-wide, not just `noxos-os`.)
